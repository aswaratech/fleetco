{
  "name": "@fleetco/orchestration",
  "version": "0.1.0",
  "private": true,
  "description": "Autonomous multi-ticket orchestration loop for FleetCo. Operator tooling, not application code. See docs/runbook/orchestration-loop-design.md.",
  "type": "module",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "start": "tsx src/index.ts",
    "dev": "DEBUG=1 tsx src/index.ts",
    "tui": "tsx src/tui.ts",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.1.0",
    "@anthropic-ai/sdk": "^0.32.0",
    "@slack/socket-mode": "^2.0.2",
    "@slack/web-api": "^7.7.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "tsx": "^4.19.0",
    "typescript": "^5.5.0",
    "vitest": "^2.1.0"
  }
}
