engine-strict=true
auto-install-peers=true
# Supply-chain guard for anyone running plain npm here by mistake (pnpm is the
# package manager; pnpm's own posture lives in pnpm-workspace.yaml). Semgrep
# npm-missing-minimum-release-age, ADR-0012 baseline.
min-release-age = 7
