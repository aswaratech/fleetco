# Current phase

> **As-of block (update whenever the governing set changes) — last updated 2026-07-22.**
> **Phase:** Phase 2 — Driver app and telematics (opened ahead of the gate per ADR-0025, whose forward license ADR-0041 closed on 2026-07-02).
> **Governing scope gates:** ADR-0041 (no new pull-forward programs without a fresh PO-ratified ADR), with two commitment-4 exceptions: **ADR-0042** (hardware GPS trackers + live map — **the first production deploy is its opening ticket M1**; code-complete, only the operator-led M1 deploy + M10 pilot remain) and **ADR-0043** (AI chat agent — **construction complete, A1–A8 code-complete**; [2026-07-05] the training opt-out is REQUESTED and the client path is VALIDATED once against the real DeepSeek API on a local stack — real client bound, tool dispatch + non-zero tokens per the runbook's §Verification — so its first production USE now waits only on the M1 deploy + setting the key in `/opt/fleetco/.env`; **that same validation session also surfaced a fabricated-write-confirmation incident** — the model claimed a demo driver was created with no matching tool call — closed same-day by a deterministic same-turn ungrounded-claim guard [ADR-0043's 2026-07-05 append-only annotation]; correcting the record here so "validated" is not read as "found nothing"). Newly governing with this PR's merge: **ADR-0044** (agent conversational data entry — ask-when-missing prompting, registry completion 34→39 with the exclusions reaffirmed, image-based entry via a no-tools vision seam; both decision boxes RESOLVED 2026-07-05 — identity documents INCLUDED under local-only processing, vision = SELF-HOSTED OCR sidecar [candidate `baidu/Unlimited-OCR`, CPU-serving shape + sizing to be validated by V0; DeepSeek's API is text-only, verified]) + **ADR-0045** (financial-records classification: Tier 3 + named handling rules). **ADR-0041 commitment 4 exercised a third time** — build-before-deploy by PO decision with the deploy-first recommendation declined; all production use still M1-gated. **Newly governing with this PR's merge: ADR-0046** (WhatsApp agent channel over the ADR-0043 agent — Twilio inbound/outbound, per-user verified phone links, `agent:use` unchanged, zero new dependency; **commitment 4 exercised a fourth time**; text path now, receipt photos built-but-inert behind the paused image intake; all production use M1-gated as the inbound webhook needs a public 24/7 HTTPS endpoint). **Newly governing with this PR's merge: ADR-0047** (trip dispatch + driver acceptance — the OFFERED hard-assign → driver-Accept reshape that finally gives the central Trip aggregate the haulage order it executes: a reusable pinned `Site` aggregate + `sites:*`, the order columns + `OFFERED`/`ACCEPTED` + milestone timestamps on Trip, the admin Sites + dispatch web surfaces, the driver Requests/Accept/order-detail + Navigate deep-link + inline map + progress taps, and a **Mock-only** `RoutingProvider` seam [the live Google/OSM impl M1-gated]; **commitment 4 exercised a fifth time**, argued afresh in its §"Phase mismatch" as the *lower-risk* ordering for a central-aggregate reshape before production trip data exists; the near-identical standing vehicle↔driver assignment stays distinct-and-deferred; W1–W9 code-complete, all production use M1-gated like every prior exception). **Newly governing with this PR's merge: ADR-0048** (active trips on the admin live map — a read-time, **zero-new-backend** composition of the ADR-0047 trips read and the ADR-0042 positions feed on `/map`: the OFFERED/ACCEPTED/IN_PROGRESS layer, always-on Tier-3 site pins, consignee Tier-2 stripped pre-island, no route lines/trails, a second slower 60 s trips poll; **commitment 4 exercised a sixth time** with the deploy-first recommendation surfaced and declined — see its §Phase mismatch; production use M1-gated like every prior exception). **Newly governing with this PR's merge: ADR-0049** (fleet documents & renewals — FleetDocument storage on vehicles/drivers/customers through the ADR-0039-c7-pre-authorized `ObjectStorage` seam [PDF+image, magic-byte-sniffed, Tier-2 bytes, authed streaming only], RenewalRecord history with an atomic renew action snapshotting old→new expiry + linking the paper and its ExpenseLog cost, and a documents reminder source over the ADR-0038 scan with the vehicle-compliance-category exclusion; `documents:read/write` operational + `documents:delete` ADMIN-only; **commitment 4 exercised a seventh time — the first post-deploy exercise**, daily use not yet established, recommendation surfaced and declined — see its §Phase mismatch). Alongside it, **ADR-0050** (staff agreements + SUPERVISOR role) is **Proposed with OPEN decision boxes and build-blocked** — it records the capability-matrix / assignment-scoping / staff-entity questions for the PO and sanctions NO code until resolved and ratified.
> **Permitted work streams:** the deploy + its owed gate items; ADR-0042 tickets M1 + M10 (operator-led); ADR-0043 activation (the deploy-gated items above — no agent construction remains); driver-app D4–D6 (RESUMED 2026-07-10 by explicit PO re-prioritization — ADR-0041 c2 in-phase completion, the deploy-first alternative surfaced and declined; build path = local prebuild per the ADR-0033 c7 discharge annotation; hardware trackers remain the primary producer per ADR-0042 — **D4–D6 COMPLETE with D6 on 2026-07-13; this in-phase stream is now exhausted, so per ADR-0041's Revisit-when the deploy is the sole remaining feature-side path**); and the always-allowed bug/security/docs/debt work (ADR-0041 c3); plus, once ADR-0044/0045 are ratified by merge, the Program-V/P tickets (P1 prompt hardening, P2 update tools, V2 storage move, V3–V7 image intake — V6/V7 gated on ADR-0044's Box-B provider choice + the V0 photo eval). The W-program (ADR-0046) is **CODE-COMPLETE — W1–W5 all merged/shipped 2026-07-09/10** (governance, phone-link identity + ledger, sender seam + renderer, the signature-guarded webhook + worker [text end-to-end], and the photo path inert behind the image pause): no agent construction remains on it; its activation (Twilio sender + webhook registration + `TWILIO_*` secrets + the PO's phone link, per `docs/runbook/whatsapp-operations.md`) is operator-led and M1-gated. Plus, once ADR-0048 is ratified by this PR's merge: its single implementation slice — the web-only active-trips layer on `/map` (helpers → pin extraction → surface, per the ADR-0048 plan), the only code work it sanctions. **Plus, once ADR-0049 is ratified by this PR's merge: its F2–F6 tickets** (F2 FleetDocument aggregate + storage + API, F3 renewal records, F4 documents web UI, F5 renewal web UI, F6 document-expiry reminder source; one PR each, F6 may run ahead of F4/F5) — with **F0** (the RESEND_FROM wiring + production env-template repair, PR #245) always-allowed under c3 regardless of ratification. **ADR-0050 sanctions NO work** while its boxes are open.
> **First production deploy: LANDED 2026-07-19** (this line previously read "still not done" — repaired 2026-07-22; the deploy-record PR #244 updated the DORA entry and runbooks but not this block). The one-time bootstrap executed end-to-end on the Hostinger KVM 2 VPS at `https://200-141-0-43.sslip.io` (images built on-box per the ADR-0014 first-bootstrap fallback under the GitHub Actions billing lock; deployed SHA e04fbb7), the first nightly encrypted backup landed in R2, and the restore-from-backup drill ran and passed the same day — see the 2026-07-13 entry in `docs/operations/dora-metrics.md` and the three ACTIVE deploy runbooks. **Still owed from the gate: Phase-1 DAILY USE** (the CEO's spreadsheets are not yet retired; the deployed system must become the daily tool), plus live 28-day SLI reporting and the weekly DORA rhythm now started. The M-program's M10 pilot and every built program's activation (agent key, WhatsApp, reminders' RESEND config, R2) are operator-led post-deploy steps. **Known operational blocker: GitHub Actions is billing-locked since 2026-07-19** — every workflow run dies at startup, so CI cannot run on PRs and image builds fall back on-box; merges proceed on local-gate evidence until the operator clears the billing lock.

**Current phase: Phase 2 — Driver app and telematics** (opened ahead of the Phase-1 daily-use gate per ADR-0025; the first production deploy + daily use remain **owed** — see the gate note in the Phase-1 entry below). This file is a chronological running log: it opens with Phase 0 (Kickoff) and Phase 1 ("The Spine") and continues through the Phase-2 work.

Phase 0 (Kickoff) is complete: the monorepo scaffold, NestJS API skeleton, Next.js admin skeleton, Postgres + Redis via Docker Compose, Prisma baseline + auth migration, CI on GitHub Actions, the security baseline jobs from ADR-0012, the baseline documentation, the agent guardrails, the orchestration loop (ADR-0022), and basic auth scaffolding for a single admin user are all in place.

Phase 1 replaces spreadsheets and paper for daily fleet operations. The system in Phase 1 is web admin only, with the CEO as the only user. The phase is built as a sequence of vertical slices per ADR-0006 (schema → migration → service → API → UI → test), in this order: Vehicles, Drivers, Customers, Jobs, Trips, Fuel logs, Expense logs, Reports v1. Operational milestones (first production deploy, first restore-from-backup test, SLI instrumentation, OpenTelemetry, performance budget) are interleaved across the same window.

The first three Phase-1 aggregates have shipped: Vehicles (iters 1–4: schema + read + write + tests), Drivers (iters 6–7: read + write + tests), and Trips (iters 8–9: read + write + tests, with the FK Restrict → 409 mapping closing the cross-aggregate delete-blocker loop). Iter 10 landed the first cross-slice read affordance — "Recent trips" sections on the Vehicle and Driver detail pages, plus a "View N trips" deep-link inside the 409 delete-blocker dialogs. Iter 11 closed the Trip → Vehicle odometer auto-update loop: when a Trip transitions to COMPLETED, the service bumps the referenced Vehicle's `odometerCurrentKm` inside the same Prisma interactive transaction as the trip write, so a vehicle's odometer reading on the detail page reflects the latest completed trip without operator intervention. Iter 12 added per-vehicle lifetime stats (completed trip count, total km logged, most recent driver) under `GET /api/v1/vehicles/:id/stats`, surfaced as a "Lifetime stats" section on the Vehicle detail page. Iter 13 shipped the symmetric per-driver lifetime stats — `GET /api/v1/drivers/:id/stats` plus the matching "Lifetime stats" section on the Driver detail page — rotated 90° around the Trip aggregate (most-recent-vehicle replaces most-recent-driver). Iter 14 extended the Vehicle aggregate with Nepal-specific compliance metadata: eight nullable columns plus an `InsuranceType` enum (Bluebook number + expiry, insurer + policy number + type + expiry, route permit number + expiry), surfaced on the Vehicle create form, edit form, and a new "Compliance" section on the detail page. Bishesh Anumati, expiring-soon badges, and the `<NepaliDate>` BS-calendar component remain deferred to their own slices. Iter 15 opened the Customers aggregate with the read path: `model Customer` (name, contactPerson, phone, email, panNumber unique-when-present, address, `CustomerStatus` enum ACTIVE/INACTIVE, plus the standard audit fields and `createdBy` FK), `GET /api/v1/customers` with status filter + sortable columns (name / createdAt) + pagination, `GET /api/v1/customers/:id` with 404 mapping, list / detail / loading UI under `apps/web/src/app/customers/`, and a fourth nav button on the gated home page. The pino redact list gains `*.contactPerson` so the new Tier 2 PII does not leak into logs. Iter 16 (this PR) closes the Customers aggregate by adding the write path: `POST /api/v1/customers` (201 + created row), `PATCH /api/v1/customers/:id` (200 + updated row), `DELETE /api/v1/customers/:id` (204), each guarded by `AuthGuard`; service-layer PAN normalization (trim + uppercase) makes the unique index case-insensitive in practice; Prisma `P2002` on `panNumber` is mapped to HTTP 409 with the response body `{ statusCode: 409, message, field: "panNumber" }` so the matching create / edit forms (under `apps/web/src/app/customers/new/` and `apps/web/src/app/customers/[id]/edit/`) surface the conflict inline on the PAN input via `form.setError`; Prisma `P2003` on `DELETE` is mapped to HTTP 409 with "Cannot delete customer: it is referenced by other records." — forward-compatible dead code today, exercised in iter 17 when Jobs adds its `customerId` FK under `onDelete: Restrict`. The detail page gains an Edit / Delete cluster mirroring Drivers iter 7; the delete dialog is a shadcn `AlertDialog` client island with a placeholder "View related records" link that iter 17 will swap to a `/jobs?customerId=…` deep-link. Iter 17 opened the Jobs aggregate with the read path: `model Job` (`jobNumber` unique [`JOB-YYYY-NNNNN`; generator lands iter 18], `customerId` FK Restrict to Customer, free-text `description`, `JobStatus` enum PLANNED/IN_PROGRESS/COMPLETED/CANCELLED, nullable scheduled/actual start+end dates, nullable `notes`, audit fields), `GET /api/v1/jobs` with status + customerId filters, sortable columns (jobNumber / scheduledStartDate / createdAt), pagination, `GET /api/v1/jobs/:id` returning the nested Customer, list / detail / loading UI under `apps/web/src/app/jobs/`, and a fifth nav button on the gated home. The Job→Customer FK flips the iter-16 forward-compatible Customer-delete-blocker from dead code to live: an integration test seeds a Customer + a referencing Job, attempts the delete, and asserts the `P2003 → 409` "Cannot delete customer: it is referenced by other records." path. Iter 18 (this PR) closes the Jobs aggregate by adding the write path: `POST /api/v1/jobs` (201 + created row with the nested Customer), `PATCH /api/v1/jobs/:id` (200 + updated row), `DELETE /api/v1/jobs/:id` (204), each guarded by `AuthGuard`; the jobNumber generator runs server-side (`JOB-YYYY-NNNNN`, current UTC year + highest existing per-year sequence + 1, retried up to 3 times on `P2002` so a concurrent insert cannot deterministically collide); `jobNumber` and `customerId` are immutable post-create (the `PATCH` schema's `.strict()` rejects both); the four date pairs (scheduled start/end, actual start/end) enforce end ≥ start as a service-layer cross-field rule that re-runs against the merged shape on `PATCH` (so a PATCH that touches a single date in a pair re-validates against the stored value for the other half); `P2002` on `jobNumber` is mapped to HTTP 409 with the response body `{ statusCode: 409, message, field: "jobNumber" }` so the matching create form can surface the (effectively unreachable, defensively handled) conflict inline; `P2003` on `customerId` is mapped to HTTP 400 with "Customer <id> does not exist." which the create form surfaces as a field-level error on the customer picker. Create / edit / delete UI lands under `apps/web/src/app/jobs/new/` and `apps/web/src/app/jobs/[id]/edit/` plus `[id]/delete-job-dialog.tsx`; the create form fetches the active customers list (sortBy=name asc, take=200) server-side and renders a native `<select>` picker; the edit form pre-fills every field, renders jobNumber + customer name as read-only, and PATCHes only the diff against initial values; the delete dialog is a shadcn `AlertDialog` client island with named "Delete job" / "Keep job" buttons. The Customer delete-dialog placeholder "View related records" link is swapped to the now-live `/jobs?customerId=<id>` URL with the clearer label "View jobs for this customer". Iter 19 (this PR) opens the Fuel logs aggregate with the read path: `model FuelLog` (`vehicleId` FK Restrict required, nullable `tripId` FK Restrict — a fill exists whether or not the operator pairs it with a trip; `date` UTC; integer `litersMl` per the money-as-minor-units rule mechanically extended to volume; integer `pricePerLiterPaisa`; integer `totalCostPaisa` carried over the wire today, derived server-side in iter 20; nullable integer `odometerReadingKm`; nullable free-form `station`, `receiptNumber`, `notes`; `createdById` FK Restrict to User), with indexes on `vehicleId`, `tripId`, `(date desc)` partial, and `createdById`; `GET /api/v1/fuel-logs` with vehicleId / tripId (cuid) and startDate / endDate (inclusive) filters, sortable columns (date — the default — and createdAt), pagination clamped to 200, and the standard `{ items, total, skip, take, sortBy, sortDir }` response shape; `GET /api/v1/fuel-logs/:id` returning the nested Vehicle (always present) and Trip (nullable); 35 new tests cover the service (filters, sort round-trip, take clamp, skip-past-end, pagination stability with `id` tiebreaker) and the controller (pipe-layer schema validation rejecting bogus keys / out-of-range bounds / non-cuid ids, controller integration shape with real Prisma). List / detail / loading UI under `apps/web/src/app/fuel-logs/` with the five-column list table (Date, Vehicle registration, Liters, Price / L, Total) and a sixth nav button "Fuel logs" on the gated home page after Jobs. Two new web lib modules centralise integer-minor-units formatters per CLAUDE.md §"Money & units": `apps/web/src/lib/money.ts` (`formatNpr(paisa)` — Intl en-IN currency, em-dash for null) and `apps/web/src/lib/units.ts` (`formatLiters(milliliters)` + `formatKm(km)`); both docstrings carry the round-trip note that the helpers are display-only — paisa and milliliters stay integers end-to-end in code, only stringified at render. Iter 20 (this PR) closes the Fuel logs aggregate by adding the write path: `POST /api/v1/fuel-logs` (201 + created row with the nested Vehicle and Trip), `PATCH /api/v1/fuel-logs/:id` (200 + updated row), `DELETE /api/v1/fuel-logs/:id` (204), each guarded by `AuthGuard`; `vehicleId` is immutable post-create (the `PATCH` schema's `.strict()` rejects it; the edit form renders the vehicle registration as a read-only disabled input) while `tripId` is mutable on `PATCH` (operators correct a mis-paired fill without re-keying the rest); `totalCostPaisa` is **derived** server-side via `Math.round(litersMl * pricePerLiterPaisa / 1000)` — half-up rounding so the worked example `12345 mL × 11055 paisa/L = 136473.975` rounds to `136474` paisa, never `136473`; the wire schemas (`CreateFuelLogSchema` + `UpdateFuelLogSchema`, both `.strict()`) explicitly omit `totalCostPaisa` and `createdById` so a client cannot pass either; `createdById` is filled from `request.session.user.id` per ADR-0021; a service-layer trip-vehicle consistency check rejects a `tripId` whose Trip belongs to a different Vehicle with "Trip <reg> belongs to vehicle <reg>; cannot pair with a fuel log for vehicle <reg>." (re-runs against the merged shape on `PATCH` so changing only `tripId` while leaving `vehicleId` alone still validates against the stored vehicle); Prisma `P2003` on insert / update is mapped to HTTP 400 with "Vehicle <id> does not exist." / "Trip <id> does not exist." which the web `actions.ts` layer pattern-matches to set the right field token (`vehicleId` / `tripId`) so the form highlights the right picker — see `docs/runbook/api-error-mapping.md` for the full P2003 split (delete-when-referenced → 409, insert/update with stale FK → 400). Odometer monotonicity (rejecting a fuel log whose `odometerReadingKm` regresses against the most-recent prior reading on the same vehicle) is explicitly deferred to a later iter and marked with a `// FUTURE` comment in `FuelLogsService.create`; the kickoff treats odometer monotonicity as a cross-aggregate invariant best landed alongside trip-end-odometer reconciliation. Create / edit / delete UI lands under `apps/web/src/app/fuel-logs/new/`, `apps/web/src/app/fuel-logs/[id]/edit/`, and `[id]/delete-fuel-log-dialog.tsx`, mirroring the Jobs iter-18 shape: the create form fetches active vehicles + recent trips server-side, the trip picker is filtered client-side to the watched `vehicleId` (and `tripId` is cleared whenever the vehicle changes), liters / price-per-liter are entered as decimals (step `0.001` / `0.01`) and converted to integer mL / paisa at submit via `litersToMl` / `rupeesToPaisa` in `apps/web/src/lib/fuel-logs-schema.ts`, and the total-cost preview re-runs `Math.round(liters * pricePerLiter * 100)` so the rendered preview equals the persisted `totalCostPaisa` bit-for-bit; the edit form pre-fills via inverse converters (`mlToLitersInput`, `paisaToRupeesInput`), PATCHes only the diff against initial values, and uses the `in` operator on the diff object so a nullable field touched-and-cleared sends `null` while an untouched field is omitted; the delete dialog is a shadcn `AlertDialog` client island with named "Delete fuel log" / "Keep fuel log" buttons. Iter 21 (this PR) opens the Expense logs aggregate with the read path: `model ExpenseLog` (**nullable** `vehicleId` FK Restrict to Vehicle [nullable because a vehicle-agnostic expense like the company's quarterly insurance premium is a valid ledger row], nullable `tripId` FK Restrict to Trip, `date` UTC, `category` the new `ExpenseCategory` enum [`MAINTENANCE` / `REPAIR` / `TOLL` / `PARKING` / `INSURANCE` / `PERMIT` / `FINE` / `OTHER` — the eight values cover the CEO's paper-ledger categories; adding a ninth is a one-line migration], integer `amountPaisa` per CLAUDE.md's money-as-minor-units rule [unlike Fuel logs there is no derived total — `amountPaisa` is the authoritative entered value, not a product of two factors], nullable free-form `vendor` / `receiptNumber` / `notes`, audit fields), `GET /api/v1/expense-logs` with vehicleId + tripId + category + startDate / endDate filters, sortable columns (date / amountPaisa / createdAt — `amountPaisa` is on the whitelist because "biggest expense first" is a routine query and we'd rather expose it via the list endpoint than add a dedicated reports route), pagination, `GET /api/v1/expense-logs/:id` returning the nested Vehicle (nullable) and Trip (nullable), list / detail / loading UI under `apps/web/src/app/expense-logs/` (the list em-dashes the Vehicle / Trip columns when null; the detail page swaps the Vehicle section for a "Not vehicle-attributable" notice when `vehicleId IS NULL`), and a seventh nav button on the gated home page. 39 new tests cover the service (filters compose; default date desc; vehicleId narrows AND excludes null rows while omitted filter includes them; tripId / category filters; date range; AND combos; take clamp; skip-past-end; sortBy round-trip; pagination stability; findById / getById happy + miss) and the controller (pipe-layer .strict() / sortBy-whitelist / category-enum / cuid / date defenses; integration list with the nullable-vehicle case; getById nested shape; getById 404). Iter 22 (this PR) closes the Expense logs aggregate by adding the write path: `POST /api/v1/expense-logs` (201 + created row with the nested nullable Vehicle and Trip), `PATCH /api/v1/expense-logs/:id` (200 + updated row), `DELETE /api/v1/expense-logs/:id` (204), each guarded by `AuthGuard`; `vehicleId` is **immutable on PATCH** (the `UpdateExpenseLogSchema` omits the field and `.strict()` rejects any client-sent value with HTTP 400 — mirror of the Fuel-log iter-20 rule, rationale: rewriting `vehicleId` silently rewrites the per-vehicle cost-report basis), while `vehicleId` IS optional on `POST` so a vehicle-agnostic expense (quarterly insurance premium, office stationery) is a first-class ledger row, and `tripId` is mutable on `PATCH` so the operator can pair / unpair after the fact; `amountPaisa` is **authoritative** — unlike Fuel logs there is no derivation, no preview row, and no server-side recomputation, the operator-entered value is stored verbatim (`CreateExpenseLogSchema` / `UpdateExpenseLogSchema` enforce only the bounds `1 ≤ amountPaisa ≤ 10_000_000_000`); the trip-vehicle consistency check fires only when **both** `vehicleId` and `tripId` are present (a vehicle-agnostic expense with a tripId is rejected as nonsensical at the request boundary; a tripId on a vehicle-bound expense re-validates against the merged shape on `PATCH`) with the body "Trip <reg> belongs to vehicle <reg>; cannot pair with an expense log for vehicle <reg>."; Prisma `P2003` on a stale `vehicleId` / `tripId` / `createdById` is mapped to HTTP 400 with the standard `<Field> <id> does not exist.` body which the web `actions.ts` layer pattern-matches to set the right field token (`vehicleId` / `tripId`); `createdById` is filled from `request.session.user.id` per ADR-0021 (the `.strict()` rejects it on the wire). 28 new controller-level tests (18 pipe-layer + 10 integration) + 21 new service-level tests cover the write path — total 510 API tests passing. Create / edit / delete UI lands under `apps/web/src/app/expense-logs/new/`, `apps/web/src/app/expense-logs/[id]/edit/`, and `[id]/delete-expense-log-dialog.tsx`, mirroring the Fuel logs iter-20 shape with the three expense-log-specific divergences: (a) the create form's vehicle picker has a leading "— no vehicle —" option and the trip picker disables when no vehicle is selected; (b) there is no "Total cost (computed)" preview block — a single `amount` (NPR) input with `step="0.01"` collects the rupees decimal that the action layer converts to integer paisa via `rupeesToPaisa` (re-used from `apps/web/src/lib/fuel-logs-schema.ts`); (c) the edit form has no vehicle picker at all (immutable) and shows the current Vehicle binding as a static row above the form so the operator sees but cannot change the binding. The form schemas live at `apps/web/src/lib/expense-logs-schema.ts`. Iter 23 (this PR) ships Reports v1, the final Phase-1 slice: a new read-only `apps/api/src/modules/reports/` module (`ReportsModule`, `ReportsController`, `ReportsService`) exposing `GET /api/v1/reports/per-vehicle-cost?from=YYYY-MM-DD&to=YYYY-MM-DD&vehicleId=<cuid>` (auth-gated; the `vehicleId` filter is optional). The service runs two Prisma `groupBy` aggregations in parallel (one over `fuelLog` with `_sum.totalCostPaisa` and `_count._all`, one over `expenseLog` with `_sum.amountPaisa` and `_count._all`, both keyed by `vehicleId` and bounded by an inclusive-through-end-of-day date filter on the `date` column — the user's reporting date, not `createdAt`, per the iter-22 rule), merges the two by `vehicleId` in memory, joins to the `Vehicle` table for the registration number, sorts by `totalPaisa` desc with `registrationNumber` ascending as the tiebreaker, and totals the rows. A separate `expenseLog.aggregate` over `vehicleId IS NULL` populates the response's `companyLevel: { expensePaisa, expenseLogCount }` block so the iter-22 vehicle-agnostic expenses (quarterly insurance premium, office stationery) surface in their own row beneath the totals on the report page rather than being attributed to any one vehicle — the `companyLevel` block is **independent of the `vehicleId` filter** so the operator sees that context regardless of which vehicle they're inspecting. Zero-activity vehicles do NOT appear as zero-filled rows; the rows array contains only vehicles with at least one matching FuelLog or ExpenseLog in the window. The `ReportsQuerySchema` (Zod, `.strict()`) accepts strict YYYY-MM-DD strings (a YYYY_MM_DD regex on top of `z.string` rejects ISO 8601 timestamps that would otherwise be silently truncated to midnight UTC), runs a cross-field refine that rejects `from > to` at the schema layer with `path: ["to"]` so the web form highlights the right input, and normalises an empty-string `vehicleId` to undefined so `?vehicleId=` on the URL means "no filter" rather than "match the empty-string id". The refine is defensively guarded against the per-field `DateOnly` transform returning `z.NEVER` — without the guard a bogus `from` or `to` format crashed the refine with `data.from.getTime is not a function` and surfaced as HTTP 500 instead of HTTP 400. 21 service-level tests + 14 controller-level tests (10 pipe-layer + 4 integration) cover the 12 ticket-required cases (empty range, fuel-only, expense-only, fuel+expense merged, multi-vehicle sort, vehicle-agnostic isolation, vehicleId-filter narrowing, inclusive from/to bounds, exclusive bounds outside window, from > to schema rejection, totals match row sums) plus tie-breaker (`registrationNumber` ascending), zero-activity vehicle absence, trip-pairing-doesn't-change-the-row, and the date-vs-createdAt rule — total 545 API tests passing. Web at `/reports/per-vehicle-cost`: server-rendered page with the date-range picker (default = current calendar month UTC), a single-vehicle filter populated from a parallel server-side pre-fetch against `/api/v1/vehicles?take=200&sortBy=registrationNumber&sortDir=asc`, a six-column table (Vehicle / Fuel / Expenses / Total / Fuel logs / Expense logs) where each registration links to `/vehicles/<id>`, every paisa value rendered via `formatNpr`, a totals row in the table footer, and a "Company-level (not vehicle-attributable)" sub-row beneath the totals when `companyLevel.expenseLogCount > 0`. An eighth nav button on the gated home page surfaces the route alongside the existing Phase-1 list pages. **Phase 1 is content-complete pending the Phase-1 retrospective**: the eight aggregates (Vehicles, Drivers, Customers, Jobs, Trips, Fuel logs, Expense logs, Reports v1) plus the cross-aggregate affordances (per-vehicle / per-driver lifetime stats, Trip→Vehicle odometer auto-update, jobs-by-customer deep-link, the trip-vehicle consistency check on Fuel logs and Expense logs) all ship; the operational milestones (first production deploy, first restore-from-backup test, SLI instrumentation, OpenTelemetry, performance budget) interleave across the same window per `docs/product/roadmap.md` §"Phase 1 — The Spine". The next phase is **Phase 2 — Driver app and telematics** per `docs/product/roadmap.md` (the canonical phasing; an earlier draft of this sentence misnamed it "Drivers payroll / Settlements" — that descriptor was not an operator decision and has been corrected to match the roadmap). Per the roadmap, a new phase does not start until the previous one is in **daily use**: Phase 1 is content-complete but not yet in daily use, so its remaining operational milestones (first production deploy, first restore-from-backup test, SLI instrumentation, OpenTelemetry, performance budget) plus a Phase-1 retrospective are the operator-led gate to Phase 2. See `docs/retrospectives/phase-1.md` for the Phase-1 close-out record — what shipped, the loop process-learnings, the carried tech-debt, and the explicit Phase-1 → Phase-2 gate checklist. Update (2026-06-03): ADR-0025 opens Phase 2 ahead of this gate as a one-time documented exception (see the post-content-complete note below); the deploy-dependent gate items remain owed in parallel.

**Post-content-complete progress (through 2026-05-30).** The agent-driveable half of the Phase-1 → Phase-2 gate shipped via the "Phase-1 gate — observability instrumentation + cleanup" loop program (PRs #55–#60): the NPR money-converter tech-debt was discharged (#55, relocated to `apps/web/src/lib/money.ts`); ADR-0024 records the OpenTelemetry decision (#56); OpenTelemetry is wired into the API by extending Sentry v9's tracer, with `trace_id`/`span_id` in the pino logs and an env-gated OTLP exporter (#57, `apps/api/src/observability/otel.ts`); both ADR-0011 SLIs emit structured per-request / per-operation signals (#58 API-availability, #59 trip-creation-success, `apps/api/src/common/sli.ts`); and the performance budget is committed to `docs/operations/performance-budget.md` (#60). Three follow-ups then landed: the Phase-1 retrospective's observability gate items were marked done (#61); **ADR-0014 (production deployment — single VPS + Docker Compose) was accepted** and the deploy / rollback / restore-from-backup runbooks were committed as `STATUS: DRAFT`, then hardened as pre-deploy readiness (#62 and follow-on); and a CORS-ordering bug was fixed by registering CORS before the better-auth mount in `apps/api/src/main.ts` (#63, merged 2026-05-30). `main` is green (567 API + 9 web tests). What still gates Phase 2 is the **deploy-dependent half** — the first production deploy (per the now-accepted ADR-0014: single VPS + Docker Compose), the restore-from-backup test, live 28-day SLI/FCP reporting, the first real DORA weekly entry, daily use, and the retrospective sign-off — all operator-led. The first production deploy is **deliberately deferred for now** (a scheduling decision, not a discipline waiver), so this half of the gate stays open; the DRAFT deploy/rollback/restore runbooks are being hardened in the meantime so the eventual deploy is a copy-pasteable procedure rather than an improvised one. **Per ADR-0025 (accepted 2026-06-03), Phase 2 is now opened ahead of the daily-use gate** as a one-time documented exception for the Phase-1 → Phase-2 transition only: Phase-2 development proceeds in parallel while the deploy-dependent items above remain **owed (deferred, not cancelled)**. The daily-use-before-next-phase rule stands for all future transitions. See `docs/architecture/decisions/0025-open-phase-2-ahead-of-gate.md` and `docs/retrospectives/phase-1.md` for the gate checklist.

**Phase 2 — first feature slice (2026-06-04): RBAC implemented.** The office-staff role-based access-control model (ADR-0028) is now in place — the first Phase-2 feature slice. Two live roles (**ADMIN**, **OFFICE_STAFF**) with **DRIVER** reserved-but-undefined; the role rides `session.user.role` (a better-auth `additionalFields` field, `input: false`); a single hardcoded capability map (`apps/api/src/modules/auth/permissions.ts`) plus a `RolesGuard` composing the existing `AuthGuard` enforce `@RequirePermission(...)` / `@RequireRole(...)` (403 ≠ `AuthGuard`'s 401, opt-in per route so the Phase-1 controllers keep working unchanged); `GET /me` returns the role for UI-gating; and an idempotent `apps/api/scripts/create-user.ts` privileged path creates office-staff (and the occasional ADMIN) accounts, setting the role via a direct Prisma write after sign-up because `input: false` blocks it through the public API. This is the access-control foundation the GPS/telematics slices (ADR-0027) build on. See ADR-0028 and the glossary's RBAC / RolesGuard / capability entries.

**Phase 2 — GPS telematics ingestion (ADR-0029), COMPLETE.** The five-ticket GPS ingestion program built the seam from "a ping arrives" to "a ping is stored, retained, and read back" against the architecture ADR-0029 settled. T1 (PR #80) wired BullMQ into NestJS — a thin `@Global()` `QueueModule` owning a dedicated `maxRetriesPerRequest: null` ioredis connection (kept separate from the health-probe connection), default job options, and `enableShutdownHooks()` for graceful drain — proven by an enqueue → process → drain smoke test. T2 lands the data model: `model GpsPing` with the **first PostGIS geometry in FleetCo** — the hybrid storage of ADR-0029 commitment 8 (native Float `latitude`/`longitude` as the canonical columns plus a `GENERATED ALWAYS … STORED geometry(Point, 4326)` column with a GIST index, the generated-column SQL and index hand-authored in the migration), every coordinate/movement column and the geometry carrying the `/// Tier 5 per ADR-0027` mark, the GPS attribute keys added to the pino `redact` denylist atomically with the schema, and the previously-deferred `TripsService.delete` `P2003 → 409` mapping resolved now that `GpsPing.tripId` (onDelete: Restrict) makes Trip a referenced aggregate (which also corrected a latent 500 for Trips already referenced by FuelLog/ExpenseLog). T3 (this slice) ships the ingestion path: a telematics feature module (`apps/api/src/modules/telematics/`) that owns the `gps-ingest` queue via `BullModule.registerQueue` (per-feature ownership; the root config stays in T1's `@Global()` `QueueModule`); an authenticated batch `POST /api/v1/telematics/pings` that validates minimally (Zod `.strict()`), enqueues, and returns 202 fast — gated by the new **`gps:ingest`** capability (ADMIN-held today, granted to the reserved DRIVER role later per ADR-0029 c11) on the composed AuthGuard + RolesGuard chain, with `createdById` from the session (ADR-0021), never the body; and a `@Processor("gps-ingest")` in-process worker that bulk-inserts each batch via `createMany` (the database derives the geometry — Prisma supplies only the Float columns), with the batch job as the trace unit (ADR-0026 c4) and no GPS coordinates in spans (the ADR-0026 scrub seam is NOT invented here — it does not exist yet). The producer (the driver app) does not exist yet, so the endpoint + worker are tested with synthetic batches posted as an authenticated principal. T4 (this slice) ships the retention prune: a dedicated `RetentionModule` (`apps/api/src/modules/retention/`) owns the `traces-prune` queue (per-feature, ADR-0029 c2) and registers a SINGLE keyed repeatable (bullmq 5.78's `upsertJobScheduler` — the non-deprecated Job Schedulers API, NOT `repeat` — id `traces-prune-daily`, cron `0 3 * * *`) idempotently at boot via `onApplicationBootstrap`; its `@Processor("traces-prune")` worker (concurrency 1 per ADR-0029 c5) hard-deletes `GpsPing` rows whose fix `timestamp` is older than the provisional 90-day window (`GPS_PING_RETENTION_DAYS`, a named constant flagged as the PO's to finalize at ADR-0027 acceptance — see `docs/tech-debt.md`), with the run wrapped in one span carrying only safe attributes (deleted count, cutoff, window-days — no GPS coordinates, no invented ADR-0026 scrub seam, no forced metrics seam since no MeterProvider exists yet). Hard-delete is the default per ADR-0029 c12 (aggregation-to-Tier-3-summary is the preferred end-state, a later ticket); the prune basis is the device fix `timestamp` (switching to storage `createdAt` for late offline flushes is a one-line change, documented). A dedicated module (not folded into telematics) follows ADR-0029 c2's "the retention concern owns `traces-prune`" and the in-repo ReportsModule precedent of querying another aggregate's table via the shared PrismaService, and leaves the T3 ingestion path untouched. T5 (this slice) ships the read path and the program's last ticket: the RBAC-gated raw/derived read split (ADR-0028 c6 / ADR-0027 c7) and the FIRST PostGIS geofencing, all on the existing telematics feature module with no schema change. Three read endpoints join the composed AuthGuard + RolesGuard chain, each with its own `@RequirePermission`: `GET /api/v1/telematics/vehicles/:vehicleId/pings` (the full-resolution raw trace — paginated, time-bounded by `from`/`to`, Prisma-native Float reads that NEVER select the generated geometry column) gated `gps:read-raw` (ADMIN-only, the most-privileged operational data access in the system); and `GET …/location` (the single latest fix for the live-location map) plus `GET …/geofence-status` gated `gps:read-derived` (ADMIN + OFFICE_STAFF). The geofence-status endpoint is the derived layer's first real spatial query: it classifies a vehicle's latest fix against a **caller-parameterized** geofence via parameterized `prisma.$queryRaw` — point-in-polygon by `ST_Contains(ST_GeomFromText(<wkt>, 4326), geometry)` and proximity by `ST_DWithin(geometry::geography, <center>::geography, <meters>)` (the geography cast makes the radius meter-accurate, the documented geometry-vs-geography choice; every value is bound via the tagged template, so there is no SQL-injection surface, and the `ST_MakePoint(lon, lat)` foot-gun is guarded by inside/outside + swapped-axes tests). It returns a single boolean (+ the non-sensitive fix time, no coordinates) — a genuinely lower-resolution Tier-3 derived status, not the raw trail relabeled (ADR-0027 c6 anti-circumvention). The two read capabilities were already in the hardcoded map from the ADR-0028 RBAC slice (which fixed the split), so T5 consumed them with no map edit. Geofence-**polygon storage** (a `geometry(Polygon, 4326)` company-configuration aggregate, Tier 3 per ADR-0027 c6) is deferred to its own sibling slice — it needs its own design (depot / customer-site / route-corridor types, naming, a management surface); the geofencing query is identical once a stored-polygon slice supplies the geometry from a row instead of a query param. Tier-5 egress discipline holds on reads (coordinates go to the authorized caller over the wire but never log via the pino `redact` list and never enter a span; the spatial `$queryRaw` returns only a boolean + timestamp). 37 new tests (670 API total) prove the RBAC gate (403/200/401) and the spatial inside/outside + within/beyond classification against real PostGIS. **With T5 the GPS telematics ingestion program (ADR-0029, T1–T5) is COMPLETE** — the seam from a ping arriving to a ping stored, retained, and read back is built end-to-end. See ADR-0029 and the glossary's PostGIS / Geofence / Geospatial / BullMQ / Retention entries.

**Phase 2 — Geofence storage model (ADR-0030), COMPLETE.** ADR-0030 is accepted (the geofence-configuration aggregate: hybrid PostGIS storage, the read/write RBAC split, and a v1 Leaflet map-drawing editor). The five-ticket geofence program (G1–G5) gives the T5 geofencing query stored fences to read against. G1 (merged) landed the data model: `model Geofence` + a `GeofenceType` enum (DEPOT / CUSTOMER_SITE / ROUTE_CORRIDOR), FleetCo's **first PostGIS `geometry(Polygon, 4326)`** — the hybrid of ADR-0030 c1 (a canonical Prisma-native `boundaryWkt` text column plus a `GENERATED ALWAYS AS (ST_GeomFromText("boundaryWkt", 4326)) STORED` geometry column with a GIST index, hand-authored in the migration; one structural change from the GpsPing Point hybrid — a polygon derives from text, not scalar floats), a nullable `customerId` FK (onDelete: Restrict) for CUSTOMER_SITE ownership, Tier-3 marks, and a round-trip schema test pinning SRID 4326 + the bowtie hazard. **G2 (this PR) ships the CRUD API + RBAC layer** — `apps/api/src/modules/geofences/` (service / controller / Zod schemas mirroring `customers/`, registered in `app.module.ts`), with: the geofence boundary accepted in the same `lon,lat;…` vertex representation the T5 query parses, via a **shared** WKT builder extracted to `apps/api/src/common/wkt.ts` that BOTH telematics and geofences import (so a stored fence and a query-param fence are byte-identical WKT — ADR-0030 c1's representation-coherence guarantee made structural); a service-level `ST_IsValid` pre-write gate (parameterized `$queryRaw`) that rejects a self-intersecting bowtie ring as 400 (ADR-0030 c2, the gate the generated column cannot provide); a type/ownership `.superRefine` re-run against the merged shape on PATCH (CUSTOMER_SITE ⇔ `customerId`, ADR-0030 c4); the `geofences:read` (ADMIN + OFFICE_STAFF) / `geofences:write` (ADMIN-only) capability split added to `permissions.ts` and enforced on the composed AuthGuard + RolesGuard chain (ADR-0030 c5); and the stale-`customerId` `P2003 → 400` mapping, with the customer-delete-blocker flowing through Customer's EXISTING `P2003 → 409` arm (no CustomersService change). 83 new tests (757 API total) cover the CRUD, the RBAC split (403/200/401), the validity gate, the merged-shape ownership refine, the customer-delete-blocker 409, and WKT coherence. **G3 (this PR) ships the web admin CRUD scaffold**: `apps/web/src/app/geofences/` (list / detail / new / edit / delete + `actions.ts` + a ninth nav button on the gated home) mirroring `apps/web/src/app/customers/`, consuming the G2 API as-is (no API / schema / `permissions.ts` change and no T5 wiring). The create / edit forms use a **coordinate-entry** input — the `lon,lat;…` vertex string the G2 `boundary` field accepts (the manual / fallback path, and the headless-testable path, that G4's Leaflet editor will serialize a drawn ring into) — with a customer picker shown **only for CUSTOMER_SITE** (fetched server-side, alphabetical, `take=200`) that clears when the type changes away; the edit form pre-fills the boundary by decoding the stored `boundaryWkt` back to the vertex string and PATCHes only the diff (the `in`-operator discipline maps a cleared `customerId` to wire `null`). A new `apps/web/src/lib/geofences-schema.ts` mirrors the API's Zod shapes and the shared WKT bounds (`validateVertexInput`, mirroring `apps/api/src/common/wkt.ts`) plus the WKT→vertex pre-fill helper (`wktToVertexInput`), pinned by a 17-case unit test (`apps/web/test/geofences-schema.test.ts`; 26 web tests total). The list resolves owning-customer names via a single customers fetch and maps by id; field-level error mapping routes the `ST_IsValid` 400 to the boundary input and the type/ownership refine + stale-`customerId` `P2003 → 400` to the customer picker. NO mapping dependency is added (that is G4). **G4 (this PR) ships the map-based polygon-drawing editor + the sanctioned Leaflet dependency (ADR-0030 c8):** `leaflet@^1.9.4` / `react-leaflet@^5.0.0` (the v5 line targets the web app's React 19 — v4 targets React 18) / `@geoman-io/leaflet-geoman-free@^2.19.3` join `apps/web/package.json` (plus `@types/leaflet` as a dev dep, lockfile committed for CI's `--frozen-lockfile`), isolated to this ticket for clean review (as `bullmq` was in GPS T1). A new client-only component `apps/web/src/app/geofences/geofence-map-editor.tsx` — imported into both the create and edit forms via `next/dynamic` with `{ ssr: false }` because Leaflet references `window` at module load (the canonical App Router gotcha; the boundary is verified by a passing `next build` that code-splits the Leaflet chunk out of the initial bundle) — renders an OpenStreetMap raster basemap (no API key, Kathmandu default view) with a Geoman single-polygon draw / edit / remove toolbar (a new polygon replaces the prior one); the edit form pre-renders the stored boundary as the editable layer and fits bounds to it. The drawn ring serializes to the SAME `lon,lat;…` vertex representation the G3 coordinate-entry field uses and `validateVertexInput` accepts, via a pure, headless-testable serializer at `apps/web/src/lib/geofence-latlng.ts` (`ringToVertexInput` / `vertexInputToLatLngs`, minding Leaflet's `LatLng` lat,lng order vs. the representation's lon,lat — the same X,Y foot-gun the rest of the system guards, pinned by 10 new tests; 36 web tests total), so the API / schema / migration / `permissions.ts` / T5 telematics module are all untouched — only the input surface grows. The coordinate-entry textarea stays as the manual / fallback / headless-testable path, kept in sync with the map through the form's single `boundary` value (the map writes it via the RHF field's `onChange`; an external field edit re-renders the map, guarded against an echo loop). **G5 (this PR) wires a stored fence into the T5 geofence-status query — the program's LAST ticket and the "one-line change" ADR-0029 T5 anticipated**, shipping NO schema/migration change and NO new capability (it consumes G1–G4 and the T5 query as-is): `GeofenceStatusQuerySchema` gains a third `geofenceId` branch, so the status query now accepts a three-way **circle XOR polygon XOR `geofenceId`** choice (validated at the schema layer — supplying two-or-more, or none, is a 400, mirroring the prior circle-XOR-polygon refine, with `geofenceId` reusing the existing `Cuid` helper); when `geofenceId` is supplied the `TelematicsService` loads the stored `Geofence` via the **injected `GeofencesService` public interface** (`TelematicsModule` imports `GeofencesModule` — no cycle, the geofence aggregate reached through its service interface, not its table; the module's export comment had named G5 as the consumer) and **404s a missing id**, then runs the **EXISTING** `ST_Contains(ST_GeomFromText(<boundaryWkt>, 4326), "geometry")` query against the vehicle's latest fix — the stored row's `boundaryWkt` is byte-identical to a query-param polygon's WKT (the shared `apps/api/src/common/wkt.ts` builder, ADR-0030 c1's coherence guarantee), so the spatial query body is unchanged, only the WKT's SOURCE differs. The gate **stays `gps:read-derived`** (ADMIN + OFFICE_STAFF): this reads geofence STATUS, a Tier-3 derived boolean, NOT the geofence CONFIG, so it is emphatically not `geofences:*`. The response **echoes the resolved fence's id + type** (both Tier-3) so a `geofenceId` caller sees which fence was evaluated — no coordinates (Tier-5 egress discipline holds; the derived status stays a boolean + timestamp + the fence's own id/type). 15 new tests (772 API total) prove the stored inside/outside classification (echoing id + type), the missing-id 404, the three-way XOR refine, the RBAC gate on the `geofenceId` mode (the route-level gate is mode-agnostic, so ADMIN/OFFICE_STAFF reach a 404 fence-not-found that itself proves the gate was passed, DRIVER → 403, anonymous → 401), an HTTP integration echo over the wire, and — the load-bearing one — that **a stored fence and an equivalent query-param polygon classify a fix IDENTICALLY** (the representation-coherence guarantee made observable). **With G5 the five-ticket geofence storage program (ADR-0030, G1–G5) is COMPLETE** — the depot / customer-site / route-corridor geofencing the roadmap named now reads against stored fences end-to-end, from the `Geofence` aggregate (G1) through its CRUD API + RBAC (G2), web admin CRUD scaffold (G3), and Leaflet map-drawing editor (G4) to the wired status query (G5). See ADR-0030, ADR-0029, and the glossary's Geofence / PostGIS / Geospatial entries.

**Bikram Sambat date rendering + compliance badges (ADR-0031) — the deferred Phase-1 daily-use polish, N1 of N3.** ADR-0031 is accepted (centralize BS date display everywhere dates are shown, plus vehicle-compliance expiring-soon/expired badges; **display + badges only** — the BS date-picker INPUT widget is a deferred follow-up and compliance reminder DELIVERY [email/SMS, the "reminder delivery" SLI] is Phase 3, both explicitly out of scope). This is deferred Phase-1 (iter 14) polish landing in the Phase-2 window, not new Phase-2 scope. N1 (this PR) builds the shared formatter + component and adopts it on the highest-value surface. It adds the PO-ratified `nepali-date-converter@3.4.0` dependency (isolated to this ticket as bullmq/Leaflet were, pinned **exact** because the app now leans on the library's calendar table, verified at install: Node/SSR-safe with zero `window`/DOM refs across seven timezones, ships types, round-trips AD↔BS, covers near-future expiries — MIT, zero runtime deps); `apps/web/src/lib/nepali-date.ts` — the pure `formatNepaliDate(iso, opts?)` plus the FleetCo-owned `BS_MONTHS` array, mirroring `money.ts`/`formatNpr` (em-dash for null/unparseable, the DESIGN.md `both`/`bs`/`en` shapes such as `2083 Jestha 6 (2026-05-20)`, a Gregorian degrade past the library's BS 2000–2090 table); and the thin `<NepaliDate iso=… format=… />` **server** component (`apps/web/src/components/nepali-date.tsx`) that keeps the calendar table server-side (the page's First Load JS is unchanged). Three correctness decisions, each verified against the library and pinned by a test: (1) the **UTC-calendar-day rule** (commitment 3) — this library reads a JS Date via server-LOCAL getters, so the formatter builds `new Date(y, m, d)` from the ISO's UTC components, NOT `new Date(Date.UTC(...))` (the ADR's `e.g.` illustration), which renders one BS day early in zones west of UTC; the TZ-independent test pins it across seven zones and fails with the `Date.UTC` form; (2) the library converts 2026-05-20 to **2083** Jestha 6, not DESIGN.md's illustration of **2082** (the doc's BS year is off by one) — per ADR-0031 the library is trusted for VALUES and DESIGN.md for SHAPE (anti-pattern #12: never fabricate a BS pair); (3) month names render from the FleetCo-owned `BS_MONTHS` (the library differs at Baisakh→Baishakh, Asar→Ashadh, Aswin→Ashwin), the anti-drift guard. The Vehicle detail page drops its inline `formatDate` and renders its five date-only fields (acquired / retired + the three compliance expiries) via `<NepaliDate>`; the `… UTC` audit timestamps (`formatTimestamp`), the Recent-trips date-times (`formatDateTime`), and all date inputs stay native AD per commitment 4. 19 new pure tests (55 web tests total); the glossary gains a **NepaliDate** entry + a **BS** pointer to ADR-0031 (commitment 7). **N2** rolls `<NepaliDate>` across the remaining aggregates' list/detail pages (mechanical; leaves `formatDateTime`/`formatTimestamp` and the `edit-*-form.tsx` date inputs untouched); **N3** adds the `Badge` component + a pure `complianceBadgeState(expiryIso, now, 30)` helper + the amber "Expiring soon" / red "Expired" badges on the Vehicle detail Compliance section + the DESIGN.md Badge provenance line. See ADR-0031 and the glossary's **NepaliDate** / **BS** entries.

**Bikram Sambat date rendering — N2 (mechanical rollout), N2 of N3.** N2 rolls the N1 `<NepaliDate>` component + `formatNepaliDate` formatter across every remaining aggregate's list + detail **date-only display field**, so the whole app now speaks the operator's calendar through one centralized formatter and the ~13 inline `formatDate`/`formatAcquiredDate` copies are gone (the centralization the ADR's "What this makes easier" promised). Converted, by variant (ADR-0031 commitment 2's `bs`/`both` shapes): the **list** date cells on Vehicles (acquired), Drivers (hired, license-expires), Customers (created — rendered date-only, not a `… UTC` instant), Jobs (scheduled-start), Fuel-logs (date), Expense-logs (date), and Geofences (created) use `format="bs"` (BS prominent, the Gregorian date carried in a `title` tooltip — dense cells where horizontal space is tight); the **detail** date-only rows on Drivers (DOB, hired, license-expires, terminated), Jobs (scheduled/actual start+end), Trips (the "Date" row), Fuel-logs (Date), and Expense-logs (Date) use the default `both` (BS + muted Gregorian parenthetical — roomy rows); the Fuel-log/Expense-log breadcrumb + h1 identity uses `bs`; and the two delete-dialog `dateLabel` props (a string context on a client island) plus the Per-vehicle-cost report's window subtitle (`report.from`/`report.to`) use the pure `formatNepaliDate(iso, { format: "bs" })`. **Left AD per commitment 4 (verified by grep before each commit):** every `formatDateTime` (the Trips list + the Recent-trips and Timing date-times) and `formatTimestamp` (the `… UTC` audit instants in every Audit section), plus every `edit-*-form.tsx` native `<input type="date">` default and the report's `defaultDateRange` input/query helper — date-time and date inputs stay native AD until the deferred date-picker slice. **Three pages carry no date-only display field and are untouched:** the Trips **list** (its only date column renders started/ended via `formatDateTime`), the Customer detail, and the Geofence detail (both show only audit timestamps). `nepali-date.ts`/`nepali-date.tsx` are unchanged — N2 ships no new pure logic, no `Badge`/`complianceBadgeState` (that is N3), and no API/schema/migration change. Gates green on every checkpoint: `pnpm lint` + `format:check` + typecheck + the 55 existing web tests + the SSR `pnpm --filter @fleetco/web build` (every edited page is a server component, so the build exercises the converter at SSR/build time). **N3** (the program's last ticket) adds `apps/web/src/components/ui/badge.tsx` (a CVA `<span>` bound to the existing `color.status.*` tokens — no new design token, drift test untouched) + the pure `complianceBadgeState(expiryIso, now, 30)` helper + the amber "Expiring soon" / red "Expired" badges on the Vehicle detail Compliance section + the DESIGN.md Badge provenance line; compliance reminder *delivery* (email/SMS, the "reminder delivery" SLI) stays Phase 3 and out of scope.

**Bikram Sambat date rendering + compliance badges (ADR-0031) — N3 (Badge + compliance badges), COMPLETE; the program's final ticket.** N3 ships the passive visual compliance indicator and the `Badge` primitive, exhausting the ADR-0031 program scope. Three additions, with no API/schema/migration change and no new design token: (1) `apps/web/src/components/ui/badge.tsx` — a CVA `<span>` (status, not action — DESIGN.md anti-pattern #2) mirroring `button.tsx`'s `cva`/`cn` shape, `radius.sm` + `text.xs`, with five variants (`warning` amber / `error` red / `success` emerald / `info` blue / `neutral` zinc) each bound to a `color.status.*` / surface / text token ALREADY in DESIGN.md → `globals.css` `@theme`, so the design-token-drift test (`apps/web/test/design-token-drift.test.ts`) stays unedited and green (verified by diff) and `globals.css` `@theme` is untouched (no new token); N3 renders only `warning` + `error`, the other three round out DESIGN.md's status set for future adopters. (2) The pure `complianceBadgeState(expiryIso, now, windowDays = 30)` helper at `apps/web/src/lib/compliance.ts` (a sibling of `nepali-date.ts`, so the BS formatter stays purely about rendering): `null` / `undefined` / unparseable → `none` (date alone, no badge); UTC-day(expiry) < UTC-day(now) → `expired` (red "Expired"); within the window — today (day 0) through day 30 inclusive → `expiring-soon` (amber "Expiring soon"); else → `ok` (no badge). The comparison truncates BOTH operands to their UTC calendar day via getUTC* accessors — the SAME UTC-calendar-day discipline `formatNepaliDate` uses — so the 30-day boundary is deterministic regardless of server timezone (ADR-0031 commitment 5, the load-bearing correctness rule). (3) A local `ComplianceExpiry` helper in `apps/web/src/app/vehicles/[id]/page.tsx` wires the badge beside each of the three compliance expiry rows (`bluebookExpiresAt`, `insuranceExpiresAt`, `routePermitExpiresAt`); the existing `<NepaliDate>` render is preserved (the badge is additive), and the Acquired/Retired rows stay plain dates (not compliance documents). 14 new pure tests (`apps/web/test/compliance.test.ts`; 69 web tests total) pin the four states, the exact 30-day boundary (day 0 + day 30 = expiring-soon, day 31 = ok, yesterday = expired), day-vs-instant granularity, a custom `windowDays`, and TZ-independence across five server zones (mirroring `nepali-date.test.ts`); the `Badge` component ships untested-by-render like the other UI primitives (no jsdom harness). DESIGN.md gains a one-line Badge provenance entry (the only DESIGN.md touch — the `<Badge>` contract is followed verbatim per ADR-0031 commitment 7); the glossary gains **Badge** + **Compliance badge** entries. Gates green on every checkpoint (`pnpm lint` + `format:check` + `typecheck` + the 69 web tests + the SSR `pnpm --filter @fleetco/web build`). **Out of scope (hard boundaries), unchanged:** the vehicles-LIST compliance roll-up column (ADR-0031 §E "not in v1"), the BS date-picker INPUT widget (deferred follow-up), and ALL compliance reminder DELIVERY — email/SMS/cron/queue and the "reminder delivery" SLI are Phase 3 (`roadmap.md` §"Phase 3"). **With N3 the three-ticket ADR-0031 program (N1–N3) is COMPLETE** — every shown date renders in Bikram Sambat through one centralized formatter, and compliance lapses surface at a glance on the Vehicle detail page. See ADR-0031 and the glossary's **NepaliDate** / **BS** / **Badge** / **Compliance badge** entries.

**Home daily-ops dashboard — deferred Phase-1 daily-use polish, COMPLETE (D1–D2).** Like the ADR-0031 BS-date work, this is deferred Phase-1 (daily-use) polish landing in the Phase-2 window, not new Phase-2 scope: it replaces the placeholder navigation-only home (`apps/web/src/app/page.tsx`, formerly nine nav buttons) with the daily-ops overview specified in DESIGN.md §"Surfaces" → "Home dashboard", composed entirely from EXISTING read endpoints — zero new backend, zero new design token, zero new dependency. **D1 (merged, PR #97)** built the data layer: the server-only `loadDashboard(): Promise<DashboardData>` at `apps/web/src/lib/dashboard.ts` fires SIX existing-endpoint reads in parallel (vehicles `take=200`, trips `IN_PROGRESS take=5`, the per-vehicle-cost report for the current month, recent fuel / expense logs `take=5` each, drivers `take=1` for the count) and rolls the up-to-200 vehicle rows into the headline compliance counts SERVER-SIDE (so the rows never cross to the browser), plus the pure helpers `vehicleComplianceState` (worst-of-three documents, built on the shipped `complianceBadgeState`) / `rollUpCompliance` (each vehicle counted once by its worst state) / `currentMonthRange` (first-of-month → today, UTC-deterministic), pinned by 15 tests (`apps/web/test/dashboard.test.ts`); the canonical per-vehicle-cost wire type was extracted to `apps/web/src/app/reports/per-vehicle-cost/types.ts` for reuse. **D2 (this PR, the program's last ticket)** built the page + loading / error states against that `DashboardData` (no second data path): `page.tsx` keeps the `getServerSession()` → `redirect("/login")` gate and wraps `loadDashboard()` in the list-page `try/catch` that maps `ApiError` 401 → `redirect` and rethrows otherwise; the page header carries today's date via `<NepaliDate format="both">` + "Signed in as `<email>`" with a right-aligned sign-out. **Zone A** is six section-card summaries in a `grid gap-4 sm:grid-cols-2 lg:grid-cols-3`, co-located as server components under `apps/web/src/app/_dashboard/` over a shared `DashboardCard` shell that enforces anti-pattern #3 (one contextual text link per card, never a competing inner primary button): the full-width Fleet-compliance headline (`lg:col-span-3`; attention count + `error` / `warning` chips shown only when non-zero, all-clear → `success` "All documents current", empty fleet → "No vehicles registered." → `/vehicles/new`), Active trips (count + ≤5 `registration · driver` with the in-progress badge), This-month cost (`formatNpr` total + fuel / expense split + month range), Recent fuel & Recent expenses (last five each; the expense card guards the NULLABLE `vehicle` with a "Company" fallback, never touching `.registrationNumber` when null), and Fleet counts (three tabular stats). **Zone B** is the quick-links strip preserving all nine CRUD destinations (the dashboard augments navigation until the sidebar is built, never replaces it). The first app-root `loading.tsx` (an `animate-pulse` skeleton mirroring the list pages, shaped header + card-grid + link-strip) and `error.tsx` (a `"use client"` `{ error, reset }` boundary with the §Voice "Cannot reach the server." line + a Retry calling `reset()`) round out the states. Every empty / zero state follows the §Voice register (state the fact; no exclamation, no apology); the page reuses the shipped `<Badge>` / `<NepaliDate>` / `formatNpr` as-is, so the design-token-drift test stays green and `globals.css` `@theme` is untouched. Gates green on every checkpoint (`pnpm lint` + `format:check` + `typecheck` + the 84 web tests + the SSR `pnpm --filter @fleetco/web build`, which exercises `loadDashboard` and the new boundaries at build time). **Out of scope (unchanged):** an exact compliance count for a >200-vehicle fleet (a future dedicated-count endpoint, flagged in DESIGN.md §"Home dashboard" — the roll-up is bounded by the vehicles-list page size of 200), and the Navigation sidebar (the dashboard's Zone B carries primary nav until it lands). **With D2 the Home-dashboard program (D1–D2) is COMPLETE.** See DESIGN.md §"Surfaces" → "Home dashboard".

**Polish & debt sweep — four small Phase-1 polish / tech-debt tickets (P1–P4), P1 of P4.** A loop program of four independent, one-PR-each tickets, each against an EXISTING ADR or `docs/tech-debt.md` entry (no new ADR). Like the ADR-0031 BS-date and Home-dashboard work, these are deferred Phase-1 daily-use polish landing in the Phase-2 window, not new Phase-2 scope. **P1 (this PR) ships the vehicles-list compliance roll-up column** — the worst-of-three indicator the N3 entry above listed as out of scope ("not in v1"), now sanctioned by ADR-0031 §E / "Revisit when" ("A vehicles-list compliance roll-up is wanted — add the worst-of-three indicator column to `vehicles/page.tsx`, reusing `complianceBadgeState`"). A new pure `worstComplianceState(expiries, now, windowDays = 30)` helper in `apps/web/src/lib/compliance.ts` — the array-shaped sibling of `complianceBadgeState` — classifies each expiry with that shipped helper and returns the most urgent by the precedence `expired` > `expiring-soon` > `ok` > `none` (empty / all-null → `none`); the 30-day window and the UTC-calendar-day rule live in `complianceBadgeState` and are never re-derived (`windowDays` forwards verbatim per expiry). A read-only "Compliance" column between Status and Odometer on `vehicles/page.tsx` calls it over each vehicle's `[bluebookExpiresAt, insuranceExpiresAt, routePermitExpiresAt]` and paints the same red "Expired" / amber "Expiring soon" `<Badge>` the Vehicle detail Compliance section uses (a quiet em-dash when every document is current or unscanned); the column is NOT a `SortableHeader` and is NOT on the `sortBy` whitelist — worst-of-three is a derived value with no server-side sort column. The Home dashboard's `vehicleComplianceState` (`lib/dashboard.ts`) was refactored to delegate to the new helper — the duplicate `COMPLIANCE_RANK` precedence map is gone, so the list column and the dashboard roll-up now share one precedence definition that cannot drift (behavior-preserving; `test/dashboard.test.ts`'s 15 tests pass unchanged). **No API / schema / dependency / design-token change** (the list endpoint already returns the three `*ExpiresAt` fields and `<Badge>` already ships). 12 new pure tests (`test/compliance.test.ts`, 26 total; 96 web tests). The glossary's **Compliance badge** entry gains the list-roll-up surface + the `worstComplianceState` helper. Gates green on every checkpoint (`pnpm lint` + `format:check` + `typecheck` + the web tests + the SSR `pnpm --filter @fleetco/web build`). **P2 (merged) adopted `lucide-react`** (`docs/tech-debt.md` Paid-off). **P3 (this PR) discharges the two recorded cleanups** — the per-vehicle-cost report page converges onto the canonical `reports/per-vehicle-cost/types.ts` (a type-only, null-diff import swap) and the fuel-log odometer-monotonicity debt is resolved as a documented decision (odometer readings are **non-monotonic by design**; NO service-layer guard — the create/update logic and the API suite are unchanged), both moved to `docs/tech-debt.md` Paid-off with the rationale recorded in the glossary's Fuel log / Odometer entries. **P4 (this PR, the program's last ticket) closes the GPS Tier-5 span-scrub seam** — the owed ADR-0026 c5 / ADR-0027 c5 "raw GPS coordinates are NEVER exported in a span" guarantee, the OTLP-egress twin of the pino `redact` GPS denylist already live in `app.module.ts` (the `redact` list scrubs coordinates from LOGS; this seam scrubs the same keys from SPANS). A new pure module `apps/api/src/observability/span-scrub.ts` exports `GPS_SPAN_SCRUB_DENYLIST` (the 13 bare keys `latitude, longitude, lat, lng, lon, altitude, heading, speed, coordinates, geometry, location, point, position` — the same set as the `redact` GPS paths minus the `*.` wildcard prefix, cross-referenced as the source to keep in sync) and a `GpsSpanScrubProcessor` implementing the OpenTelemetry `SpanProcessor` interface whose `onEnd` deletes every denylist key from the span's attributes (`onStart` / `forceFlush` / `shutdown` are no-ops; the module is pure — no env reads, no I/O — mirroring `otel.ts`). `buildOtlpSpanProcessors` in `otel.ts` now returns the scrub processor at **index 0** ahead of the `BatchSpanProcessor`, so — OTel running span processors in registration order — the scrub's `onEnd` mutates each span's attributes before the exporter reads them for OTLP egress (the `endpoint`-undefined branch still returns `[]`: no exporter, nothing to scrub). `apps/api/test/span-scrub.test.ts` mirrors `otel.test.ts` (pure unit tests, no live OTLP collector): real `ReadableSpan`s obtained via an `InMemorySpanExporter` round-trip (cast-free, the otel.test.ts "properly typed fake" discipline) prove denylist-key deletion + safe-key survival, end-to-end scrub-before-export when the processor leads the array, and the `buildOtlpSpanProcessors` ordering; `otel.test.ts`'s existing single-processor assertion was updated to the now-2-element list. 7 new tests (779 API total). The memory artifacts close the seam: the `app.module.ts` KEEP-IN-SYNC comment now states the seam EXISTS (cross-referencing `span-scrub.ts`), and the glossary's **GPS ping** / **Retention** entries' "never exported in a span" phrasing is now present tense (the seam exists today). **No new dependency, no schema / migration / API change, no behavior change to any request path** (the OTLP path is only active when `OTEL_EXPORTER_OTLP_ENDPOINT` is set; pre-sanctioned by ADR-0026 c5 / ADR-0027 c5, no new ADR; no new tech-debt entry created or owed). **With P4 the four-ticket polish & debt sweep program (P1–P4) is COMPLETE** — the vehicles-list compliance roll-up column (P1), `lucide-react` adoption (P2), the two recorded cleanups (P3: per-vehicle-cost report-type convergence + the fuel-log odometer-monotonicity recorded decision), and the GPS Tier-5 span-scrub seam (P4) all ship; the program scope is exhausted. See ADR-0026, ADR-0027, and the glossary's **GPS ping** / **Retention** entries.

**Bikram Sambat date-PICKER input widget (ADR-0032) — the input twin of the shipped `<NepaliDate>` display, B1 of B3.** ADR-0032 is accepted (hand-build a `<NepaliDatePicker>` — a Radix Popover + a BS month-grid over the already-installed `nepali-date-converter` — rather than adopt a third-party React BS datepicker; **NO new top-level dependency**; the widget emits/accepts the SAME ISO date string the native `<input type="date">` does, so form Zod schemas / `actions.ts` / API contracts are UNCHANGED). Like the ADR-0031 display work this is deferred Phase-1 daily-use polish landing in the Phase-2 window, not new Phase-2 scope; it realizes ADR-0031's deferred "BS date-picker input widget" + "datepicker library" follow-ups and reuses ADR-0031's `nepali-date-converter`, the `BS_MONTHS` anti-drift array, the `<NepaliDate>` display component (for the trigger label), and the UTC-calendar-day rule. **B1 (this PR)** builds the widget and proves the react-hook-form integration end-to-end on the highest-value surface. Four pieces, with NO new dependency, NO new design token, and NO schema/migration/API change: (1) `apps/web/src/components/ui/popover.tsx` — the shadcn Popover primitive over the installed `radix-ui` umbrella (mirrors `select.tsx`'s import + `cn()` + `data-slot` + provenance-comment convention; documented local edit — the shadcn `:root` color aliases like `bg-popover` are not Tailwind utilities in this project, only the `@theme` `--color-*` tokens are, so the content uses `bg-surface-elevated` / `text-text-primary` / `border-border-subtle`, no new token, drift test untouched), with a one-line DESIGN.md component-provenance entry. (2) The pure grid/conversion helpers `apps/web/src/lib/nepali-date-picker.ts` — `bsDayToIso` (the load-bearing BS→ISO selection mapping: reads AD calendar integers off the converter's `getAD()`, so the emitted string round-trips to exactly what the native input submits, TZ-independently), `buildMonthGrid` (each cell `{ bsDay, adIso, isToday, isSelected }`), `bsMonthLength` (day-overflow probe, robust at both table edges), `stepBsMonth`/`stepBsYear` (year-wrapping, clamped to the converter's BS 2000–2090 table), `initialBsMonth`, `isoToUtcDay`/`isoToBsMonth` — all pure, the UTC-calendar-day rule reused via `formatNepaliDate`'s `en` shape (one definition), month names from `BS_MONTHS`. (3) `apps/web/src/components/nepali-date-picker.tsx` — the `"use client"` `<NepaliDatePicker value={iso|null} onChange={(iso|null)} id?=… disabled?=… />` (ADR-0032 commitment 3): a Radix Popover whose trigger shows the BS + Gregorian date via `<NepaliDate format="both">` (or a "Select date" placeholder), and whose panel renders the BS month grid (Gregorian day in muted corner text, prev/next month + year arrows, today outlined in `color.accent.primary`, selected day filled, a Clear affordance for optional fields), `forwardRef` so it composes with `FormControl` (id / aria-invalid / label association); styled only with existing `@theme` tokens + lucide icons (§Iconography stroke widths). (4) Adoption on the **Vehicle compliance date inputs** — the four `<input type="date">` controls on `apps/web/src/app/vehicles/new/create-vehicle-form.tsx` + `apps/web/src/app/vehicles/[id]/edit/edit-vehicle-form.tsx` (`acquiredAt`, `bluebookExpiresAt`, `insuranceExpiresAt`, `routePermitExpiresAt`) now render `<NepaliDatePicker>` wired through the SAME react-hook-form field (`value={field.value || null}`, `onChange={(iso) => field.onChange(iso ?? "")}`), so the field value stays the ISO string and the `DateString`/`OptionalDateString` Zod schema, `actions.ts`, and the diff-on-submit logic are untouched; non-date inputs are unchanged. The forms' First Load JS grows ~28kB (the converter table now ships client-side for the interactive picker — inherent and accepted by ADR-0032; the display side stays a server component). 30 new pure tests (`apps/web/test/nepali-date-picker.test.ts`, 126 web tests total) pin the cell→AD mapping, today/selected location, the TZ-independent round-trip across five server zones, month-step year-wrapping + clamping, and the `BS_MONTHS` spellings — every AD value locked against the library, never fabricated; no component render test (no jsdom, same posture as the display side). DESIGN.md gains the `popover.tsx` provenance line (the only DESIGN.md touch — the widget spec is followed verbatim); the glossary gains a **NepaliDatePicker** entry + an ADR-0032 pointer on **BS** / **NepaliDate**. Gates green on every checkpoint (`pnpm lint` + `format:check` + `typecheck` + the web tests + the SSR `pnpm --filter @fleetco/web build`). **B2** rolls `<NepaliDatePicker>` across the remaining create/edit form date inputs (drivers, fuel-logs, expense-logs, jobs — mechanical, value contract unchanged); **B3** (the program's last ticket) does the fuel/expense filter toolbars + the per-vehicle-cost report date-range. See ADR-0032 and the glossary's **NepaliDatePicker** / **NepaliDate** / **BS** entries.

**Bikram Sambat date-PICKER input widget (ADR-0032) — B2 (mechanical rollout across the remaining create/edit forms), B2 of B3.** B2 rolls the B1 `<NepaliDatePicker>` across the **18** native `<input type="date">` controls on the eight create/edit forms B1 left for this ticket — Drivers (`dateOfBirth` [optional] / `hiredAt` / `licenseExpiresAt`, on both create + edit), Fuel logs (`date`), Expense logs (`date`), and Jobs (`scheduledStartDate` / `scheduledEndDate` / `actualStartDate` / `actualEndDate`) — each wired through its EXISTING react-hook-form field exactly as B1's Vehicle adoption (`value={field.value || null}`, `onChange={(iso) => field.onChange(iso ?? "")}`). The field value stays the ISO `YYYY-MM-DD` string, so every form's Zod schema, `actions.ts`, and submit/diff-against-initial-values logic are UNTOUCHED; because the emitted string is byte-identical to the native input's, the Jobs forms' **end ≥ start cross-field refine** (validated on the YYYY-MM-DD string, re-run server-side against the merged shape on PATCH) keeps working unchanged, and the optional/nullable fields (Drivers' `dateOfBirth`, all four Job dates) reproduce the native empty-date state exactly via the `"" → null → ""` mapping. Two non-blind-replace details the rollout handled: (1) the Fuel-log / Expense-log / Job date inputs carried a `className="font-mono"` (it aligned the native `YYYY-MM-DD` digits) which is DROPPED — the picker trigger renders BS + Gregorian prose via `<NepaliDate format="both">` with its own chrome (and `tabular-nums` inside the grid), matching B1's Vehicle adoption which carries no className; (2) in `create-job-form.tsx` the four date fields were the form's ONLY `<Input>` uses, so the now-unused `Input` import is replaced by the `NepaliDatePicker` import (keeping `pnpm lint` / `typecheck` green), while every other form keeps `Input` for its text / number / read-only fields (e.g. the edit-job read-only Job number / Customer identity rows render `<Input disabled>`); the create-job header comment that described the fields as native OS date pickers is corrected to the widget (stale-comment hygiene per CLAUDE.md). **No new dependency, no new component or pure logic (B1's TZ-independent helper tests already pin the value contract, so B2 adds no unit test — the suite stays at 126 web tests), no new design token, and no schema/migration/`actions.ts`/API change.** Gates green on every checkpoint (`pnpm lint` + `format:check` + `typecheck` + the 126 web tests + the SSR `pnpm --filter @fleetco/web build`), pushed as four independent per-aggregate checkpoints (drivers → fuel-logs → expense-logs → jobs) so a halt costs at most one to recover. **B3** (the program's LAST ticket) does the fuel/expense filter toolbars (`fuel-logs-filters.tsx`, `expense-logs-filters.tsx`) + the per-vehicle-cost report date-range (`per-vehicle-cost-filters.tsx`) — each toolbar keeps its TWO separate `<NepaliDatePicker>` inputs (a single BS range-picker is out of scope per ADR-0032 c6); those are filter toolbars (not react-hook-form forms), so the picker wires to the existing state/`onChange` the native input used, leaving the query/URL-param logic untouched. See ADR-0032 and the glossary's **NepaliDatePicker** / **NepaliDate** / **BS** entries.

**Bikram Sambat date-PICKER input widget (ADR-0032) — B3 (filter toolbars + report date-range), COMPLETE; the program's final ticket.** B3 rolls the B1 `<NepaliDatePicker>` across the **6** native `<input type="date">` controls (from/to × three) on the three filter toolbars B1/B2 left for this ticket — the Fuel-logs filter (`apps/web/src/app/fuel-logs/fuel-logs-filters.tsx`), the Expense-logs filter (`apps/web/src/app/expense-logs/expense-logs-filters.tsx`), and the Per-vehicle-cost report date-range (`apps/web/src/app/reports/per-vehicle-cost/per-vehicle-cost-filters.tsx`) — the last native date-input sites in the app. Unlike B1/B2 these are **filter toolbars, not react-hook-form forms**: there is no `FormControl`/RHF `field`; each native `<Input type="date">` was an UNCONTROLLED input (`defaultValue` + `onBlur`/`onChange` reading `e.target.value`) that pushed the bound from/to value to URL `searchParams` via the toolbar's `setParam` inside a `useTransition`. The picker is controlled, so each becomes `<NepaliDatePicker value={existing || null} onChange={(iso) => setParam(key, iso ?? "")} />` — the picker hands the ISO `YYYY-MM-DD` string directly (no `e.target`), and because that string is byte-identical to what the native input produced, **`setParam` / the URL-searchParams logic / the skip-reset / the from/to window / the bookmarkable-filter contract are all untouched** — only the input widget changes; the verbose native-input onBlur/onChange commit dance is gone (the picker fires `onChange` once per selection / clear). Each toolbar keeps its **two separate date inputs** (a single BS range-picker is out of scope per ADR-0032 c6); every non-date control is untouched (the Expense-logs `category` `<select>` and the report's `vehicle` `<select>` stay as raw selects, with their own state/handlers). Two non-blind-replace details: (1) in all three toolbars the two date inputs were the file's ONLY `<Input>` use (the category / vehicle controls are raw `<select>`s), so the now-unused `Input` import is replaced by the `NepaliDatePicker` import to keep `pnpm lint` / `typecheck` green — the B2 create-job situation, three more times; (2) the trigger is sized `className="w-56"` (twMerge overrides the trigger's default `w-full`) so the widget's intrinsic BS + Gregorian label (DESIGN.md §"BS calendar" — the trigger shows both dates via `<NepaliDate format="both">`, wider than a native date input's 10-char `YYYY-MM-DD`) is not truncated, matching the existing vehicle `<select>` width; the `id` passes through to the trigger button so each `<label htmlFor>` association is preserved. **No new dependency, no new component or pure logic (B1's TZ-independent helper tests already pin the value contract, so B3 adds no unit test — the suite stays at 126 web tests), no new design token, and no schema/migration/`actions.ts`/API change.** Gates green on every checkpoint (`pnpm lint` + `format:check` + `typecheck` + the 126 web tests + the SSR `pnpm --filter @fleetco/web build`), pushed as three independent per-toolbar checkpoints (fuel-logs → expense-logs → report) so a halt costs at most one to recover. **With B3 the three-ticket ADR-0032 program (B1–B3) is COMPLETE** — the BS calendar is now symmetric: operators read AND enter dates in Bikram Sambat across every former native-date-input surface (the create/edit forms via B1/B2, the filter toolbars + report date-range via B3), all through one reusable `<NepaliDatePicker>` whose unchanged value contract (ISO in, ISO out) leaves every form Zod schema / `actions.ts` / API contract untouched — the asymmetry ADR-0031 opened (read BS, enter Gregorian) is closed. See ADR-0032 and the glossary's **NepaliDatePicker** / **NepaliDate** / **BS** entries.

**Phase 2 — Driver-app foundation accepted (ADR-0033/0034/0035) + the D0 toolchain spike, D0 of D6.** The three driver-app foundation ADRs are accepted — **ADR-0033** (mobile platform), **ADR-0034** (mobile auth + the now-defined DRIVER role), **ADR-0035** (offline GPS producer) — the architecture for Phase 2's headline pillar, the driver app, whose backend (RBAC / GPS ingestion / geofencing per ADR-0028/0029/0030) shipped but has sat **inert for want of a producer**. **D0 (this slice) is the toolchain spike: FleetCo's first mobile surface** — a standalone **Expo SDK 56** (managed + CNG; React Native 0.85 / React 19.2) app at `apps-mobile/driver/`, Android-first, deliberately **OUTSIDE the pnpm workspace** (its own `package.json` + lockfile, installed `pnpm install --ignore-workspace`) per ADR-0033 c3 — with a no-login "hello" screen (`App.tsx` rendering a pure `driverGreeting()` from `src/greeting.ts`), one jest-expo test, eslint-config-expo lint, and a `tsc --noEmit` typecheck, all green. (The ADR illustrated "SDK 55"; ADR-0033 c9 defers exact versions to the slice and SDK 56 is the current stable, pinned here.) A new **`mobile` CI job** in `.github/workflows/ci.yml` (parallel to `ci`) installs the standalone app `--frozen-lockfile --ignore-workspace` and runs the network-free **typecheck + lint + jest-expo** trio (ADR-0033 c4; `expo-doctor` / `prebuild` / EAS stay OUT of the polled gate); the root `eslint .` + `prettier --check .` now **ignore `apps-mobile/`** (the app owns its own lint/format), and `.github/dependabot.yml` gains the standalone lockfile's npm ecosystem so it is not an update blind spot (ADR-0012). The **device-build (binary) path is deferred** (ADR-0033 c7): **Expo Go** is the D0–D3 on-device runtime (scan a QR — no build/signing), and a custom build becomes a prerequisite only at D4 (background location, SQLCipher). No auth, no GPS, no API calls yet — those are D1 onward: **D1** driver login (better-auth `bearer()` + the `User`↔`Driver` link + the DRIVER role) → **D2** trip start/stop + the own-record row-scope (atomic with the write-capability grant) → **D3** odometer/fuel entry → [binary-path gate] → **D4** foreground GPS → **D5** background GPS + the encrypted outbox → **D6** geofence context. Real field use also depends on the still-owed production deploy (the app is the API's consumer). See ADR-0033 / ADR-0034 / ADR-0035.

**Phase 2 — Driver login + the DRIVER role (ADR-0034), D1 of D6.** D1 is the first driver-app slice with real behavior: a driver authenticates against the API from the phone and sees their identity + role. Mostly additive; verified green (783 API tests + the mobile typecheck/lint/jest-expo trio). **Server:** the better-auth **`bearer()`** plugin in `auth.ts` — the signed session token rides the `set-auth-token` response header and the native client replays it as `Authorization: Bearer`; `AuthGuard` already resolves it via `getSession({ headers: fromNodeHeaders(...) })`, so **no guard change** and the web cookie flow is untouched (regression-pinned by `auth.bearer.test.ts` + the existing `auth.role.test.ts` cookie test). The **`User`↔`Driver` link** lands as a nullable-unique `Driver.userId` FK → User (migration `20260616120000_add_driver_user_link`, ADR-0034 c4) — the 1:1 login↔driver binding the D2 own-record scoping reads through. `create-user.ts` now **accepts DRIVER** (the `input:false` defense + the privileged-write path preserved). Per ADR-0034 c5's hard rule, **`permissions.ts` is unchanged — DRIVER's capability set stays empty**: a D1 DRIVER session authenticates and reads `/me` but is inert against every gated route (`roles.guard.test.ts` pins it); the write capabilities + their own-record row-scope land **atomically in D2**. **Mobile** (`apps-mobile/driver/`, runs in **Expo Go** — no custom build): the `@better-auth/expo` client (`src/auth.ts`) stores the bearer token in **expo-secure-store** and auto-injects `Authorization: Bearer`; `App.tsx` is a login form → "Signed in: `<email>` / `<role>`" via `authClient.useSession()` (the session round-trip on the stored bearer token is the D1 proof); the `fleetco://` scheme is declared on the Expo CLIENT, not server `trustedOrigins` (ADR-0034 c2); the API base URL is set via `EXPO_PUBLIC_API_URL` (the operator's LAN IP / tunnel for a real phone). Token lifecycle (ADR-0034 c3): D1 accepts better-auth's default revocable-session TTL and documents the `revokeSession` lever; a driver-specific TTL + an automated role-change-revocation hook are tunable follow-ups (no role-change flow exists yet). **Out of scope (next): D2** grants the DRIVER write capabilities + the own-vehicle/own-trip row-scope **atomically** (ADR-0034 c5), reusing `PATCH /trips/:id` + `POST /fuel-logs`; the custom device build is still deferred to the D4 boundary (ADR-0033 c7); real field use still needs the owed production deploy. See ADR-0034.

**Phase 2 — Driver trip start/stop + the DRIVER own-record row-scope (ADR-0034), D2 of D6.** D2 is the slice that **activates** the DRIVER role: a driver starts and stops their own trips from the phone. Verified green (**808 API tests** [+25] + the mobile typecheck/lint/jest-expo trio). **The grant, atomic with its scope (ADR-0034 c5):** `permissions.ts` now gives DRIVER exactly **`trips:*` + `fuel-logs:*`**, and the **service-layer own-record predicate** lands in the SAME change — the inseparable pairing c5 mandates. A new **`DriverScopeService`** (`apps/api/src/modules/auth/driver-scope.service.ts`, provided + exported by `AuthModule`) resolves an authenticated DRIVER to their own `Driver.id` through the D1 `Driver.userId` link and **fails closed (403)** when a DRIVER login has no linked Driver row; ADMIN/OFFICE_STAFF resolve to `null` (no restriction, zero DB cost). The trips and fuel-logs **controllers thread the acting `{ userId, role }`** (built via the shared `toUserRole` coercion — not a guard change, the same shape as the existing `createdById`-from-session threading) into their services, which apply the policy: a DRIVER **reads / starts / stops only their own** trips (list force-scoped to the own `driverId`; `findById`/`update` 404 a foreign trip BEFORE the `$transaction` + odometer-bump runs), logs fuel **only against their own trip** (a driver `POST /fuel-logs` must carry a `tripId` resolving to one of their trips — the c4 definition of "their vehicle"; reads scoped by `createdById`), and **cannot create or delete** either (403). **The load-bearing finding:** the Phase-1 trips/fuel-logs controllers are `AuthGuard`-only (NOT RolesGuard-gated — opt-in per ADR-0028), so adding the caps does **not** gate those routes; the service predicate is the **sole** enforcement (ADR-0034 c7), which also **closes the latent post-D1 gap** where an authenticated DRIVER could reach those ungated routes fleet-wide. **Deferred per c5:** `gps:ingest` (D4/D5, ADR-0035) and `gps:read-derived` (D6) are NOT granted yet — each lands with its own scope. **SLI (c9):** `SLI_TRIP_START_SUCCESS` (`"trip_start_success"`) is emitted from the trips controller on the `PATCH → IN_PROGRESS` path only (mirroring the trip-creation SLI; class-name-only `error_kind`, no id/message leak), instrumenting ADR-0026 c6's provisional 99.0% target. **Reuse, no new surface:** no mobile BFF, no new endpoint, **no schema/migration change** (the `prisma migrate diff` shows only the pre-existing PostGIS generated-column drift on geofence/gps_ping), no new dependency. The 25 new API tests cover the DRIVER own-record scope across trips + fuel-logs (own succeeds, foreign 404s, create/delete 403, unlinked 403), the trip-start SLI (success / failure+`error_kind` / no-leak / not-a-start), and a DRIVER capability-set pin; the harness migration threads an `actor` through every existing trips/fuel-logs service/controller call site (compile-enforced) + adds `DriverScopeService` to the affected test modules' providers. **Mobile** (`apps-mobile/driver/`, Expo Go): the signed-in branch of `App.tsx` now renders a **trip start/stop screen** — `src/api.ts` (an authenticated fetch wrapper attaching the `@better-auth/expo` session credential via `getCookie()` as a `Cookie` header — the credential mechanism THIS client version actually uses, resolved by the server's `getSession`; the D1 `auth.ts` "bearer" comment is imprecise but the `bearer()` server plugin is enabled and additive) wraps `listMyTrips()` (auto-scoped, no `driverId` filter needed) and `patchTrip()`; `src/trips.ts` holds the pure `tripStartPayload` / `tripStopPayload` builders (5 jest-expo tests). **Out of scope (next): D3** the mobile odometer/fuel **entry** UI (D2 shipped the fuel-logs server scope; the mobile fuel screen is D3) → the **EAS-vs-local-prebuild binary-path gate** (ADR-0033 c7) → **D4** foreground GPS (the first real producer for the ADR-0029 backend). Real field use still needs the owed production deploy (the app is the API's consumer). See ADR-0034.

**Phase 2 — Driver mobile fuel / odometer entry (ADR-0034), D3 of D6.** D3 lets a driver record a fuel fill + odometer reading from the phone — the consumer for the fuel-logs own-record server scope D2 already shipped. It is **mobile-only and additive**: verified green (the mobile typecheck/lint/jest-expo trio), with **no API / schema / migration / dependency change** (the 808-test API suite is unaffected — re-confirmed during planning that the D2 `FuelLogsService.create` driver scope already covers this exactly). A driver opens the new **Log fuel** screen, picks one of **their own trips** (the same auto-scoped `listMyTrips()` list the trip screen uses), and the **vehicle is derived from that trip** — so the server's trip-vehicle consistency check always passes and the c4 "their vehicle = the vehicle on their own trip" rule is honored by construction; they enter **liters + price-per-liter** (decimals) and an **optional odometer** reading, see a **total-cost preview**, and submit `POST /api/v1/fuel-logs { vehicleId, tripId, date: now, litersMl, pricePerLiterPaisa, odometerReadingKm? }`. `totalCostPaisa` + `createdById` are derived server-side (the `.strict()` schema rejects either), and the D2 predicate enforces the own-trip rule (a tripless driver log → 400, a foreign trip → 404, an unlinked driver → 403). **New `apps-mobile/driver/src/fuel.ts`** holds the standalone pure domain — `litersToMl` / `rupeesToPaisa` (copies of the web `fuel-logs-schema.ts` / `money.ts` converters, kept in sync not imported; the same `Math.round` half-up rule, so the wire integers match the API bit-for-bit), `previewTotalCostPaisa` (algebraically identical to the server's `deriveTotalCostPaisa`, so the preview equals the persisted total), and the pure `fuelLogPayload({ vehicleId, tripId, liters, pricePerLiter, odometerKm? }, nowIso)` builder (converts to mL / paisa, omits the odometer key when unentered) — mirroring `trips.ts`'s "primitives + nowIso" shape; 9 new jest-expo tests (`src/fuel.test.ts`). **`src/api.ts`** gains `createFuelLog()` reusing the existing `apiFetch` (the `getCookie()` session credential, unchanged). **`App.tsx`** grows a `HomeScreen` shell — a shared header + a **Trips / Log fuel** two-button segmented toggle (no navigation library yet; the app stays a single conditional tree) + sign-out — with the trip start/stop content lifted into a now-propless `TripScreen` and the new `FuelScreen` (a trip selector, liters / price / odometer inputs with inline bounds checks mirroring `TripRow`, the total preview, and a submit that clears the form + confirms). **Out of scope:** the station / receipt-number / notes fields (the server accepts them as optional; the mobile form stays minimal — liters / price / odometer); a tripless / no-vehicle driver fuel log (the server contract requires the driver's own trip); and the **EAS-vs-local-prebuild binary-path gate** (ADR-0033 c7), surfaced to the operator now that D3 is done — **D4** (foreground GPS) is the first slice that needs a custom build, not Expo Go. Real field use still needs the owed production deploy (the app is the API's consumer). See ADR-0034 and ADR-0033.

**Deploy-prep program — the agent-driveable half of the first-production-deploy gate (ADR-0014), T1–T6, COMPLETE.** This is the still-owed Phase-1 operational-milestone work: the first production deploy is the deploy-dependent half of the Phase-1 → Phase-2 gate, which per ADR-0025 stays **owed (deferred, not cancelled)** while Phase 2 proceeds. ADR-0014 (single VPS + Docker Compose + Caddy + nightly `pg_dump`→age→R2 backups) named a set of "implementation follow-on" artifacts it never built; this six-ticket loop program built them, **each ticket shipping its artifact WITH a CI check that validates it** so the loop's green-before-merge gate is meaningful for the non-TypeScript files (Dockerfiles / compose / workflows / shell) the existing unit tests never touch. **T1** ships the production image Dockerfiles — `apps/api/Dockerfile` (its runtime stage carries the Prisma CLI + `prisma/` schema + `migrations/` + `tsx` + the seed's TypeScript sources, **option (a)**, so `prisma migrate deploy` + `db:seed` run in-image — closing the unverified assumption `deploy.md` had flagged) and `apps/web/Dockerfile` (Next.js `output: 'standalone'`) — plus the PR-time `build-images` CI job that `docker build`s both on every PR (no push) so a broken Dockerfile reds the PR. **T2 (PR #122)** ships `docker-compose.prod.yml` (the five services — api / web / postgres `postgis/postgis:16-3.5` / redis `redis:7-alpine` / caddy — with `env_file`, named data volumes, `/health/ready` healthchecks, and `${IMAGE_TAG}` image tags), the `Caddyfile` (automatic Let's-Encrypt TLS; `/api/*` `/auth/*` `/health*` → `api:3001`, everything else → `web:3000`), and `.env.production.example` (every required var with placeholders + ADR-0013 tier annotations, no real secret), validated by the `deploy-config` CI job (`docker compose … config` + `caddy validate`). **T3 (PR #123)** adds the `push-images` job that, on merge to `main`, pushes `ghcr.io/aswaratech/fleetco-{api,web}:<sha>` to GHCR via the automatic `GITHUB_TOKEN` (`permissions: packages: write`, no operator secret) and generates a **CycloneDX SBOM per image** — landing the one ADR-0012 security-baseline item that had been deferred, so that baseline is now complete. **T4 (PR #124)** ships `.github/workflows/deploy.yml` (`workflow_dispatch` with a `sha` input — SSH to the box → `pull` → `migrate deploy` → `up -d` → smoke check) and the committed `deploy/smoke.sh` (`/health` + `/health/ready`, exit non-zero on failure); it is dispatch-only, so it never auto-runs and cannot break main-green, lint-validated by the `shellcheck deploy/*.sh` CI step. **T5 (PR #125)** ships `deploy/backup.sh` (nightly `pg_dump | gzip | age -r <recipient>` → R2 via rclone, 30-day prune, the cron line) and `deploy/restore.sh` (a guarded fetch → decrypt → scratch-`fleetco_restore` load that STOPS before cutover), configured by the gitignored `deploy/backup.env`, with `restore-from-backup.md` reconciled to the real script paths. **T6 (this PR, the program's last ticket)** reconciles `deploy.md` + `rollback.md` to the now-committed artifact paths (dropping the "implementation follow-on" / "unverified assumption" caveats), adds a copy-paste **first-production-deploy operator checklist**, and updates this file + `docs/tech-debt.md`. **All three deploy runbooks deliberately stay `STATUS: DRAFT`** — they flip to `ACTIVE` only after a real deploy actually executes them. **The operator-only half remains the owed gate to Phase-2 daily-use and is OUT of scope for the agent loop:** provisioning the VPS (amd64, 2–4 GB), pointing DNS, writing the real `/opt/fleetco/.env` secrets, generating the `age` keypair, running the first real deploy, and the within-two-weeks restore-from-backup drill. **No deploy has happened** — this program turned the first one into a copy-pasteable procedure backed by CI-validated artifacts rather than an improvised one. See ADR-0014, ADR-0012 (SBOM), ADR-0025 (the gate-ahead exception), and `docs/runbook/deploy.md` → "First production deploy — operator checklist".

**Fuel-efficiency report (Reports v2) — deferred Phase-1 daily-use polish, COMPLETE (A1–A3).** Like the ADR-0031 BS-date, Home-dashboard, and BS-date-picker work, this is deferred Phase-1 (daily-use) polish landing in the Phase-2 window, not new Phase-2 scope: it completes the "fuel efficiency" deliverable named for Reports v1 in `docs/product/roadmap.md` (§"Phase 1 — The Spine") that iter-23 left unshipped (iter-23 shipped the cost half only, via `getPerVehicleCost`). The per-vehicle **km/L + NPR/km** report with theft/engine **outlier flags** is computed entirely from data ALREADY captured — **no new ADR, no new dependency, no schema/migration change** (every input exists: `Trip.startOdometerKm`/`endOdometerKm`, `FuelLog.litersMl`/`totalCostPaisa`/`date`). The UI-surface gate is a **DESIGN.md §"Surfaces" section** (the Home-dashboard precedent), NOT an ADR. **A1 (PR #129)** specced the surface in DESIGN.md → "Per-vehicle fuel-efficiency report" (route, table columns, the `<Badge>`-flag mapping, the coverage caveat). **A2 (PR #130)** shipped the server slice: `ReportsService.getPerVehicleEfficiency({from,to,vehicleId?})` extends the EXISTING reports module along the seam its docstring anticipated — distance is Σ(`endOdometerKm` − `startOdometerKm`) over **COMPLETED trips** (the Trip aggregate is the distance system-of-record, ADR-0003), attributed to the window by `endedAt`, deliberately NOT the nullable/non-monotonic fuel-log odometer; consumption is `fuelLog.groupBy` `_sum:{litersMl,totalCostPaisa}` on the reporting `date`; km/L and NPR/km are display ratios computed at the edge and never stored; the per-vehicle flag (`EfficiencyFlag` = `degraded|improved|normal|insufficient-data`, 15% deviation / 50 km / zero-litre floors) compares the window against the prior equal-length window via exact integer cross-multiplication; exposed as `GET /api/v1/reports/per-vehicle-efficiency` (AuthGuard-only, reusing `ReportsQuerySchema` verbatim — no new capability, no `ReportsModule` change), with ~35 new tests over the real `fleetco_test` PostGIS DB. **A3 (this PR, the program's last ticket)** ships the web surface + the memory close-out: `apps/web/src/app/reports/per-vehicle-efficiency/` (`page.tsx` + `per-vehicle-efficiency-filters.tsx` + `types.ts` + `loading.tsx`) mirrors the `per-vehicle-cost/` trio — the seven-column table (Vehicle → `/vehicles/<id>`, Distance via `formatKm`, Litres via `formatLiters`, km/L via the new `formatKmPerLitre` [two decimals], NPR/km via `formatNpr` over the paisa-per-km figure, Fuel cost via `formatNpr`, Status), the Status `<Badge>` (`degraded` → error "Efficiency down", `improved` → success "Improved", `insufficient-data` → neutral "Not enough data", `normal` → no badge — the absence is the signal), a totals footer, the BS window subtitle, and a muted coverage note framing flags as "investigate", not proof; the empty state is "No fuel or trip activity in the selected window." The pure `formatKmPerLitre` helper (two-decimals-or-em-dash) joins `lib/units.ts` beside `formatKm`/`formatLiters` (9 new tests; 135 web total). The date controls are the shipped `<NepaliDatePicker>` (ADR-0032 / B3), exactly as the cost report — the kickoff's "native AD inputs" phrasing predated ADR-0032 (it carried ADR-0031 commitment 6, which deferred the picker; ADR-0032 shipped it and the cost report adopted it), and DESIGN.md's controlling instruction is "exactly as on the cost report"; the now-corrected DESIGN.md §"Subtitle & BS calendar" bullet records this supersession. A "Fuel efficiency" quick-link joins the home dashboard beside "Cost report". **No new design token or component** (the drift test stays green, `globals.css` `@theme` untouched), no new dependency, no API/schema/migration change. **Memory:** a new glossary **Per-vehicle fuel-efficiency report** entry + the **Fuel log** / **Odometer** forward-refs flipped from "future km/L report" to shipped (documenting that the report deliberately uses completed-trip distance, NOT the fuel-log odometer); roadmap + this file note Reports v1's "fuel efficiency" is now shipped as Reports v2; a `docs/tech-debt.md` back-ref from the Paid-off odometer-monotonicity entry; DESIGN.md's stale "native AD" subtitle line corrected to the shipped `<NepaliDatePicker>`. Gates green on every checkpoint (`pnpm lint` + `format:check` + `typecheck` + the 135 web tests + the SSR `pnpm --filter @fleetco/web build`, which exercises the new server component at build time). **HONEST FRAMING (unchanged):** with a nullable/non-monotonic fuel odometer and ~15–20% dashboard drift this is a reliable fleet/period-level km/L **trend + exception flag, not a forensic per-fill meter**; a flagged row is "investigate", not "proven theft"; it self-sharpens once the driver app makes odometer-at-fill routine. **A minor design-spec note for the operator / A1:** DESIGN.md describes the Distance column as "via `formatKm` (one decimal)", but the shipped `formatKm` renders whole km (zero decimals); A3 reuses `formatKm` as the kickoff's reuse list directs (modifying it would change the Vehicle-detail and Fuel-log surfaces — a visual change needing its own ticket), and since `distanceKm` is always an integer the difference is cosmetic ("1,234 km" vs "1,234.0 km"). **With A3 the three-ticket Reports v2 program (A1–A3) is COMPLETE.** Per-vehicle and per-trip P&L remain the open Reports items. See DESIGN.md §"Surfaces" → "Per-vehicle fuel-efficiency report" and the glossary's **Per-vehicle fuel-efficiency report** entry.

**Preventive-maintenance program — engine-hours tracking + service scheduling (ADR-0036 + ADR-0037), Phase-3 scope pulled forward, COMPLETE (B1–B6).** Both ADRs were **accepted by the PO on 2026-06-18** ("I accept all ADRs, go ahead", the build-now path), and Program B built them as a six-ticket vertical-slice sequence. This is **Phase-3 headline material** — the roadmap's "Program B": maintenance schedules driven by kilometres / engine-hours / calendar dates, and service history per vehicle (`docs/product/roadmap.md` §"Phase 3" — Calendar-driven concerns) — **pulled forward under the ADR-0025 ahead-of-gate pattern** as a new, separately-decided exception (each ADR's §"Phase mismatch" records the proposal; the PO's acceptance authorized it). It rotates proven machinery rather than inventing: the iter-11 Trip→Vehicle odometer `$transaction` bump, the iter-12/13 lifetime stats, the ADR-0031 `complianceBadgeState` due/overdue pattern, and the Phase-1 aggregate CRUD convention (ADR-0006). What landed: **B1 (PR #137)** — engine-hours schema + service: a `meterType` discriminator on `Vehicle` (`ODOMETER_KM` default / `ENGINE_HOURS` / `BOTH`) + nullable integer engine-hours columns (tenths-of-hour) on `Vehicle` (start + current) and `Trip` (start + end) mirroring the odometer pair; the Trip→Vehicle hours auto-update inside the EXISTING `COMPLETED`-transition `$transaction` under the same monotonic "once forward" rule; `totalHoursLogged` in the per-vehicle lifetime stats; hand-authored migration; server + tests only. **B2 (PR #138)** — engine-hours capture UI: the meter-aware reading capture on the web trip edit/stop form AND the driver-app trip-stop screen (`apps-mobile/driver/src/trips.ts` pure builders + `App.tsx`), prompting for the reading(s) the vehicle's `meterType` calls for; the lifetime-hours stat surfaced; + mobile jest tests. **B3 (PR #139)** — the maintenance aggregate: `ServiceSchedule` + `ServiceRecord` models + hand-authored migration + CRUD service/controller mirroring `customers/` (`AuthGuard`, Zod `.strict()`, the `{ items, total, skip, take, sortBy, sortDir }` list shape, `createdById` from session, the P2002→409 / P2003→400|409 / P2025→404 mapping) + tests; `intervalType` (`DISTANCE_KM` / `ENGINE_HOURS` / `CALENDAR_DAYS`) + integer `intervalValue` in the dimension's minor units; both tables FK `Vehicle` `onDelete: Restrict`. **B4 (PR #140)** — service due/overdue: the pure `serviceScheduleState` / `worstServiceState` classifiers (`apps/web/src/lib/maintenance.ts`) rotating `complianceBadgeState` (a schedule vs. the vehicle's current meter reading [km/hours] or the wall clock [calendar] → due-soon/overdue, the calendar case literally reusing the compliance UTC-calendar-day date math); the amber/red `<Badge>` on the Vehicle detail page; the `ServiceRecord`→`ExpenseLog` MAINTENANCE/REPAIR cost-link (nullable FK — never a duplicated money column); and the "last service" anchor advancing on a `ServiceRecord` write inside one `$transaction`. **B5 (PR #141)** — web UI: schedules + service-history CRUD + the `/service-schedules/due` due-list + a "Services due" Home-dashboard card (`rollUpServiceSchedules`, `apps/web/src/lib/dashboard.ts`) + nav buttons + a DESIGN.md §"Surfaces" → "Preventive maintenance" note. **B6 (this PR, the program's last ticket)** — tests sweep + memory: the two B5 web form-schema modules (`service-schedules-schema.ts` / `service-records-schema.ts`) gained their own focused unit tests (the interval-representation conversion round-trip, the per-type bounds/decimal-budget validation, and the meter-reading validators — 44 new web tests, **236 total**), closing the gap they shipped with; the four glossary terms (**engine-hours** / **meterType** / **ServiceSchedule** / **ServiceRecord**) added and the now-stale "Service due/overdue badge" roll-up line corrected (B5 shipped it); this close-out + the roadmap progress note. **Disciplines reused (not re-derived):** the **integer-minor-units** rule — engine-hours and the `ENGINE_HOURS` interval store as integer tenths-of-an-hour, never floats (the `FuelLog.litersMl` precedent extended again), with display dividing at the render edge — and the **UTC-calendar-day rule** for the calendar-interval due/overdue boundary (shared with compliance via the one `thresholdState` core). **Out-of-scope boundaries that remain (stated honestly):** reminder **delivery** — email/SMS/push when a service crosses into due-soon/overdue — is **DEC3/ADR-0038, Phase 3**; this aggregate is the passive *producer*, and the badge warns only on a page the operator already opens (no cron, no queue). The per-dimension provisional due-soon windows (`DEFAULT_SERVICE_DUE_SOON_WINDOWS` — 500 km / 250 tenths-of-hour / 30 days) **and** the GPS-retention 90-day window are the **PO's to finalize** (both flagged in `docs/tech-debt.md`, the owner-level-number pattern). A parts/inventory/CMMS model, fleet-template (define-once-apply-to-many) schedules, and a background due-scan are explicitly not v1 (ADR-0037 §F). **The production-deploy gate is NOT met** and stays owed in parallel: the first production deploy, the restore-from-backup drill, live SLI/DORA reporting, and the retrospective sign-off remain operator-led (ADR-0025; `docs/tech-debt.md`). These two pulled-forward Phase-3 aggregates widen the "built-but-unused" surface ADR-0025 flagged — accepted as the PO-decided cost of the pull-forward, and the signal that the deploy should precede further pull-forwards. **With B6 the six-ticket preventive-maintenance program (ADR-0036 + ADR-0037, B1–B6) is COMPLETE.** See ADR-0036, ADR-0037, the glossary's **engine-hours** / **meterType** / **ServiceSchedule** / **ServiceRecord** / **Service due/overdue badge** entries, and DESIGN.md §"Surfaces" → "Preventive maintenance".

**Notification / compliance-and-maintenance reminder DELIVERY (ADR-0038), Phase-3 scope pulled forward, COMPLETE (C1–C4).** ADR-0038 was **accepted by the PO on 2026-06-18** ("I accept all ADRs, go ahead", the build-now / compliance-first path), and Program C built it as a four-ticket sequence. This is **Phase-3 headline material** — the roadmap's §"Phase 3 — Calendar-driven concerns": "email notifications for both maintenance and compliance… the SLI catalog adds a 'reminder delivery' indicator" — **pulled forward under the ADR-0025 ahead-of-gate pattern** as a new, separately-decided exception (ADR-0038 §"Phase mismatch" recorded the proposal; the PO's acceptance authorized it). It is the **active delivery layer** the passive ADR-0031 compliance badges and ADR-0037 service-due badges always pointed at — closing the gap that a lapse was only ever visible on a page the operator might never open. It **composes proven parts rather than inventing**: the ADR-0029 T4 RetentionModule daily-repeatable is the literal template, the ADR-0011 `sli.ts` catalog the SLI shape, the shipped `complianceBadgeState` / `serviceScheduleState` the due/overdue sources. What landed: **C1 (PR #143)** — the **Mailer seam**: a thin one-method `Mailer` interface + the pinned-exact `resend@6.14.0` dependency (isolated to this ticket) + a `ResendMailer` (reads the Tier-1 `RESEND_API_KEY` via the typed `env`) + a no-network `MockMailer` the DI factory selects whenever the key is absent (so dev / test / CI never reach the network). **C2 (PR #144)** — the **scan + idempotency + compliance source**: a dedicated `NotificationModule` owning the `notifications` queue + a single keyed `upsertJobScheduler` daily scan (the RetentionService template, concurrency-1) + the new **`NotificationLog`** model + migration (the dedup tuple `(subjectType, subjectId, reminderKind, state, occurrenceKey)`, send-once-per-lapse) + the compliance source via the SHARED `complianceBadgeState` + `formatNepaliDate` **extracted into `@fleetco/shared`** (`packages/shared/`, the drift guard made structural) + the pure §Voice Bikram-Sambat **reminder digest** + the scan→send split (one send job per recipient, each independently retried). **C3 (PR #145)** — the **maintenance source + SLI**: Program B's `serviceScheduleState` / `worstServiceState` wired into the SAME scan via the same shared-classifier discipline (moved to `@fleetco/shared`), plus the **`reminder_delivery` SLI** in `apps/api/src/common/sli.ts` (the valid event is a `Mailer.send` attempt, `error_kind` = exception class name only so the Tier-2 address never leaks, target 99.0% / 28d). **C4 (this PR, the program's last ticket)** — the **web read surface + close-out**: a read-only reminder-history view (`GET /api/v1/notification-logs` + `apps/web/src/app/notification-logs/`) that makes the `NotificationLog` audit ledger visible (list + detail, Sent in BS, the state `<Badge>`, recipient, provider message id; ADMIN-only via a new `notifications:read` capability; the ledger is append-only, so there is no write path), a read-only delivery-config panel (cadence + recipient policy, operator-configured), the five glossary terms (**Mailer** / **NotificationModule** / **NotificationLog** / **reminder digest** / **reminder_delivery**), and this close-out. The read surface is a **sibling `NotificationLogsModule`**, not a controller on `NotificationModule` — ADR-0038 c2 fixes that the scan module has NO HTTP surface, so the read concern reads the `notificationLog` table through the shared PrismaService (the ReportsModule precedent). **Disciplines reused (not re-derived):** per-feature queue ownership + the idempotent boot scheduler (ADR-0029 c2/c3), the shared-classifier **drift guard** (one pure `@fleetco/shared` classifier serves both the web badge and the API scan, ADR-0038 c6), and the **send-once dedup** keyed on the lapse occurrence (a renewal re-arms; ADR-0038 c5). **HONEST framing — the channel is BUILT but cannot yet DELIVER.** A reminder channel is the most deploy-dependent feature yet (ADR-0038's accepted cost): real email reaches an inbox only from a **running production system** with the operator's `RESEND_API_KEY` set **and** a verified sending-domain DNS (SPF/DKIM/DMARC) — none of which exist while the app has never run in production. Until then the scan runs against the `MockMailer` (records, no network), and the 28-day `reminder_delivery` SLI has no real sends to measure. **The production-deploy gate is NOT met and stays owed in parallel** (the first production deploy, the restore-from-backup drill, live SLI/DORA reporting, the retrospective sign-off — all operator-led, ADR-0025; `docs/tech-debt.md`); this is the third pulled-forward Phase-3 program widening the "built-but-unused" surface ADR-0025 flagged — accepted as the PO-decided cost, and the sharpest signal yet that the deploy should precede further pull-forwards. **With C4 the four-ticket notification program (ADR-0038, C1–C4) is COMPLETE.** See ADR-0038, ADR-0011, ADR-0031, ADR-0037, and the glossary's **Mailer** / **NotificationModule** / **NotificationLog** / **reminder digest** / **reminder_delivery** entries.

**Customer VAT invoicing (ADR-0039), Phase-4 "Money" scope pulled forward, COMPLETE (D1–D7).** ADR-0039 was **accepted by the PO on 2026-06-18** ("I accept all ADRs, go ahead", the build-now path — see its `## Acceptance` block), and Program D built it as a seven-ticket vertical-slice sequence. This is **Phase-4 headline material** — the roadmap's §"Phase 4 — Money": "customer invoicing" — and the **FURTHEST pull-forward yet**: a Phase-4 jump from a Phase-2 present (two phases ahead, larger than the Phase-3 Programs B and C), **pulled forward under the ADR-0025 ahead-of-gate pattern** as a new, separately-decided exception (ADR-0039 §"Phase mismatch" recorded the proposal; the PO's acceptance authorized it). It is FleetCo's **first revenue-side aggregate** — every prior money surface tracks what the fleet COSTS (FuelLog / ExpenseLog); this is the first that BILLS a customer. It **composes proven parts rather than inventing** (ADR-0039 "What this makes easier"): the dedicated two-table aggregate copies ServiceSchedule/ServiceRecord (ADR-0037) + the customers/geofences CRUD shape; the VAT/TDS math is the `FuelLogsService` half-up `Math.round` rule applied to a new computation; the gapless numbering HARDENS (does not reuse) `JobsService.nextJobNumber`; the cross-aggregate line assembly mirrors `ReportsService` read-and-merge through PUBLIC interfaces; dates render Bikram Sambat (ADR-0031); the PDF library + the R2 client each hide behind a thin FleetCo-owned interface (the Mailer / wkt.ts / nepali-date.ts wrap-the-vendor discipline). What landed: **D1 (PR #147)** — the `Invoice` + `InvoiceLine` schema + hand-authored migration + the read skeleton (list + detail) + 36 tests (the nullable-until-issue frozen-tax columns + the `InvoiceStatus` / `DocumentType` / `InvoiceServiceType` enums + the self-FK + back-relations). **D2 (PR #148)** — the PURE VAT + TDS computation (`invoice-tax.ts` / `invoice-tax.constants.ts`: the FLAGGED proposed rates `INVOICE_VAT_RATE_BP = 1300`, `INVOICE_TDS_RATE_BP { VEHICLE_HIRE: 150, GOODS_TRANSPORT: 250 }`, integer paisa half-up, the rates riding IN the returned snapshot so D3 freezes them; 19 unit tests; migration-free). **D3 (PR #149)** — the gapless BS-fiscal-year numbering (`model InvoiceNumberSequence`, composite PK `(documentType, bsFiscalYear)` + `lastValue`, a counter row deliberately NOT a Postgres SEQUENCE — gapless across rollbacks) + `bsFiscalYear` in `@fleetco/shared` + the DRAFT→ISSUED lifecycle (freeze the snapshot, assign the number, one-way `issue`) + `createCreditNote` + the `InvoiceSettingsService` supplier-PAN gate; +109 tests. **D4 (PR #150)** — line management (`addLine` / `updateLine` / `removeLine`) + `buildFromJob`; `lineAmountPaisa` always derived server-side; every line write gated to a DRAFT. **KEY DISCOVERY (`docs/tech-debt.md`): there is NO `Trip.jobId`**, so build-from-job is operator-selected trips + amounts, NOT a Job→Trip traversal; the strongest consistency rule the schema supports (a referenced **Job** belongs to the invoice's **Customer**) is enforced, while the impossible trip-level customer check is flagged, not guessed. **D5 (PR #151)** — the server-side `pdfkit` PDF render behind the `InvoicePdfRenderer` seam + the **FIRST in-app Cloudflare R2 storage** behind the generic `ObjectStorage` seam (`@aws-sdk/client-s3`; creds operator-supplied env, flagged, the mock storage the dev/test/CI default); `issue()` renders + stores the frozen PDF INSIDE the issuing `$transaction` before the number-committing write (a render/store failure rolls back — NO burned number); `getPdf` is the anti-tamper split (ISSUED streams the frozen R2 bytes by `pdfR2Key`, DRAFT regenerates a watermarked preview). **FLAGGED (`docs/tech-debt.md`):** the Devanagari **कर बीजक** label awaits a bundled Devanagari font (pdfkit built-ins are Latin-only); v1 prints the English "Tax Invoice" label. **D6 (PR #152)** — the web invoicing surface (list / detail / create + DRAFT-edit workbench / build-from-job / same-origin PDF download) mirroring `apps/web/src/app/jobs/`; the pure client-side tax preview faithfully mirrors the API's `computeInvoiceTax`; +47 web tests; a DESIGN.md §"Surfaces" → "Customer VAT invoicing" note (the established UI-surface gate). **D7 (this PR, the program's last ticket)** — the tests sweep + memory close-out: surgical, non-duplicate test additions closing the gaps the program left (an end-to-end MANUAL write-path issue test through `create → addLine → update(serviceType) → issue` asserting the gapless number + frozen snapshot + `pdfR2Key`; an exact-Shrawan-1-boundary numbering test; two web pure-helper edge branches — **+4 tests, API 1285 / web 278**); the six glossary terms (**Invoice** / **InvoiceLine** / **VAT** / **TDS** / **credit note** / **fiscal-year invoice numbering**); this close-out + the roadmap Phase-4 progress note; and the **ADR-0013-revisit-for-financial-records** flag (`docs/tech-debt.md`). No feature / schema / API / `permissions.ts` change in D7 (the sweep found no bug; it is documentation + a final test-gap pass). **HONEST framing — invoices are BUILT but cannot yet really BILL** (the register's established framing, e.g. the Program-C notification close-out): the Nepal VAT/TDS rates, the service-type→rate mapping, FleetCo's own supplier PAN, and the exact IRD-required field set all remain **operator/accountant-verify before real billing** (ADR-0039 c9 — a DEFERRED operator gate the build-and-flag posture preserves, NOT a closed one; the PO chose build-now with PROPOSED values); a real issue also needs the still-owed **production deploy** + the operator's R2 credentials + supplier PAN/name env; and **real-time IRD/CBMS e-billing is explicitly OUT of v1** (ADR-0039 c10 — FleetCo produces compliant PDF invoices, it does not transmit them live to the IRD). **Companion decision owed:** the **ADR-0013 data-classification revisit for financial records** (whether invoices warrant a distinct tier / longer retention / mandatory audit-logging) — flagged in `docs/tech-debt.md`, to be decided in its own ADR later, NOT assumed now (ADR-0039 c9 / "Revisit when"). **The production-deploy gate is NOT met and stays owed in parallel** (the first production deploy, the restore-from-backup drill, live SLI/DORA reporting, the retrospective sign-off — all operator-led, ADR-0025; `docs/tech-debt.md`). This is the **first Phase-4 pull-forward** — after the three Phase-3 ones (engine-hours ADR-0036, maintenance ADR-0037, notifications ADR-0038) — and the **furthest yet** (two phases past the Phase-2 present), widening the "built-but-unused" surface ADR-0025 flagged: accepted as the PO-decided cost, and the **sharpest signal yet that the deploy should precede further pull-forwards**. **With D7 the seven-ticket customer-invoicing program (ADR-0039, D1–D7) is COMPLETE.** See ADR-0039, ADR-0025, ADR-0013, and the glossary's **Invoice** / **InvoiceLine** / **VAT** / **TDS** / **credit note** / **fiscal-year invoice numbering** entries.

**UX uplift — design-system→app consistency (deferred Phase-1 daily-use polish), Phase 0 + Phase 1 COMPLETE (PRs #154–#168).** A multi-phase program closing the drift between the canonical design system (`docs/design/DESIGN.md`, ADR-0007) and the shipped `apps/web`, run **design-first** (DESIGN.md or a locked slice-mockup leads → the PO ratifies-by-merge → code follows), landing in the ADR-0025 window as contract-honoring polish, not new phase scope. **Phase 0 (PRs #154–#158, #160)** fixed the systemic Tailwind-4 wiring bug: shadcn's `:root` aliases (`bg-primary` / `border-input` / `ring-ring`) generate NO utilities under Tailwind 4 — only `@theme` `--color-*` tokens do — so `button` / `input` / `select` / `alert-dialog` / `form` were re-pointed to the live `@theme` semantic utilities (`popover.tsx` was already correct = the template); the load-bearing trap is `accent-foreground` (shadcn's alias resolves to text.primary but FleetCo's `--color-accent-foreground` is white, so classes are mapped by *resolved color*, not name). Phase 0 also added five leaf primitives (`ui/{card,skeleton,checkbox,textarea}.tsx` + `components/money.tsx`) and — the durable artifact — the **design-token-consumption guard** (`apps/web/test/design-token-consumption.test.ts`), which derives the dead-alias set from `globals.css`, scans every `ui/*.tsx`, and fails CI on any dead-alias consumption (it caught two transparent-render bugs a manual audit had missed). **Phase 1 (PRs #161–#168)** built the authenticated navigation shell DESIGN.md §Navigation had specified but left "Contract only": a single nav source (`apps/web/src/lib/nav.ts` — five PO-ratified groups Operations / Money / Maintenance / Reports / Logs feeding both the sidebar and the home quick-links strip via `navForRole(role)`); the `(app)` route-group shell (`components/app-shell/app-shell.tsx` — a 240px↔64px collapsible sidebar persisted to localStorage + a top-bar user menu, hosted by `app/(app)/layout.tsx` with one auth gate + a `/me` role fetch, all routes `git mv`d under `(app)/` so URLs are unchanged); the Breadcrumb primitive + a `/reports` index; the migration of all **24 list + detail pages** onto four extracted shared components (`SortableHeader` / `Pagination` / `DetailRow` + `lib/list-params`), eliminating the ~12× hand-copied duplication; and finally the **⌘K command palette** (ADR-0040) — `cmdk@^1.1.1` as the one new top-level dependency (React-19 verified at add time), `ui/command.tsx` re-pointed to `@theme` with the modal built on the already-installed `radix-ui` Dialog (no new dialog dependency), mounted in the shell via a ⌘/Ctrl+K keydown listener + a top-bar "Search…" affordance that sources `navForRole(role)` → `router.push` (navigation-only; resource search is a later phase). Each ticket shipped as its own PR, PO-ratified-by-merge, never self-merged; DESIGN.md's component-status table was flipped (Navigation shell / Breadcrumb / Pagination / Command → Built) with dated provenance, and the locked app-shell mockup was archived to `slices/_archive/`. **NEXT for this program: Phase 2 — surface consistency (e.g. TanStack behind the `Table` primitive) — NOT started.**

**Pre-deploy audit, the ADR-0041 window, and the two commitment-4 programs (2026-07-02).** Two batches landed this day. First, the **pre-deploy audit + hardening batch (PRs #169–#173)**: the multer transitive high pinned via a root pnpm override (#169); the pino redact list fixed to the real Tier-2 field names `fullName`/`phone`/`dateOfBirth` with the paths extracted to `apps/api/src/observability/log-redact.ts` + a regression test (#170); first-read memory repair — this file's drifted header fixed, the UX-uplift program backfilled, ADR-0040 flipped to Accepted, an ADR-0039 correction appended (#171); **ADR-0041** closing the ADR-0025 pull-forward exception window (#172); and the main branch-protection ruleset authored as `docs/runbook/branch-protection.md`, `STATUS: DRAFT` until the operator applies it (#173). Second, a **29-agent verified whole-project audit** (6 dimension auditors, every medium/high finding adversarially verified — 39 claimed → 16 confirmed — plus 2 feasibility scouts and 2 critique-hardened program designs) produced: the governance/docs repairs in this PR (ADR-0041's Status flipped to Accepted mirroring the ADR-0040 pattern; ADR-0025 gains the forward-license-closed pointer; the stale pre-`(app)` paths in living docs repaired; `scripts/check-doc-paths.mjs` now guards living-doc path/link integrity as a CI step; `memory-architecture.md`'s overstated automation claims corrected to describe what actually runs); **two new program ADRs, each the fresh PO-ratified ADR that ADR-0041 commitment 4 requires** — **ADR-0042** (dedicated hardware GPS trackers via a version-pinned self-hosted Traccar gateway container feeding the existing `gps-ingest` pipeline through a new machine-authenticated adapter + `TrackerDevice` IMEI→vehicle model, and a read-only `/map` page on the existing Leaflet stack with the DEPOT geofence as the yard; **ticket M1 is the first production deploy**, which the always-on hardware makes a technical prerequisite rather than a deferred obligation) and **ADR-0043** (an AI chat agent as an in-process tool layer over the module services — DeepSeek-first behind a provider-agnostic raw-fetch client, a curated ~25-tool registry with no deletes/invoice-issue/raw-GPS, **fully autonomous creates/updates by explicit PO decision** compensated by pre-image undo, server-derived action cards, loop budgets, and an indefinitely-retained `AgentAction` audit spine, plus the ADR-0013 amendment naming the hosted-LLM egress); and a **five-PR engineering-fix wave** now in flight from the audit's confirmed findings *(update: all five merged the same day, PRs #174–#178)* — this docs/governance PR, the RBAC hardening (the live DRIVER role can currently CRUD nearly every AuthGuard-only surface, 12 of 16 controllers; also the `toUserRole` fail-closed coercion escalates instead of de-escalating), the web dead-token fix (27 route files styling form controls with `:root`-alias utilities that compile to no CSS, plus extending the token-consumption guard to scan `src/app`), the Caddyfile security-header block, and the mobile 401/session-expiry handling (ADR-0034 c3). The PO's four program decisions (build-both-now with the deploy as M1; hardware trackers over driver-phone GPS; provider-agnostic DeepSeek-first with the Tier-5/license/DoB redaction line; fully-autonomous agent writes) were taken interactively with risks surfaced and are recorded in the two ADRs' Context sections. **Driver-app D4–D6 are deprioritized (deferred, not cancelled) per ADR-0042** — the hardware tracker is the primary GPS producer now. See the as-of block at the top of this file for the resulting permitted-work-streams picture.

**Phase 2 — AI chat agent (ADR-0043), COMPLETE (A1–A8).** The eight-ticket agent program is code-complete: an in-process, curated tool layer over the existing module services, DeepSeek-first behind a provider-agnostic client, every action executed as the requesting human and audited. A1 landed the env plumbing (`DEEPSEEK_API_KEY` optional-Tier-1 + `DEEPSEEK_MODEL`, the RESEND kill-switch pattern) and the glossary entries; A2 the three-model persistence + audit spine (`AgentConversation` / `AgentMessage` / `AgentAction`, the repo's first `onDelete: SetNull` on the audit row's transcript FKs, a 180-day transcript prune keyed on last-activity `updatedAt` while `AgentAction` rows are kept indefinitely, and the transcript keys joined to the pino redact + span-scrub denylists); A3 the `LlmClient` seam (`DeepSeekClient` raw-fetch with a 60 s per-call abort + budget-aware retries, and a no-network `MockLlmClient` bound whenever the key is unset — dev/CI/prod-kill-switch); A4 the 23-tool read/report registry with the redaction choke point (Tier-5 GPS + `dateOfBirth` stripped, `licenseNumber` masked, names/phones passed as operational contact data — c6); A5 the turn loop (≤8 rounds / ≤15 tool executions / 90 s wall-clock, a per-conversation in-flight lock → 409, per-call token usage on each message, an `AgentAction` row for every dispatch), the conversation/turn endpoints, and the `agent:use` gate (ADMIN-only, c1); A6 the DESIGN.md §"Agent chat" section and the `/chat` page (the first shippable agent — questions + reports end-to-end); A7 the eight create tools (the reversible-write tier, transform-free wrappers mirroring each module's create schema, re-validated module-side; a registry `dispatch()` envelope that derives the affected entity from the pre-redaction result; the `/chat` action card's server-derived deep-link); and A8 (this program's last ticket) the three update tools (`update_vehicle` / `update_driver` / `update_trip`) with **pre-image capture** — the raw prior row stored into `AgentAction.previousJson` before an update executes (c4b), never sent to the model, the source for the manual undo procedure — plus the cross-user activity ledger (`GET /api/v1/agent/actions` on the `agent:use` gate → the `/agent/activity` page, the second ADMIN-only web surface, in the Logs group beside Reminder history), `docs/runbook/agent-operations.md` (key rotation, the kill switch, the pre-image undo, the prompt-injection posture, the DeepSeek training-opt-out request), the glossary's **Action card** / **Pre-image** entries, and the tech-debt entry for the deferred SSE token-streaming follow-up (c7). **Honest framing:** the program is BUILT, not yet in production USE. No production deploy has run (`deploy.yml` still has zero runs); `DEEPSEEK_API_KEY` is unset, so the `MockLlmClient` binds and no prompt content egresses; the training-opt-out request is a runbook procedure not yet executed. Per ADR-0043's phase-mismatch section the agent's first production use stays gated on the ADR-0042 M1 deploy — construction complete, activation owed. *[Update 2026-07-05: the training-opt-out request has been sent, and the full client path was validated once against the REAL DeepSeek API on a local stack (real key in the dev env only) — `DeepSeekClient` bound, `fleet_snapshot` dispatched and audited, non-zero `promptTokens`/`completionTokens`. Production remains untouched — no deploy has run and no key exists outside the dev machine; activation now waits only on M1.]* **With A8 the eight-ticket AI-agent program (ADR-0043, A1–A8) is COMPLETE.** See ADR-0043, `docs/runbook/agent-operations.md`, and the glossary's **AI agent (FleetCo)** / **DeepSeek** / **Action card** / **Pre-image** entries.

**Phase 2 — WhatsApp agent channel (ADR-0046), W1–W2 (2026-07-09).** The W-program opens a two-way WhatsApp channel over the existing ADR-0043 agent — a message to the company number runs the agent AS the resolved human behind a verified number and replies in-thread — the fourth exercise of ADR-0041 commitment 4; all production use is M1-gated (the inbound webhook needs a public 24/7 HTTPS endpoint that only exists post-deploy). **W1 (PR #221, merged)** is governance only: ADR-0046 (ratified by the PO's merge), the ADR-0041 fourth-exercise + ADR-0013 bidirectional-egress annotations, inert `TWILIO_*` / `WEB_PUBLIC_URL` env plumbing, glossary + DESIGN §Voice, and a DRAFT `docs/runbook/whatsapp-operations.md`. **W2 (this PR)** lands the schema + identity seam with no channel wiring yet — two new tables plus the normalize/resolve service and the provisioning script. `AgentPhoneLink` is the per-user phone→user map: `phoneE164` is `@unique` (the exact-match security key — a loose match would let a stranger's number resolve to a linked user, or the owner's own number fail to resolve), a nullable `conversationId` FK (SetNull) anchors one deterministic get-or-create thread per link, and `verifiedAt` / `active` carry provisioning + opt-out; `userId` FK is Restrict (the audit-actor posture). `WhatsAppMessageLog` is the inbound/outbound delivery + idempotency ledger on the `NotificationLog` shape: `providerSid` is `@unique` — the load-bearing replay defense, since a Twilio signature carries no nonce, so check-existing-by-SID before the turn is the only guard against a replayed webhook POST double-firing the no-confirmation-gate agent writes (ADR-0043 c3); its conversation/message FKs are SetNull so the wire-facts ledger survives the 180-day transcript prune (the `AgentAction` posture). A shared, STRICT `normalizeE164` (strip the Twilio `whatsapp:` prefix, require canonical E.164, reject internal separators, never echo the raw number) is the ONE normalizer used at both link-write and inbound-resolve, so a stored key and an inbound number are byte-identical (the coherence guarantee). `WhatsAppIdentityService.resolveSenderToActor` is the channel's **authorization chokepoint**: `runTurn` trusts its `actor` and does not re-check `agent:use`, so this resolver is the wall the HTTP `RolesGuard` used to be — it fails closed (403) on an unparseable, unmapped, deactivated, or non-`agent:use` number and re-checks the **live** role at turn time (c9B), so a demotion after linking closes the channel automatically. The privileged, idempotent `link-whatsapp-number.ts` script provisions a link, **refuses a non-ADMIN user**, and has no public endpoint (the `create-user.ts` precedent). 32 new tests (1704 API total). The `WhatsAppModule` + `TwilioSignatureGuard` + inbound webhook + worker (W4) and the `WhatsAppSender` seam + text-action-card renderer (W3) are the remaining text-path tickets; the receipt-photo path (W5) inherits the standing image-intake pause. See ADR-0046 and the glossary's **WhatsApp channel** / **Twilio** / **Phone link** entries.

**Phase 2 — WhatsApp agent channel (ADR-0046), W3 (2026-07-10).** W3 lands the outbound half's two composable parts, still with no channel wiring (the module/webhook/worker are W4): the **`WhatsAppSender` seam** and the **pure reply renderer**. The seam is the `Mailer` pattern rotated to WhatsApp (ADR-0046 c5): an abstract `WhatsAppSender` DI token (`send({to, body}) → {sid?}`), a `TwilioWhatsAppSender` that POSTs the Messages API over ONE raw `fetch` with Basic auth — zero new dependency, the only file naming Twilio's endpoint — and a recording `MockWhatsAppSender`, selected by `createWhatsAppSender` (the extracted `useFactory` W4 wires verbatim: the real sender binds only when the full `TWILIO_ACCOUNT_SID`/`TWILIO_AUTH_TOKEN`/`TWILIO_WHATSAPP_FROM` group is present — the kill switch; `TWILIO_WEBHOOK_URL` gates inbound, not this). Sender discipline: ONE attempt, no internal retries (the W4 queue's job-retry policy is the retry layer — a sender-level ladder would multiply with it and double-send on an ambiguous timeout, since a Twilio POST is not idempotent); a per-send abort timeout; Tier discipline throughout (recipient + body ride the form body never the URL, errors carry only a category + Twilio's numeric code, the auth token exists only in the Authorization header). The renderer is ADR-0043 c4c honesty over text (c6): a **pure** `renderTurnForWhatsApp(turn) → string` renders the FINAL assistant message (skipping empty tool-call rounds), server system notices AS notices (`⚠ system:` prefix — including the ungrounded-claim guard's; the server never ventriloquizes the model and vice versa), and one text action card per `AgentAction` — glyph + tool + status word, a changed-fields summary (argsJson keys minus `id`, succeeded writes only), and ONE absolute deep-link against `WEB_PUBLIC_URL` through a mirrored entity-route + plain-id allowlist (fail closed: unknown type or suspect id renders as text, never a URL; the map is kept in sync with the web's `ENTITY_ROUTES` by comment-pinned convention). The body truncates at 4096 codepoints with an honest marker and `chunkWhatsAppBody` splits ≤1600-codepoint segments preferring newline boundaries, content-preserving and surrogate-safe. 38 new pure tests (1747 API total) — wire shape, PII-free errors, timeout/network categories, factory selection, final-message selection, notice rendering, card statuses, allowlist fail-closed paths, truncation, chunk invariants. W4 (module + `TwilioSignatureGuard` + webhook + worker — the text path end-to-end) is next; W5 (inert photo path) after. See ADR-0046 c5/c6.

**Phase 2 — WhatsApp agent channel (ADR-0046), W4 (2026-07-10): the text path end-to-end.** W4 wires W2's identity + ledger and W3's sender + renderer into the live channel — a `WhatsAppModule` on the NotificationModule dedicated-delivery precedent (c1), registered in the app graph and **fail-closed inert until the operator sets the `TWILIO_*` env** (verified by boot smoke: the webhook answers 503 on an unconfigured box). Inbound is the Traccar machine-ingress posture, Twilio-adapted (c2): a `TwilioSignatureGuard` verifies `X-Twilio-Signature` = `base64(HMAC-SHA1(auth token, configured-canonical-URL + ascending-key params))` with a length-guarded `timingSafeEqual` — the recipe is zero-dependency and **pinned to Twilio's PUBLISHED test vector** (token `12345` → `0/KCTR6DLpKmkAf8muzZqo1nDgQ=`, an external truth, not the implementation agreeing with itself); the URL is the `TWILIO_WEBHOOK_URL` constant, never reconstructed from request headers; unconfigured → 503, missing/mismatched signature → 403. The guarded controller (`api/v1/whatsapp/inbound`, no AuthGuard — the human's authorization happens at turn time) validates a LOOSE boundary schema and 202s fast, enqueueing onto the feature-owned `whatsapp-inbound` queue with a 6-attempt exponential-backoff envelope sized to outlive the agent's 90 s turn wall-clock. The `WhatsAppInboundProcessor` (in-process per ADR-0029 c5 — load-bearing: it shares the agent's in-memory in-flight lock) runs the c3 steps, each an audited `WhatsAppMessageLog` outcome: **(a)** claim by MessageSid — the create against the `@unique` column IS the dedup/replay defense (a signature has no nonce; c7/c9A), with an atomic reclaim (`waiting_retry`, or a stale `processing` only when `attemptsMade > 0`) so the job's own retries proceed while a concurrent Twilio duplicate can never steal an in-flight row; **(b)** `STOP`/`START` keyword handling before any turn (idempotent deactivate/reactivate, `opt_out`/`opt_in` audit rows, never a reply); **(c)** the resolver chokepoint — unmapped/unparseable → `dropped_unmapped`, deactivated or non-`agent:use` → `dropped_unauthorized`, all silent drops (c9C open-relay: the reply goes ONLY to the link's stored canonical phone, never a number from message content); **(d)** a per-user UTC-daily inbound cap (c8 — every inbound is a billed outbound; a PO-tunable named constant, 100/day) → `rate_limited`; **(e)** get-or-create the link's stable conversation (c4 — pointer read via the shared-Prisma public-data seam, creation through `AgentService.createConversation`, a stale/foreign pointer falls through to a fresh create); **(f)** `runTurn` as the resolved human; **(g)** a same-conversation 409 parks the row `waiting_retry` and rethrows for the backoff retry (re-enqueue, never drop) — **any other turn failure is TERMINAL** (`failed`, no retry: a turn is not idempotent, and auto-re-running one that may have executed writes is exactly the double-fire the dedup exists to prevent); **(h)** render (the c6 honesty renderer) → chunk → send, one outbound ledger row per segment (`sent` with the provider SID, or `failed` with the remaining segments withheld). `AgentModule` now exports `AgentService` with the c1 warning attached (runTurn trusts its actor — every non-HTTP caller must bring its own turn-time authorization wall). The runbook's opt-out section is filled (STOP/START semantics + permanent-removal via row deletion). 32 new tests (1779 API total): the published-vector pin + signing properties; the webhook over the real wire (parser × guard × pipe × controller — 202+envelope, all-params signature coverage, tamper 403, missing-header 403, unconfigured 503, non-message 400); and the processor pipeline against real Postgres (happy path, stable-thread reuse, stale pointer, duplicate/replay skip, both reclaim branches vs the steal defense, unmapped/unparseable/deactivated/demoted drops, STOP/START incl. unmapped, 409 rethrow, terminal failure, the cap, send failure, lossless chunked sends). Remaining: W5 (inert photo path + runbook promotion). Production use gates on M1 as ever — but with W4 the channel is CODE-COMPLETE for text: post-deploy it needs only the Twilio console setup + env + the PO's phone link. See ADR-0046 c1–c4/c7–c9.

**Phase 2 — WhatsApp agent channel (ADR-0046), W5 (2026-07-10): the photo path + runbook close-out — THE W-PROGRAM IS CODE-COMPLETE.** W5 lands the receipt-photo half of the PO's "text + receipt photos" scope, built-now-inert exactly as decided (§Context decision 5): an inbound photo's `MediaUrl0` (signature-verified with the rest of the webhook, forwarded as the loose boundary's first-and-only media item — WhatsApp sends at most one) is downloaded by a new zero-dependency `TwilioMediaClient` — Basic auth presented **only** to `https://api.twilio.com` (an SSRF host-allowlist checked before credentials attach, pinned against look-alike-host and userinfo tricks), one `fetch` with `redirect: "follow"` riding undici's documented Authorization-drop on the cross-origin 302→S3 hop, a 10 MB cap, and bare PII-free error categories — then stored through `AgentAttachmentsService.upload()`, which re-runs the REAL magic-byte sniff + size cap (Twilio's declared content type is never trusted; the processor tests prove a lying `image/jpeg` declaration on PDF bytes is rejected), and finally rides `runTurn` as a first-class attachment (claim-on-send, extraction step 0) exactly like a web-composer photo. **With the image intake PAUSED (`AGENT_OCR_URL` unset — the ADR-0044 V0 decision) the turn degrades to the honest not-configured notice: the photo is stored and auditable, nothing is extracted, and the un-pause is the agent's own env flip with zero WhatsApp change.** Two honest edges: a failed download/sniff drops the WHOLE message (caption included) with a server-authored "photo could not be received" reply audited as `media_failed` — a caption-only turn would answer as if the photo had arrived — and a message with neither text nor supported media (sticker, location, reaction) drops as `ignored_empty` before any conversation or LLM cost. `AgentModule` exports grow by `AgentAttachmentsService` (owner-scoped end-to-end, so the widening stays bounded by the same actor the W4 wall resolved). The runbook gains §Photo messages + a six-step post-M1 §Verification first-run checklist (text round-trip, action-card write, paused-photo degradation, fail-closed unlinked, STOP/START round-trip, kill switch) and its header now reads: mechanism fully built, awaiting only the M1 deploy for the DRAFT→ACTIVE flip. 19 new tests (1798 API total): the media client's allowlist/auth/redirect/cap/category contract, the webhook's media-forwarding + NumMedia-0 defense, and the processor's photo-with-caption, captionless-photo, media-failure, lying-content-type, and ignored-empty paths. **W1–W5 are all shipped: the WhatsApp channel is code-complete** — post-M1 its activation is purely operator work (Twilio console + secrets + the PO's phone link + the verification checklist). See ADR-0046.

**2026-07-10 — Local deploy dry-run (ADR-0041 c3 deploy-prep lane; branch `feat/deploy-dry-run`): deploy.md's first-production-deploy checklist + restore-from-backup.md + traccar.md's bring-up + rollback.md's tag-swap core executed END-TO-END for the first time anywhere, on a local production-shaped stack — 7 findings, 3 of them real bugs, all fixed and re-verified live in the same session.** Setup: the production compose project (name `fleetco`) stood up from an `/opt/fleetco`-analog scratch dir on the arm64 dev machine; api+web images built locally with ci.yml's exact commands at `d27f90d` (GHCR packages are PRIVATE — anonymous manifest GET refused, so the pull path is operator-PAT-only, marked); every `.env.production.example` placeholder filled with generated throwaways; an rclone `:local:` remote + throwaway `age` keypair stood in for R2; every genuinely-production-only step marked (ACME/DNS replaced by Caddy's internal CA on `host.docker.internal`, root-chown skipped, cron installed-not (hand-run instead), `NODE_TLS_REJECT_UNAUTHORIZED=0` only for the internal-CA SSR hairpin). Walk results: migrate-from-zero on the prod api image (full chain) ✓; seed ✓ (post-fix); smoke contract ✓ through Caddy (`/health`, `/health/ready`, 80→443 redirect, all four 2026-07-02 hardening headers, `-Server` stripped); admin sign-in + authed list + SSR pages ✓; backup→restore→Branch-A-cutover→revert proven on real bytes with a marker-row cross-check (cutover demonstrably served the restored DB) and the `_restore` guard mutation-tested (`RESTORE_DB=fleetco` refused, exit 2); traccar's first-boot crash-loop observed live → traccar.md's one-time DB creation run verbatim → gateway healthy, port posture correct (5027 wildcard / 8082 loopback-only), backup.sh's traccar dump self-activated on the next run exactly as promised, `traccar-prune.sh` clean; routine-deploy + rollback rehearsed by tag swap (api container moved dryrun→dryrun2→dryrun, smoke green throughout, datastores untouched). THE THREE BUGS — all invisible to dev and to both test suites, all only manifest on the production shape: (1) `db:seed` minted the founder at OFFICE_STAFF — `role` is `input:false` (ADR-0028 c8) so signUpEmail applies the least-privilege default, and seed-admin.ts never did create-user.ts's privileged post-signUp elevate; a real first deploy would have shipped a founder with no users:manage / agent:use / invoices:write behind green smoke, with no in-app repair path; fixed by delegating seedAdmin→createUser pinned ADMIN + 3 integration tests (mutation-tested: re-injecting the old behavior fails exactly the two ADMIN pins), verified on-stack with a before/after 403→200 on the agent routes. (2) `GET /me` — the ONE root-level API route — was missing from the Caddyfile `@api` matcher, fell through to the web service and 404'd, so EVERY server-rendered page threw 500 behind the proxy (the (app) shell calls `/me` for nav) while dev, where web talks straight to :3001, can never reproduce it; fixed (matcher += `/me`, convention comment added), verified SSR 500→200. (3) restore.sh's post-restore sanity check queried Prisma MODEL names (`"Vehicle"`/`"Trip"`/`"FuelLog"`/`"ExpenseLog"`/`"User"`) that don't exist — the schema `@@map`s all tables snake_case — so a real recovery would false-alarm "tables missing? empty dump?" against a perfectly good backup at the worst possible moment; fixed to the mapped names (`user` stays quoted, reserved word) and the drill re-ran clean. DOC LIES fixed in the same diff: no runbook step installed `age`/`rclone` on the box though backup.sh hard-requires both on the host cron (added to bootstrap); the traccar first-deploy crash-loop expectation existed nowhere (an expected-behavior note added after step 12, incl. the self-activating backup dump); the `NEXT_PUBLIC_API_URL` record was wrong in THREE places (deploy.md steps 0 and 5, the compose web comment, the env template) claiming the on-box value feeds the web server's runtime fetches — disproved from the built image (Next.js inlines `NEXT_PUBLIC_*` into the SERVER bundle too; 19 baked occurrences under the image's server chunks), all three now state the bake truth, and a new What-can-go-wrong entry documents the two SSR failure modes this implies (public-origin hairpin reachability from the box itself; matcher fall-through); `TWILIO_*` + `WEB_PUBLIC_URL` — read by env.ts since W1 and demanded by whatsapp-operations.md's setup step 3 — appeared in NEITHER env template, so inert empty-with-comment blocks were added to both (the DEEPSEEK kill-switch idiom, activation still owned by whatsapp-operations.md and M1-gated). Register updates: a dated correction on the existing NEXT_PUBLIC-bake tech-debt entry (its "on-box fixes server-side" clause was the same falsehood; the runtime-config discharge case is now stronger), plus a new small entry for `x-powered-by: Express` riding through Caddy (fingerprinting hygiene; own commit later, not a rehearsal rider). WHAT PRODUCTION ALONE STILL OWES (unchanged by this rehearsal, marked throughout): real ACME/DNS/Let's-Encrypt issuance; GHCR pull auth on the box (packages confirmed private); amd64 postgis on a real amd64 VPS (locally the arm64 pull-refusal was observed — "no matching manifest" — and the runbook's own documented `imresamu` ARM branch was taken, validating that warning empirically); deploy.yml's SSH transport + the `production` Environment secrets; real R2 and a real cron; the step-0 repo Actions variable with the real domain; sizing under load. STATUS and "Last verified" lines untouched everywhere — deploy.md / rollback.md / restore-from-backup.md / traccar.md stay DRAFT until the REAL deploy + drill flip them per their own gates; this rehearsal's record lives here and in the PR's step-verdict table. The founder-role and /me classes are exactly what ADR-0041's deploy-first stance predicted: unvalidated production surface hiding behind green suites — and the M1 operator checklist is now a procedure that has survived one full execution.

**2026-07-10/11 — Driver-app D4 SHIPPED (foreground on-trip GPS producer; ADR-0035, the resumed D4–D6 stream): the FIRST real producer to feed the inert ADR-0029 telematics backend, verified end-to-end on an emulator against a live API.** The PO re-prioritized the parked D4–D6 stream on 2026-07-10 (the deploy-first alternative was surfaced with ADR-0042's own deprioritization rationale and declined; ADR-0041 c2 in-phase completion — no fresh ADR); the ADR-0033 c7 build-path gate was discharged the same day (**local prebuild** over EAS; PR-1). D4 landed as two stacked PRs. **API half:** DRIVER granted `gps:ingest` **atomically with** its row-level scope (ADR-0034 c5's hard rule) — a new `TelematicsService.assertDriverCanIngest` runs pre-enqueue and requires every ping in a DRIVER batch to carry the driver's **own IN_PROGRESS trip** (tripId mandatory, `trip.driverId` resolved through the `Driver.userId` link via `DriverScopeService`, `ping.vehicleId === trip.vehicleId`); **any violation rejects the whole batch 403 fail-closed**, matching the worker's all-or-nothing `createMany`; ADMIN's synthetic path (ADR-0029 c11) is unchanged. The ingest route now emits the **`telematics_ping_freshness` SLI** per accepted batch (ADR-0026 c6's provisional 95.0% target consumed, ADR-0035 c8's on-trip+foregrounded window; pino-field idiom, timestamps-only never coordinates, a documented provisional 120s bound). Tests: the rbac matrix + `permissions.test.ts` pin the grant; `telematics.controller.test.ts` pins the own-trip 202/403 matrix over real rows (own-trip 202 / foreign 403 / unlinked 403 / missing-tripId 403 / vehicle-mismatch 403 / non-IN_PROGRESS 403 / mixed-batch whole-batch 403 / ADMIN-unscoped 202), mutation-tested (drop the driverId comparison → exactly the ownership tests fail); boot-smoked. **Mobile half:** `expo-location`'s `watchPositionAsync` drives a foreground-only capture bound to the active trip — the pure half (`src/gps.ts`: wire-ping construction against the `.strict()` contract, batch chunking, flush decision) is unit-tested (12 new jest), the native shell (`src/gps-task.ts`) is untested per the ADR-0033 c4 binary-free gate; capture starts after the trip-start PATCH, drains-then-stops before the trip-stop PATCH (ordering is load-bearing against the server predicate), self-heals on trips-load after a relaunch, and degrades honestly on permission denial / Expo-Go. **DURABLE FINDING — the mechanism refinement (ADR-0035's 2026-07-11 annotation):** the first D4 build used c1's literal foreground-SERVICE + `expo-task-manager`, which **hard-crashes on Android 14+/API 35** when a fix arrives backgrounded (a `location`-type FGS can't start from the background without `ACCESS_BACKGROUND_LOCATION`; a JS-side stop races the native broadcast receiver) — caught by the **live emulator E2E** (the app crash-looped). Switched to `watchPositionAsync` (no background receiver, no FGS type → structurally can't hit it), which exactly implements c8's foreground-only D4 window; the foreground service + persistent notification + background permission + encrypted outbox is **D5's slice** where background permission makes it stable. **E2E VERIFIED (real APK on an android-35 AVD, real dev API, injected GPS route):** driver login → start trip → foreground capture (OS location indicator) → **7 GpsPing rows** for the own trip (the predicate 202'd them) → `telematics_ping_freshness` `sli_good:true` with **batch sizes 4 then 2** exactly matching the flush logic → `/telematics/positions/latest` (the `/map` consumer) shows the live moving fix → stop trip drains the tail then capture stops (a post-stop fix produced no ping). **The build path itself was proven for the first time anywhere** (PR-1: `openjdk@17` formula [the temurin cask needs sudo — corrected], `expo prebuild` + Gradle → a 132 MB debug APK, android-35 AVD; three executed-truth runbook fixes). D5 (background + SQLCipher outbox + SyncManager + heartbeat) and D6 (geofence context + DRIVER `gps:read-derived`) remain; the deploy-gate items are unmoved (real production use still M1-gated — trackers remain the primary producer per ADR-0042).

**2026-07-11 — Driver-app D5 SHIPPED (background GPS + the SQLCipher-encrypted offline outbox + the SyncManager; ADR-0035, the resumed D4–D6 stream): capture now survives screen-off, backgrounding, dead zones, and app kills — the durability half that makes phone GPS genuinely useful.** ONE PR, base main (the stacked-merge orphaning lesson applied). **API half:** the D4 ingest predicate conflicted with ADR-0035 c5's ratified late-delivery discharge (it required IN_PROGRESS at delivery time, which would 403 exactly the offline-buffered tail a stopped trip drains later) — `assertDriverCanIngest` now also accepts the driver's own **COMPLETED** trip when the fix timestamp lies inside `[startedAt − 5 min, endedAt + 5 min]` (`INGEST_WINDOW_SLACK_MS`, documented provisional); PLANNED stays 403 and **CANCELLED stays 403 (recorded judgment call: no `endedAt` requirement → no bounded window → unbounded spoof surface)**; plus a parser-cap consistency fix (json body limit `1mb` bound to `BATCH_MAX` — the ratified 1000-ping batch is ~150–230 KB and would have 413'd at the ~100 KB default before validation ever ran), all mutation-tested. **Mobile half:** capture is permission-gated onto the D5 mechanism — the foreground service + persistent notification (`killServiceOnDestroy: false`) under `ACCESS_BACKGROUND_LOCATION`, with the D4 watch as the honest no-permission fallback — and BOTH paths write straight to the **SQLCipher-encrypted outbox** (raw-key `PRAGMA key` from expo-crypto via expo-secure-store; per-row autocommit inserts; 48 h age-out + 20 k row cap, provisional); the **SyncManager** (pure jest-pinned core + native glue) is the single sender — NetInfo online-transition, AppState foreground, a 30 s tick, capture events, and the trip-stop `drainOutboxNow` boundary trigger drains of `DRAIN_BATCH=300` (pinned ≤ the server cap by test), delete-on-202 (at-least-once), 4xx-drop with count-only warnings, 5 s→5 min jittered backoff, and a cookie interlock so a logged-out/headless drain can never 401 the driver into a sign-out; the onboarding card runs the full permission ladder ("all the time" → notifications → a battery-optimization pointer with Nepal-OEM copy), declining degrades honestly. The trip binding survives headless relaunches in secure-store; reconcile re-binds from the server's answer and converges capture onto the best mechanism. **E2E-VERIFIED on the API-35 emulator (all five planned scenarios plus the ladder itself):** normal capture→outbox→drain→rows; backgrounded + screen-off capture with delivery continuing and ZERO crashes (the exact D4 FGS crash class shown dead); offline buffering → reconnect drain (backgrounded, moving coordinates, capture-time stamps); force-stop survival (pre-kill fixes delivered after relaunch) + **encryption at rest proven by `sqlite3` refusing the pulled file**; and c5 LIVE — fixes captured offline mid-trip, trapped through a force-stop, accepted (202) on the trip AFTER the office completed it, including a fix 1 s past `endedAt` (inside the slack), with an out-of-window hand-crafted ping 403'd. **Five Android-truths were discovered live and fixed in-slice** (persisted-job delivery requires `RECEIVE_BOOT_COMPLETED` or every fix fatally crashes the process; a location-FGS start is silently refused — warning + RESOLVED promise — unless the app is truly active, so starts gate on AppState and cycle stop→start; `hasStartedLocationUpdatesAsync` reports registration not service state; an open transaction from a paused backgrounded JS thread wedges the shared SQLite connection → autocommit; backgrounded `setInterval` throttling → capture-driven drain), all recorded in the ADR-0035 D5 annotation + dev-setup. The c1 heartbeat question is closed as satisfied-at-vehicle-level (M7 fixAgeSeconds + M8 freshness + M9 map; the per-trip signal is the named revisit if phones ever become primary). Glossary: **Outbox / SQLCipher / NetInfo**. D6 (geofence context + DRIVER `gps:read-derived`) remains; then Phase-3 SMS (fresh ADR). Production use stays M1-gated; trackers remain primary (ADR-0042).

**Trip dispatch + driver acceptance (ADR-0047), Phase-5 scope pulled forward, COMPLETE (W1–W9).** ADR-0047 (dated 2026-07-12) is **ratified and on `main`** — the PO's merge of the W1 governance PR is its durable ratification (an agent never self-accepts it, per the ADR-0038 rule its Status line cites; the deploy-first recommendation was surfaced and declined, and its §"Phase mismatch" is the fresh argument ADR-0041 commitment 4 requires). The dispatch program built it as a nine-ticket vertical-slice sequence, each its own PR, PO-ratified by merge, never self-merged. This is **Phase-5 headline material** — the roadmap's §"Phase 5 — Dispatch and manager mobile": "job assignment and dispatch UI" — and the **fifth exercise of ADR-0041 commitment 4** (after the map ADR-0042 [M], the agent ADR-0043 [A], data-entry ADR-0044/0045 [V], and WhatsApp ADR-0046 [W]). It **completes, does not replace, ADR-0003's central Trip aggregate**: the Trip was a "skeleton" that carried no order — haul *which* material, *from* where, *to* whom, *for* whom — and ADR-0003 explicitly reserved it as "the first place we will ADR-revise"; this reshape finishes it, and doing it **before** production trip data exists is the *lower-risk* ordering (far cheaper than migrating live trips afterward — the §"Phase mismatch" argument the PO accepted). The workflow is **ride-hailing-style hard-assign + driver-acknowledge** (not marketplace matching): the admin dispatches a trip to a chosen driver + vehicle carrying the order; the driver receives a request in the phone app and **accepts** it (accept-only, *no in-app decline* — a driver who cannot take it is handled out-of-band and the admin **reassigns** by editing the OFFERED/ACCEPTED trip); then drives it, tapping progress. What landed, ticket by ticket: **W1** the ADR + the ADR-0003/0013 annotations + DESIGN.md §"Sites"/§"Trip dispatch" (governance + design, no code). **W2** the schema + hand-authored migration — a new `Site` model (the ADR-0029 hybrid PostGIS **Point** pattern; `SiteKind` = CRUSHER/PIT/DELIVERY_SITE/DEPOT/OTHER), the `MaterialType` enum, the Trip order columns (material + note, pickup/drop-off Site FKs `onDelete: Restrict`, consignee name/phone, load count, instructions, docket) + the six nullable **milestone timestamps** + the `OFFERED`/`ACCEPTED` `TripStatus` values, and the consignee/site-contact Tier-2 redact paths (the ADR-0013 amendment, c6). **W3** the Sites CRUD module + the new `sites:*` capability (ADMIN + OFFICE_STAFF, the Customer/Geofence module pattern). **W4** the Trip dispatch API — the `PLANNED → OFFERED → ACCEPTED → IN_PROGRESS → COMPLETED` transition matrix (with the `OFFERED → PLANNED` reassign-back and the `PLANNED → IN_PROGRESS` back-compat direct start kept), the order fields + milestone timestamps on both `.strict()` Create/Update schemas, the OFFERED-order-required + monotonic-milestone rules, server-stamped `offeredAt`/`acceptedAt`, the pickup/drop-off-Site FK 400s (`assertSitesExist`), and the `LIST_SELECT`/`DETAIL_INCLUDE` projections. **W5** the admin Sites web surface (`/sites` CRUD with a single-Point draggable-marker Leaflet pin editor, `lib/sites-schema.ts`, a `Sites` nav entry, the client-only `site-map-editor.tsx` island). **W6 (PR #236)** the admin dispatch UI + the decoupled provider-agnostic **`RoutingProvider`** seam — a `ROUTING_PROVIDER`-selected abstract DI token with a deterministic **no-network `MockRoutingProvider`** for dev/test/CI (haversine × road-factor + a bézier-bowed `[lat,lng]` route line) and an **M1-gated** `LiveRoutingProviderStub` — plus the `trips:*`-gated `POST /api/v1/routing/route-preview` endpoint (reachable by the DRIVER, a pure coord calc) that draws the route polyline + an "≈ 45 min · 32 km (estimated)" ETA; the web OFFERED/ACCEPTED vocab + status `<Badge>`s, the shared dispatch Order form section on both trip forms, the trip-detail Order section + a six-milestone progress timeline, and the code-split Leaflet live-tracking island. **W7 (PR #237)** the driver **Requests** tab + **Accept** + the shared order-detail view + the **Navigate deep-link** — the driver's *authoritative* turn-by-turn is the device's Google Maps via `navigateUrl(lat,lng)` (no API key, no cost, c9), reached by the one API change (widening the pickup/drop-off Site projection to carry `latitude`/`longitude` on the driver's OWN trip, since a DRIVER holds `trips:*` not `sites:*`); the one real code break `isStartable` moving `PLANNED → ACCEPTED` (c7/c8); the mobile `TripStatus` union gaining `OFFERED`/`ACCEPTED` (the **fifth** lock-step status mirror moved). **W8 (PR #238)** the driver **inline map** (`@maplibre/maplibre-react-native` v11, OSM raster, no API key, behind the custom dev build, mocked into the jest-expo gate) beside the deep-link — consuming the W6 `route-preview` for a route/ETA **preview** (the `[lat,lng]→[lng,lat]` GeoJSON swap lives in one tested place) — plus the four **live progress taps** (Arrived at pickup → Loaded → Arrived at drop-off → Delivered), each PATCHing its own-trip milestone timestamp (no status change, monotonic; a done tap reads as its label + a device-local HH:MM, "a timestamp, not a toggle"). **W9 (this PR, the program's last ticket)** the docs/memory close-out — the glossary (**Dispatch** / **Site** / **MaterialType** / **consignee** / **milestone timestamps** + the extended **Trip** / **trip.status** entries), this running-log entry, the roadmap Phase-5 pull-forward annotation, and the tech-debt refresh — **no code / schema / migration / API / UI change**. **Disciplines reused, not re-derived:** the ADR-0029 hybrid-Point pattern for `Site`, the ADR-0042 Leaflet stack for the admin pin picker + live map, the ADR-0036 columns-on-Trip precedent for the order (dispatch:trip is 1:1), the existing own-record `DriverScopeService` scope for accept/progress (a driver still **cannot create or delete** trips — "orders come only from the admin app" holds by construction), and the ADR-0013 Tier-2 redaction for consignee/site contacts. **Intra-trip progress is timestamps, not a status explosion** (c1) — the single biggest anti-over-engineering call, matching how mature dispatch systems track stop-level stages within one in-progress order. **HONEST framing — dispatch is BUILT but cannot yet DISPATCH a real trip:** like every prior commitment-4 exception, all production use gates on the **M1 first deploy** (the driver app is the API's consumer; no trip is really dispatched until FleetCo runs in production), which stays owed and operator-led. **Explicitly deferred (`docs/tech-debt.md`):** the standing **vehicle↔driver assignment** (a persistent pairing, distinct from per-journey dispatch), `Trip.jobId` + trip-level pricing (their own ADR-0003-touching decisions — the order attaches per-dispatch on Trip, not via a Job), the `Site.geofenceId` auto-arrival link (c4), weighbridge/POD-photo/per-load pricing (c11), and the cross-slice `pickupSiteId`/`dropoffSiteId` trips-list filter W6 flagged. This is the **fifth pull-forward** widening the "built-but-unused" surface ADR-0041/0025 flag — accepted as the PO-decided cost, and (per the ADR) the *lower-risk* ordering for this specific central-aggregate reshape precisely because it precedes production data. **With W9 the nine-ticket trip-dispatch program (ADR-0047, W1–W9) is COMPLETE.** See ADR-0047, ADR-0003, and the glossary's **Dispatch** / **Site** / **MaterialType** / **consignee** / **milestone timestamps** / **Trip** / **trip.status** entries.

**2026-07-13 — Driver-app D6 SHIPPED (geofence context + the DRIVER `gps:read-derived` grant; ADR-0035, the LAST slice of the resumed D4–D6 stream).** D6 completes the ADR-0035 driver-GPS producer stream and closes ADR-0034 c6's lean driver capability set. The API change is a capability grant landed ATOMICALLY with its own-record scope (ADR-0034 c5's hard rule — the read twin of D4's ingest predicate): DRIVER now holds `gps:read-derived`, and `TelematicsService.assertDriverCanReadVehicle` scopes a driver to the derived status of the vehicle on their OWN `IN_PROGRESS` trip (403 fail-closed otherwise — there is no direct Driver→Vehicle link, so the check is a scoped Trip lookup), while the fleet-wide `/positions/latest` route stays 403 for a DRIVER (`assertCanReadFleetPositions`). This is the load-bearing part: the single capability opens all three derived read routes (`/vehicles/:id/location`, `/vehicles/:id/geofence-status`, `/positions/latest`) at the RolesGuard gate at once, so the SERVICE predicate is what narrows a driver to their own vehicle — without it a driver would read the whole fleet. The driver app gains an "Arrival (from GPS)" indicator on the order-detail view (IN_PROGRESS only): it polls the derived geofence status of the driver's own vehicle against a proximity CIRCLE around each pickup/drop-off `Site` pin (`ARRIVAL_RADIUS_M = 150 m`, an owner-level provisional) and renders Arrived / Not yet / Location unknown beside the manual progress taps. Circle-mode against the `Site` POINT — **NOT** a stored `Geofence` per site: `Site.geofenceId` automatic arrival *detection* (auto-stamping `arrivedPickupAt`) stays deferred (ADR-0047 c4/c11; `docs/tech-debt.md`), and a DRIVER does not hold `geofences:read`. No schema change, no new ADR (ADR-0041 c2 in-phase completion). Tests: +7 API tests over real Postgres pin the own-vehicle-200 / other-vehicle-403 / completed-trip-403 / fleet-route-403 / unlinked-driver-fail-closed matrix (the `permissions` / `rbac.matrix` cap assertions flipped to reflect the grant); +14 mobile unit tests pin the pure arrival helpers; API + mobile typecheck/lint green. See ADR-0035's and ADR-0034's 2026-07-13 D6 annotations. **Governance consequence:** with D6 the D4–D6 stream — the ONE in-phase feature stream ADR-0041 commitment 2 permitted — is complete; per ADR-0041's "Revisit when," the first production deploy is now the **sole remaining path forward** for feature progress and should not be deferred further.
