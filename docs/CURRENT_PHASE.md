# Current phase

**Phase 1 — The Spine**

Phase 0 (Kickoff) is complete: the monorepo scaffold, NestJS API skeleton, Next.js admin skeleton, Postgres + Redis via Docker Compose, Prisma baseline + auth migration, CI on GitHub Actions, the security baseline jobs from ADR-0012, the baseline documentation, the agent guardrails, the orchestration loop (ADR-0022), and basic auth scaffolding for a single admin user are all in place.

Phase 1 replaces spreadsheets and paper for daily fleet operations. The system in Phase 1 is web admin only, with the CEO as the only user. The phase is built as a sequence of vertical slices per ADR-0006 (schema → migration → service → API → UI → test), in this order: Vehicles, Drivers, Customers, Jobs, Trips, Fuel logs, Expense logs, Reports v1. Operational milestones (first production deploy, first restore-from-backup test, SLI instrumentation, OpenTelemetry, performance budget) are interleaved across the same window.

The first three Phase-1 aggregates have shipped: Vehicles (iters 1–4: schema + read + write + tests), Drivers (iters 6–7: read + write + tests), and Trips (iters 8–9: read + write + tests, with the FK Restrict → 409 mapping closing the cross-aggregate delete-blocker loop). Iter 10 landed the first cross-slice read affordance — "Recent trips" sections on the Vehicle and Driver detail pages, plus a "View N trips" deep-link inside the 409 delete-blocker dialogs. Iter 11 closed the Trip → Vehicle odometer auto-update loop: when a Trip transitions to COMPLETED, the service bumps the referenced Vehicle's `odometerCurrentKm` inside the same Prisma interactive transaction as the trip write, so a vehicle's odometer reading on the detail page reflects the latest completed trip without operator intervention. Iter 12 added per-vehicle lifetime stats (completed trip count, total km logged, most recent driver) under `GET /api/v1/vehicles/:id/stats`, surfaced as a "Lifetime stats" section on the Vehicle detail page. Iter 13 shipped the symmetric per-driver lifetime stats — `GET /api/v1/drivers/:id/stats` plus the matching "Lifetime stats" section on the Driver detail page — rotated 90° around the Trip aggregate (most-recent-vehicle replaces most-recent-driver). Iter 14 extended the Vehicle aggregate with Nepal-specific compliance metadata: eight nullable columns plus an `InsuranceType` enum (Bluebook number + expiry, insurer + policy number + type + expiry, route permit number + expiry), surfaced on the Vehicle create form, edit form, and a new "Compliance" section on the detail page. Bishesh Anumati, expiring-soon badges, and the `<NepaliDate>` BS-calendar component remain deferred to their own slices. Iter 15 opened the Customers aggregate with the read path: `model Customer` (name, contactPerson, phone, email, panNumber unique-when-present, address, `CustomerStatus` enum ACTIVE/INACTIVE, plus the standard audit fields and `createdBy` FK), `GET /api/v1/customers` with status filter + sortable columns (name / createdAt) + pagination, `GET /api/v1/customers/:id` with 404 mapping, list / detail / loading UI under `apps/web/src/app/customers/`, and a fourth nav button on the gated home page. The pino redact list gains `*.contactPerson` so the new Tier 2 PII does not leak into logs. Iter 16 (this PR) closes the Customers aggregate by adding the write path: `POST /api/v1/customers` (201 + created row), `PATCH /api/v1/customers/:id` (200 + updated row), `DELETE /api/v1/customers/:id` (204), each guarded by `AuthGuard`; service-layer PAN normalization (trim + uppercase) makes the unique index case-insensitive in practice; Prisma `P2002` on `panNumber` is mapped to HTTP 409 with the response body `{ statusCode: 409, message, field: "panNumber" }` so the matching create / edit forms (under `apps/web/src/app/customers/new/` and `apps/web/src/app/customers/[id]/edit/`) surface the conflict inline on the PAN input via `form.setError`; Prisma `P2003` on `DELETE` is mapped to HTTP 409 with "Cannot delete customer: it is referenced by other records." — forward-compatible dead code today, exercised in iter 17 when Jobs adds its `customerId` FK under `onDelete: Restrict`. The detail page gains an Edit / Delete cluster mirroring Drivers iter 7; the delete dialog is a shadcn `AlertDialog` client island with a placeholder "View related records" link that iter 17 will swap to a `/jobs?customerId=…` deep-link. Iter 17 opened the Jobs aggregate with the read path: `model Job` (`jobNumber` unique [`JOB-YYYY-NNNNN`; generator lands iter 18], `customerId` FK Restrict to Customer, free-text `description`, `JobStatus` enum PLANNED/IN_PROGRESS/COMPLETED/CANCELLED, nullable scheduled/actual start+end dates, nullable `notes`, audit fields), `GET /api/v1/jobs` with status + customerId filters, sortable columns (jobNumber / scheduledStartDate / createdAt), pagination, `GET /api/v1/jobs/:id` returning the nested Customer, list / detail / loading UI under `apps/web/src/app/jobs/`, and a fifth nav button on the gated home. The Job→Customer FK flips the iter-16 forward-compatible Customer-delete-blocker from dead code to live: an integration test seeds a Customer + a referencing Job, attempts the delete, and asserts the `P2003 → 409` "Cannot delete customer: it is referenced by other records." path. Iter 18 (this PR) closes the Jobs aggregate by adding the write path: `POST /api/v1/jobs` (201 + created row with the nested Customer), `PATCH /api/v1/jobs/:id` (200 + updated row), `DELETE /api/v1/jobs/:id` (204), each guarded by `AuthGuard`; the jobNumber generator runs server-side (`JOB-YYYY-NNNNN`, current UTC year + highest existing per-year sequence + 1, retried up to 3 times on `P2002` so a concurrent insert cannot deterministically collide); `jobNumber` and `customerId` are immutable post-create (the `PATCH` schema's `.strict()` rejects both); the four date pairs (scheduled start/end, actual start/end) enforce end ≥ start as a service-layer cross-field rule that re-runs against the merged shape on `PATCH` (so a PATCH that touches a single date in a pair re-validates against the stored value for the other half); `P2002` on `jobNumber` is mapped to HTTP 409 with the response body `{ statusCode: 409, message, field: "jobNumber" }` so the matching create form can surface the (effectively unreachable, defensively handled) conflict inline; `P2003` on `customerId` is mapped to HTTP 400 with "Customer <id> does not exist." which the create form surfaces as a field-level error on the customer picker. Create / edit / delete UI lands under `apps/web/src/app/jobs/new/` and `apps/web/src/app/jobs/[id]/edit/` plus `[id]/delete-job-dialog.tsx`; the create form fetches the active customers list (sortBy=name asc, take=200) server-side and renders a native `<select>` picker; the edit form pre-fills every field, renders jobNumber + customer name as read-only, and PATCHes only the diff against initial values; the delete dialog is a shadcn `AlertDialog` client island with named "Delete job" / "Keep job" buttons. The Customer delete-dialog placeholder "View related records" link is swapped to the now-live `/jobs?customerId=<id>` URL with the clearer label "View jobs for this customer". Iter 19 (this PR) opens the Fuel logs aggregate with the read path: `model FuelLog` (`vehicleId` FK Restrict required, nullable `tripId` FK Restrict — a fill exists whether or not the operator pairs it with a trip; `date` UTC; integer `litersMl` per the money-as-minor-units rule mechanically extended to volume; integer `pricePerLiterPaisa`; integer `totalCostPaisa` carried over the wire today, derived server-side in iter 20; nullable integer `odometerReadingKm`; nullable free-form `station`, `receiptNumber`, `notes`; `createdById` FK Restrict to User), with indexes on `vehicleId`, `tripId`, `(date desc)` partial, and `createdById`; `GET /api/v1/fuel-logs` with vehicleId / tripId (cuid) and startDate / endDate (inclusive) filters, sortable columns (date — the default — and createdAt), pagination clamped to 200, and the standard `{ items, total, skip, take, sortBy, sortDir }` response shape; `GET /api/v1/fuel-logs/:id` returning the nested Vehicle (always present) and Trip (nullable); 35 new tests cover the service (filters, sort round-trip, take clamp, skip-past-end, pagination stability with `id` tiebreaker) and the controller (pipe-layer schema validation rejecting bogus keys / out-of-range bounds / non-cuid ids, controller integration shape with real Prisma). List / detail / loading UI under `apps/web/src/app/fuel-logs/` with the five-column list table (Date, Vehicle registration, Liters, Price / L, Total) and a sixth nav button "Fuel logs" on the gated home page after Jobs. Two new web lib modules centralise integer-minor-units formatters per CLAUDE.md §"Money & units": `apps/web/src/lib/money.ts` (`formatNpr(paisa)` — Intl en-IN currency, em-dash for null) and `apps/web/src/lib/units.ts` (`formatLiters(milliliters)` + `formatKm(km)`); both docstrings carry the round-trip note that the helpers are display-only — paisa and milliliters stay integers end-to-end in code, only stringified at render. Iter 20 (this PR) closes the Fuel logs aggregate by adding the write path: `POST /api/v1/fuel-logs` (201 + created row with the nested Vehicle and Trip), `PATCH /api/v1/fuel-logs/:id` (200 + updated row), `DELETE /api/v1/fuel-logs/:id` (204), each guarded by `AuthGuard`; `vehicleId` is immutable post-create (the `PATCH` schema's `.strict()` rejects it; the edit form renders the vehicle registration as a read-only disabled input) while `tripId` is mutable on `PATCH` (operators correct a mis-paired fill without re-keying the rest); `totalCostPaisa` is **derived** server-side via `Math.round(litersMl * pricePerLiterPaisa / 1000)` — half-up rounding so the worked example `12345 mL × 11055 paisa/L = 136473.975` rounds to `136474` paisa, never `136473`; the wire schemas (`CreateFuelLogSchema` + `UpdateFuelLogSchema`, both `.strict()`) explicitly omit `totalCostPaisa` and `createdById` so a client cannot pass either; `createdById` is filled from `request.session.user.id` per ADR-0021; a service-layer trip-vehicle consistency check rejects a `tripId` whose Trip belongs to a different Vehicle with "Trip <reg> belongs to vehicle <reg>; cannot pair with a fuel log for vehicle <reg>." (re-runs against the merged shape on `PATCH` so changing only `tripId` while leaving `vehicleId` alone still validates against the stored vehicle); Prisma `P2003` on insert / update is mapped to HTTP 400 with "Vehicle <id> does not exist." / "Trip <id> does not exist." which the web `actions.ts` layer pattern-matches to set the right field token (`vehicleId` / `tripId`) so the form highlights the right picker — see `docs/runbook/api-error-mapping.md` for the full P2003 split (delete-when-referenced → 409, insert/update with stale FK → 400). Odometer monotonicity (rejecting a fuel log whose `odometerReadingKm` regresses against the most-recent prior reading on the same vehicle) is explicitly deferred to a later iter and marked with a `// FUTURE` comment in `FuelLogsService.create`; the kickoff treats odometer monotonicity as a cross-aggregate invariant best landed alongside trip-end-odometer reconciliation. Create / edit / delete UI lands under `apps/web/src/app/fuel-logs/new/`, `apps/web/src/app/fuel-logs/[id]/edit/`, and `[id]/delete-fuel-log-dialog.tsx`, mirroring the Jobs iter-18 shape: the create form fetches active vehicles + recent trips server-side, the trip picker is filtered client-side to the watched `vehicleId` (and `tripId` is cleared whenever the vehicle changes), liters / price-per-liter are entered as decimals (step `0.001` / `0.01`) and converted to integer mL / paisa at submit via `litersToMl` / `rupeesToPaisa` in `apps/web/src/lib/fuel-logs-schema.ts`, and the total-cost preview re-runs `Math.round(liters * pricePerLiter * 100)` so the rendered preview equals the persisted `totalCostPaisa` bit-for-bit; the edit form pre-fills via inverse converters (`mlToLitersInput`, `paisaToRupeesInput`), PATCHes only the diff against initial values, and uses the `in` operator on the diff object so a nullable field touched-and-cleared sends `null` while an untouched field is omitted; the delete dialog is a shadcn `AlertDialog` client island with named "Delete fuel log" / "Keep fuel log" buttons. Iter 21 (this PR) opens the Expense logs aggregate with the read path: `model ExpenseLog` (**nullable** `vehicleId` FK Restrict to Vehicle [nullable because a vehicle-agnostic expense like the company's quarterly insurance premium is a valid ledger row], nullable `tripId` FK Restrict to Trip, `date` UTC, `category` the new `ExpenseCategory` enum [`MAINTENANCE` / `REPAIR` / `TOLL` / `PARKING` / `INSURANCE` / `PERMIT` / `FINE` / `OTHER` — the eight values cover the CEO's paper-ledger categories; adding a ninth is a one-line migration], integer `amountPaisa` per CLAUDE.md's money-as-minor-units rule [unlike Fuel logs there is no derived total — `amountPaisa` is the authoritative entered value, not a product of two factors], nullable free-form `vendor` / `receiptNumber` / `notes`, audit fields), `GET /api/v1/expense-logs` with vehicleId + tripId + category + startDate / endDate filters, sortable columns (date / amountPaisa / createdAt — `amountPaisa` is on the whitelist because "biggest expense first" is a routine query and we'd rather expose it via the list endpoint than add a dedicated reports route), pagination, `GET /api/v1/expense-logs/:id` returning the nested Vehicle (nullable) and Trip (nullable), list / detail / loading UI under `apps/web/src/app/expense-logs/` (the list em-dashes the Vehicle / Trip columns when null; the detail page swaps the Vehicle section for a "Not vehicle-attributable" notice when `vehicleId IS NULL`), and a seventh nav button on the gated home page. 39 new tests cover the service (filters compose; default date desc; vehicleId narrows AND excludes null rows while omitted filter includes them; tripId / category filters; date range; AND combos; take clamp; skip-past-end; sortBy round-trip; pagination stability; findById / getById happy + miss) and the controller (pipe-layer .strict() / sortBy-whitelist / category-enum / cuid / date defenses; integration list with the nullable-vehicle case; getById nested shape; getById 404). The remaining Phase-1 slices are the Expense logs write path (iter 22 — `POST` / `PATCH` / `DELETE`, with `vehicleId` becoming **immutable on PATCH** the same way Fuel-log iter 20 made it immutable for the same "rewriting cost-basis history" rationale) and Reports v1; operational milestones (first production deploy, first restore-from-backup test, SLI instrumentation, OpenTelemetry, performance budget) interleave across the same window. See `docs/product/roadmap.md` §"Phase 1 — The Spine" for the full Phase 1 scope and operational commitments. When the last Phase 1 slice (Reports v1) ships and the CEO is using FleetCo daily, this file is updated to indicate Phase 2 is active.
