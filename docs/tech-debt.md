# Tech Debt Register

This is the project's prospective memory for items that have been deliberately deferred and must not be lost. The tech debt register is not a wishlist of features and not a roadmap of phases; it is the place where small commitments that emerged during work-in-flight are held until they are addressed. The discipline is that nothing accepted as a future commitment leaves the conversation that produced it without being written here.

Each entry has a short title, a brief description of what is owed, the slice or PR that introduced or surfaced the debt, the reason the debt was accepted (rather than fixed in place), and a rough estimate of the work required to discharge it. Entries are removed from the register only when the debt is genuinely paid off, and the PR that pays it off mentions the debt entry in its description.

## Active debt

This section lists active debt entries.

### Secret scanning and secret scanning push protection unavailable at current GitHub plan tier

- **What is owed:** Enable repository-level secret scanning and secret scanning push protection for the `addressanup/fleetco` repository, satisfying in full the four-feature commitment in ADR-0012. Today, only two of the four (Dependabot alerts and Dependabot security updates) are enabled.
- **Where surfaced:** Phase 0 — Ticket 1 (repository bootstrap), first commit on `main`. The `gh api -X PATCH` calls to set `security_and_analysis.secret_scanning.status=enabled` and `security_and_analysis.secret_scanning_push_protection.status=enabled` both returned HTTP 422 with body `"Secret scanning is not available for this repository."` The repo's `security_and_analysis` field comes back as `null`, GitHub's signal that the feature is not offered for this repo/account combination — not a payload error. Verified by also attempting `secret_scanning` alone, which fails identically.
- **Why accepted now:** GitHub does not include repository-level secret scanning on private repos at the Free plan tier. Enabling it requires the GitHub Secret Protection add-on (≈$19/active-committer/month at announced pricing) or the larger GitHub Advanced Security bundle. Phase 0 contains no real secrets in the repository (only documentation), so the absence of repo-level scanning is low-risk in the immediate term. Defense-in-depth is partially provided by (a) GitHub's user-level push protection, which scans every push from the account across all repos and is free and on by default, and (b) the Semgrep job arriving in Ticket 12 per ADR-0012, whose `p/security-audit` ruleset includes secret-pattern detection that overlaps with what GitHub's scanner would catch.
- **Estimate to discharge:** ~10 minutes once the account is on a tier that includes secret scanning. The original `gh api -X PATCH` payload should then succeed unchanged; verification is one read-only `gh api /repos/addressanup/fleetco --jq '.security_and_analysis'` call.
- **Revisit when:** any of: (a) Phase 1 begins, because that is when real secrets (database password, Cloudflare R2 credentials, session secret, Sentry DSN) enter the project's operational surface, even if scoped to a local `.env`; (b) the project moves to a GitHub organization account or upgrades to a paid plan for any other reason; (c) GitHub changes the bundling or pricing of secret scanning in a way that makes enablement cheap or free.

### Orchestration loop's destructive-Bash blocklist will need tuning as the stack evolves

- **What is owed:** Periodically review `scripts/orchestration/src/destructive-bash.ts` against the project's current stack and operational practices. The blocklist as built is conservative (over-blocks rather than under-blocks) and tuned for FleetCo's TypeScript / NestJS / Next.js / PostgreSQL / Prisma / Docker Compose / pnpm / Slack stack at Phase 0. As the project picks up new tools (a deploy CLI, a database migration tool different from Prisma, a different package manager, a new CI helper), the blocklist will need entries added; as the project finds false positives that cause repeated operator interrupts, entries may need narrowing.
- **Where surfaced:** ADR-0022 and `docs/runbook/orchestration-loop-design.md`. The principle is documented in the design as "tune as the project's stack evolves" but no automation enforces revisit cadence.
- **Why accepted now:** A conservative blocklist on day one is the right tradeoff because the cost of a false positive is one operator interrupt while the cost of a false negative can be data loss. Over time, false-positive friction dominates and the blocklist needs trimming. Tuning requires real operational signal (which operator interrupts the blocklist caused, which destructive commands the agent attempted) that does not exist before the loop runs in earnest.
- **Estimate to discharge:** ~30 minutes per quarterly review, or as-needed when an operator interrupt traces to a blocklist false positive. The reviewer reads the recent `decisions.log` for `destructive_bash_denied` entries, judges which were legitimate denials and which were false positives, and adjusts the patterns.
- **Revisit when:** any of: (a) the loop has been used to drive at least one program of 10+ tickets and operational signal is available; (b) an operator interrupt traces to a blocklist false positive; (c) a destructive command escapes the blocklist and causes damage (treat as a SEV2 minimum and add a postmortem-driven blocklist entry); (d) the project's stack changes meaningfully (new package manager, new database, new deploy tooling).

### Orchestration loop's Slack bot capability set can be extended

- **What is owed:** The bidirectional Slack bot ships with /status, /tail, /stop, /resume, /queue, plus an AI fallback that routes free-form messages to Haiku with recent log context. If the operator finds the basic set insufficient during real use, additional commands may be valuable: `/skip-iter` (force the loop past a stuck iter by overwriting kickoff.md from a backup), `/scope-extend` (mid-program scope addition with explicit operator note), `/halt-on-budget` (configure the loop to stop automatically when the SLO error budget approaches exhaustion, once SLI instrumentation lands in Phase 1). None of these are needed at launch; all are deferred.
- **Where surfaced:** ADR-0022 and `docs/runbook/orchestration-loop-design.md`'s "things explicitly NOT built" list, and "open items (intentionally deferred)" subsection.
- **Why accepted now:** The five-command set + AI fallback is the empirically-validated minimum from the prior program run that produced approximately twenty PRs end-to-end. Additional commands add maintenance surface and registration overhead at Slack (each command needs registering in the app config and handling in `src/slack-bot.ts`). The right cadence is to add commands when the operator has felt the absence of one during a real program, not to predict them in advance.
- **Estimate to discharge:** ~30 minutes per additional command (handler in slack-bot.ts + Slack app registration + a note in the operator guide).
- **Revisit when:** the operator explicitly notes during a program retro that a specific command would have helped, or notes a recurring frustration that a bot command could automate.

### Business continuity plan carries illustrative synthetic content pending founder-supplied real values

- **What is owed:** Replace the `[ILLUSTRATIVE]`-marked values throughout `docs/runbook/business-continuity.md` (POCs block under "Designated points of contact" and protocol block under "Credentials and access") with the founder's real designated business POC, personal POC, technical substitute, and emergency-access protocol details. After the swap, rewrite the document's STATUS-line sentence to drop the "illustrative content" qualifier, promote `STATUS: STUB` to `STATUS: ACTIVE`, and update the "Last review" date if the swap-in is a separate review event rather than a continuation of the 2026-05-16 review.
- **Where surfaced:** Phase 0 — Ticket 2 (resolve founder TODOs in active runbook procedures). The kickoff plan's resolved clarifying question 3 anticipated the founder supplying real values during Ticket 2 itself, allowing the BCP to close fully with no STATUS: PARTIAL marker and no tech-debt entry. During Ticket 2 planning the CEO redirected: synthetic illustrative content now, real values later. Ticket 2 therefore partially closes — the two incident-response files close fully (date stamps are real); the BCP partially closes (structure is in place, dates are stamped, STATUS reflects the partial state honestly, and the real-content swap is recorded here as the missing piece).
- **Why accepted now:** The founder did not have all real values immediately to hand — a normal real-world circumstance: the technical substitute's retainer terms, the personal POC's information-holding scope, and the precise emergency-access wiring all require some out-of-session work to confirm. Holding Ticket 2 entirely until those answers are in hand would block the two incident-response files' date stamps for no good reason, since those carry no synthetic content and close fully. The BCP's structural improvements (status-line rewording, labeled-field layout, explicit scope-in / scope-out for emergency access) are also valuable independent of the specific names: when the real values arrive they slot into a finished container rather than into a TODO marker. CLAUDE.md Part 1's "stale memory is worse than missing memory" principle is honored by keeping STATUS at STUB and marking every illustrative value clearly, so no reader is misled into treating the document as operational.
- **Estimate to discharge:** ~30 minutes once the founder has real values in hand: read each `[ILLUSTRATIVE]`-marked value, replace it with the real value, remove the `[ILLUSTRATIVE]` markers, rewrite the STATUS-line sentence and promote `STATUS: STUB — illustrative content` to `STATUS: ACTIVE`, update the "Last review" date, and move this tech-debt entry to the paid-off section with a note referencing the swap PR.
- **Revisit when:** any of: (a) before Phase 1 ships, because Phase 1 is when real customer-facing operations begin and the BCP must be operational by then; (b) before the personal POC is asked to actually hold the envelope, because the document should describe what is in fact arranged rather than an illustrative example; (c) before the technical substitute is engaged for the quarterly verification commitment from the "What the founder commits to" section, because the substitute must be a real person to verify against.

### GPS raw-ping retention window is provisional (90 days) pending the PO's ADR-0027 number

- **What is owed:** The PO's FINAL raw-ping retention window, set at ADR-0027 acceptance. The `traces-prune` job hard-deletes `GpsPing` rows older than `GPS_PING_RETENTION_DAYS` (`apps/api/src/modules/retention/retention.service.ts`), which currently defaults to **90 days**. ADR-0027 commitment 3 fixes the policy (a short window) but explicitly leaves the number to the PO; ADR-0029 commitment 12 sanctions shipping the 90-day provisional until then. When the PO fixes the number, change that one constant. Separately, ADR-0027 c3's preferred end-of-window action is **aggregate-to-Tier-3-summary** (simplified polyline, distance, stop/geofence events), not hard-delete; that aggregation feature does not exist yet (hard-delete is the documented fallback), so building it — and switching the job's end-of-window action — is the larger owed follow-up.
- **Where surfaced:** GPS telematics retention/prune job, ADR-0029 T4 (`feat/traces-prune-retention`). The T4 kickoff defaulted to the 90-day provisional and required it be flagged here rather than halting; ADR-0027's own "Costs we accept" and "Revisit when" already name both the provisional number and the not-yet-built aggregation feature.
- **Why accepted now:** The number is a privacy/cost trade the PO owns (how far back investigations realistically reach vs. the disk budget of a single VPS), and per ADR-0025 it is being set without production telematics data — calibrating it now would be guessing on top of a guess. 90 days is the ADR-documented provisional, sized to cover realistic operational lookback (dispute resolution, fuel-theft investigation, billing reconciliation). Aggregation is real work (polyline simplification, stop detection) that ADR-0027 deliberately deferred; shipping hard-delete first keeps the highest-row-count table bounded today without blocking on a feature the PO has not yet asked for.
- **Estimate to discharge:** ~2 minutes for the window number once the PO sets it — change `GPS_PING_RETENTION_DAYS` and adjust the `retention.prune` boundary test's day offsets if the new value crosses the 89/91-day fixtures. Also revisit the prune BASIS at that point: it prunes by device fix `timestamp` today; switching to storage `createdAt` (robust to a late offline batch whose old fix-times would prune immediately) is a one-line column swap in `RetentionService.prune`, documented in that method. The aggregation feature is a separate, larger ticket (est. days, governed by ADR-0027) — out of scope for the window-number fix.
- **Revisit when:** any of: (a) ADR-0027 reaches acceptance and the PO states the final window; (b) real telematics volume and operational use exist (after the deferred first deploy, ADR-0025) and the 90-day assumption can be calibrated against how far back investigations actually reach; (c) the PO wants aggregate-to-summary instead of hard-delete at window's end (build the aggregation ticket, then change the job's end-of-window action); (d) the driver-app offline/queueing design forces the `timestamp`-vs-`createdAt` basis question (ADR-0027 c2).

### First production deploy + restore-from-backup drill remain owed (operator-led); the three deploy runbooks stay `STATUS: DRAFT` until then

- **What is owed:** The operator-only half of the Phase-1 → Phase-2 first-production-deploy gate. The agent-driveable deploy artifacts now all exist on `main` (the T1–T6 deploy-prep program — see `docs/CURRENT_PHASE.md` and the Paid-off entry below), but a real deploy has not run. Still owed, all operator-led and out of scope for the agent loop because they require real secrets / infrastructure: provision the VPS (amd64, 2–4 GB), point DNS at it, write the real `/opt/fleetco/.env` secrets, generate the `age` backup keypair + `deploy/backup.env`, run the first deploy end-to-end via `docs/runbook/deploy.md` → "First production deploy — operator checklist", and run the first restore-from-backup drill **within two weeks** of that deploy. Only after the deploy + drill pass do the three runbooks (`deploy.md`, `rollback.md`, `restore-from-backup.md`) flip from `STATUS: DRAFT` to `STATUS: ACTIVE` with a real "Last verified" date, and the first real `docs/operations/dora-metrics.md` entry + live 28-day SLI reporting begin.
- **Where surfaced:** The deploy-prep program (T1–T6, ADR-0014). The deploy-dependent gate items are tracked canonically in `docs/CURRENT_PHASE.md` and `docs/retrospectives/phase-1.md` (the Phase-1 → Phase-2 gate checklist); this entry is the register's pointer to that owed operational work so it is not lost among feature debt.
- **Why accepted now:** The first deploy is deliberately hand-bootstrapped by the operator (ADR-0014 "Costs accepted"): it needs real secrets, a real VPS, DNS, and the `age` keypair the agent loop must never create or possess (CLAUDE.md, ADR-0013). Per ADR-0025 the deploy is deferred (a scheduling decision, not a discipline waiver) while Phase 2 proceeds. The agent half was completed first so the eventual deploy is a copy-pasteable procedure backed by CI-validated artifacts rather than an improvised one.
- **Estimate to discharge:** ~1–3 hours for the first deploy following the checklist (most of it VPS / DNS / secret provisioning), plus a separate ~1–2 hours for the within-two-weeks restore drill; then ~10 minutes to flip the three runbooks to `ACTIVE` and record the first DORA entry.
- **Revisit when:** the operator schedules the first production deploy (ADR-0025's deferred item), at which point this entry is discharged in step with promoting the runbooks to `ACTIVE`.

## Paid-off debt

This section is an archive of debts that have been discharged. When an active entry is resolved, it moves here with a note about the PR that resolved it and the date. The archive exists so that future readers can see what kinds of debt the project tends to accumulate and what kinds of fixes tend to discharge them, which is itself a piece of organizational learning.

### Production deploy artifacts were "implementation follow-on" only, and the CycloneDX SBOM (ADR-0012 component 5) was deferred — paid off 2026-06-17

- **Discharged in:** the six-ticket deploy-prep program (T1–T6; T2 #122, T3 #123, T4 #124, T5 #125, T6 `feat/deploy-runbook-reconcile`). ADR-0014 named a set of production artifacts as "implementation follow-on" that did not exist — which is why the deploy / rollback / restore runbooks shipped `STATUS: DRAFT` and `deploy.md` carried an unverified assumption about the api image's migrate + seed tooling — and ADR-0012's CycloneDX-SBOM-per-image (component 5) was the one security-baseline item still deferred. Both are now discharged on `main`: `apps/api/Dockerfile` (its runtime carries the Prisma CLI + `prisma/` schema + `migrations/` + `tsx` + the seed's TypeScript sources, so `prisma migrate deploy` + `db:seed` run in-image — resolving the `deploy.md` unverified assumption) + `apps/web/Dockerfile` (Next.js `output: 'standalone'`) with the PR-time `build-images` CI gate (T1); `docker-compose.prod.yml` + `Caddyfile` + `.env.production.example` with the `deploy-config` CI job (`docker compose config` + `caddy validate`) (T2); the `push-images` job pushing `ghcr.io/addressanup/fleetco-{api,web}:<sha>` to GHCR via the automatic `GITHUB_TOKEN` with a **CycloneDX SBOM per image** — closing ADR-0012 component 5, so the security baseline is now complete (T3); `.github/workflows/deploy.yml` + `deploy/smoke.sh` + the `shellcheck deploy/*.sh` CI step (T4); `deploy/backup.sh` + `deploy/restore.sh` + the gitignored `deploy/backup.env` convention, with `restore-from-backup.md` reconciled (T5); and the reconciled `deploy.md` / `rollback.md` + the first-production-deploy operator checklist (T6). Each artifact shipped with a CI check that validates it (a broken Dockerfile / compose / Caddyfile / shell script reds its PR), so the green-before-merge gate is meaningful for the non-TypeScript files the unit tests never touch. **What is NOT discharged** — and stays open in the Active-debt entry above — is the first real deploy, the within-two-weeks restore drill, and flipping the three runbooks from `DRAFT` to `ACTIVE` (all operator-led). No deploy has happened; this program made the first one a validated copy-pasteable procedure, not the deploy itself.

### Per-vehicle cost report page declared its wire type inline; converged onto the canonical `reports/per-vehicle-cost/types.ts` — paid off 2026-06-06

- **Discharged in:** `chore/recorded-cleanups` (P3 of the polish & debt sweep program — cleanup (a) of two). `apps/web/src/app/reports/per-vehicle-cost/page.tsx` shed its two inline `PerVehicleCostRow` / `PerVehicleCostReport` interface declarations (carried since iter 23, when the report page was the shape's only consumer) and now does `import type { PerVehicleCostReport, PerVehicleCostRow } from "./types";` — the co-located canonical module the D1 Home-dashboard data layer (`apps/web/src/lib/dashboard.ts`) already consumes, so the report's wire shape now has a single source of truth. The swap is type-only and a verified null runtime/visual diff: the canonical and inline shapes are structurally identical (the canonical `PerVehicleCostReport.totals` / `companyLevel` resolve to the exact fields the inline object-literals declared). One micro-divergence from the entry's plan is worth recording: the `.map` callback is annotated `(row: PerVehicleCostRow)` so the second imported name is genuinely used — the report type references the row type internally, so a bare `import { PerVehicleCostReport }` would have left `PerVehicleCostRow` unused and tripped `@typescript-eslint/no-unused-vars`; the annotation keeps the "import both" convergence the entry specified while satisfying lint, at zero runtime cost. `import type` honors the repo's `consistent-type-imports` rule. No API / schema / dependency / test change; all five web gates (lint / format:check / typecheck / `@fleetco/web` test [96 tests] / `@fleetco/web` SSR build — the page is a server component) stayed green on the checkpoint.

### Fuel-log odometer-monotonicity check deferred — paid off 2026-06-06 (resolved as the documented decision, option (b))

- **Discharged in:** `chore/recorded-cleanups` (P3 of the polish & debt sweep program — cleanup (b) of two). The active entry offered two options: (a) a service-layer monotonicity guard, or (b) a documented, deliberate decision to leave odometer non-monotonic. P3 takes **option (b)** — there is NO service-layer guard and the `FuelLogsService` create / update logic is unchanged, so the API test suite stays green, unedited. The decision: fuel-log `odometerReadingKm` readings are **non-monotonic by design** — a hard write-time reject would fight backdated corrections, odometer swaps / replacements (a broken-odometer swap makes the new reading genuinely lower), and the future bulk-import path; the soft signal that actually matters (a per-vehicle km/L outlier) belongs with the future per-vehicle fuel-efficiency report, not the write path. Concretely: the `// FUTURE` comment in `FuelLogsService.create` was tightened from an open deferral into this recorded decision (comment-only), and the rationale was recorded in the glossary's **Fuel log** and **Odometer** entries — the two homes the active entry named for option (b). No code, schema, migration, dependency, or test change. Gates green on the checkpoint: `lint` / `format:check` / `typecheck` / `@fleetco/api` test (unchanged, still passing).

### `lucide-react` not yet adopted; sort indicator and Select chevrons use inline SVG — paid off 2026-06-06

- **Discharged in:** `feat/lucide-react-adoption` (P2 of the polish & debt sweep program). `lucide-react` (caret `^1.17.0`) was added as a direct dependency of `apps/web` and the lockfile committed so CI's `--frozen-lockfile` install passes; no new ADR was needed because DESIGN.md §Iconography and §Tables had already pre-sanctioned Lucide as the project's icon library (the design system chooses the library; an ADR is not required to consume it), and the dependency was isolated to its own PR mirroring the `bullmq` / `react-leaflet` / `nepali-date-converter` precedent. Two files shed their inline-SVG workarounds: (a) `apps/web/src/components/ui/select.tsx` — the three local inline-SVG components (`ChevronDown` / `ChevronUp` / `Check`) and their `IconProps` interface were removed in favor of `import { Check, ChevronDown, ChevronUp } from "lucide-react"`; (b) `apps/web/src/app/vehicles/page.tsx` — the `SortableHeader`'s inline `SortArrow` `<svg>` now renders Lucide `ChevronUp` (asc) / `ChevronDown` (desc), and the now-false "lucide-react is not currently installed" comment was corrected. The swap is a verified **null visual diff**: Lucide's `chevron-up` (`m18 15-6-6-6 6`), `chevron-down` (`m6 9 6 6 6-6`) and `check` (`M20 6 9 17l-5-5`) path data is byte-identical to the removed inline SVGs (confirmed against the installed `lucide-react@1.17.0`), so the only render-affecting deltas were Lucide's default `strokeWidth` of 2 and its absent `aria-hidden` — both overridden at every call-site with an explicit `strokeWidth` (1.5 for the size-4 chevrons; 1.75 for the size-4 Check and the 12px sort arrow, honoring DESIGN.md §Iconography "Stroke width", which mandates 1.5/1.75 rather than the Lucide default) and `aria-hidden="true"`. No design token, API, schema, or behavior changed; the design-token-drift test was untouched and all five web gates (lint / format:check / typecheck / `@fleetco/web` test [96 tests] / `@fleetco/web` SSR build) stayed green on every checkpoint. Two small divergences from the original entry's plan are worth recording. First, it predicted importing the `*Icon`-suffixed upstream names (`ChevronDownIcon`, …); the non-suffixed aliases were used instead — lucide-react exports both, and the non-suffix names matched the existing call-site tags for a smaller diff. Second, it predicted a strict "byte-for-byte upstream shadcn" `select.tsx`, but the explicit `strokeWidth` props are a deliberate, documented deviation: they keep the render identical to the prior inline SVGs (CLAUDE.md §"visual change is behavior change under cover of refactoring" forbids changing the rendered stroke) and they satisfy DESIGN.md's stroke-width spec, which is itself non-default for Lucide. DESIGN.md provenance records the migration (the 2026-05-26 `select.tsx` entry's local-edit (a) marked reversed, plus a new 2026-06-06 entry). With the dependency in, the next shadcn primitive copy-in (Dialog, DropdownMenu, Combobox) ships its Lucide icons as plain `import` lines with no per-icon inline-SVG substitution.

### NPR money converters live in a feature-schema module, not the canonical `lib/money.ts` — paid off 2026-05-29

- **Discharged in:** `refactor/money-converters-to-lib-money` (the T_DEBT ticket of the observability-instrumentation + cleanup program). `rupeesToPaisa(rupees: number): number` and `paisaToRupeesInput(paisa: number): string` moved out of `apps/web/src/lib/fuel-logs-schema.ts` into `apps/web/src/lib/money.ts`, where they sit next to `formatNpr` and are docstringed as its input→paisa inverse (`formatNpr` renders stored paisa for display; these convert the operator-typed rupees decimal at a form boundary into the integer paisa the API stores, and back). `fuel-logs-schema.ts` now imports both from `./money` and re-exports them; `expense-logs-schema.ts` changed its import of them from `./fuel-logs-schema` to `./money` (it already re-exported them, and continues to). Because both schema modules still export the two names, the four call-sites — `apps/web/src/app/fuel-logs/actions.ts`, `apps/web/src/app/expense-logs/actions.ts`, and the two edit forms under `fuel-logs/[id]/edit/` and `expense-logs/[id]/edit/` — are untouched: a pure relocation with no behavior change. The structural wart the entry named is resolved — the import graph no longer routes NPR money logic through `fuel-logs-schema.ts` for a non-fuel feature. The three non-money helpers (`litersToMl`, `mlToLitersInput` — volume; `previewTotalCostPaisa` — a fuel-cost derivation) deliberately stayed in `fuel-logs-schema.ts`; they are not generic money logic and have no home in `money.ts`. The active entry assumed "the existing form tests pin correctness," but there were none (the only `apps/web/test/` file was `design-token-drift.test.ts`), so the "no behavior change" claim was unverified; the discharge adds `apps/web/test/money.test.ts` (the first web form-lib unit test) pinning `rupeesToPaisa(150) === 15000`, half-up rounding at the paisa boundary (`rupeesToPaisa(150.005) === 15001`), `paisaToRupeesInput(15000) === "150.00"` and `paisaToRupeesInput(12345) === "123.45"`, and the rupees→paisa→rupees round-trip — converting that assumption into a checked invariant.

### Vehicle/Driver delete must map P2003 to HTTP 409 once Trip write path lands — paid off 2026-05-26

- **Discharged in:** Trips slice iter 9 (`feat/trip-write-path`). The two service-layer `delete` methods now catch `Prisma.PrismaClientKnownRequestError` with `code === "P2003"`, run `prisma.trip.count({ where: { vehicleId: id } })` (or `driverId`), and throw `new ConflictException(\`Cannot delete vehicle: ${count} trip${count === 1 ? "" : "s"} reference this vehicle.\`)` (or driver). The plural arm is exercised by the new Vitest cases in `apps/api/test/vehicles.service.test.ts` (count=2) and `apps/api/test/drivers.service.test.ts` (count=1), both pinning that the row remains present after the failed delete (`Restrict` prevented the delete from happening, and the service did not double-delete). The iter-8 inline FK-Restrict block in `apps/api/test/trips.service.test.ts` flipped from "throws raw P2003" to "throws ConflictException with the trip count in the message," wiring `VehiclesService` and `DriversService` into the trips test module so the assertion can call their delete methods directly. The "block-when-referenced" choice (over soft-delete) was the right call here in practice: a driver or vehicle with historical trips is a different entity from a never-existed one, and the audit value of "this row was deleted on Date X" is lower than the simplicity of "the row exists or it doesn't." Soft-delete remains an option if a future audit-trail requirement reverses the calculus. The glossary's `legal-status-transition` entry now records this iter's status-transition matrix; the existing "Soft delete" entry was already aligned with the block-when-referenced choice and needs no edit.

### API has no test framework yet; ci.yml runs no `apps/api` tests — paid off 2026-05-26

- **Discharged in:** API test framework ticket (`feat/api-test-framework`). ADR-0023 was opened to record the two decisions baked into the discharge: Vitest over Jest (one-stack consistency with `apps/web` per ADR-0019; faster cold-start; native ESM matches the rest of the monorepo) and reset-between-tests against a real `fleetco_test` Postgres database over Testcontainers or schema-per-test (deterministic, fast, and lets the migration history be the single source of truth via `prisma migrate deploy` in a one-time global setup). The discharge wires Vitest into `apps/api` with `unplugin-swc` for NestJS decorator metadata, a `tsconfig.test.json` that includes `test/**/*`, a `global-setup.ts` that runs `prisma migrate deploy` once per run, a `setup.ts` that imports `reflect-metadata` for DI, a `test/db.ts` that exposes a `resetDb()` helper running `TRUNCATE ... RESTART IDENTITY CASCADE` for inter-test isolation, and a `.env.test.example` template (with `.env.test` gitignored to allow per-machine port overrides). CI gains a `services: postgres` block on `postgis/postgis:16-3.5` and a new `pnpm --filter @fleetco/api test` step ordered after `prisma validate`. Initial coverage shipped is 39 tests across four files: (a) `auth.guard.test.ts` — session-present happy path, missing-cookie 401, expired-cookie 401, header forwarding, with `AUTH` provider stubbed and `AuthGuard` wired through a real NestJS testing module; (b) `health.controller.test.ts` — liveness `@Get()` returns `{ ok: true }` unconditionally, readiness returns `{ ok: true, db: "up", redis: "up" }` against a real `PrismaService`, readiness throws `ServiceUnavailableException` (HTTP 503) when Prisma's `$queryRaw` fails (test header comment documents that the kickoff's `/health/live` and `/health/ready` paths don't exist as routes — the implementation uses unprefixed `@Get()` and `@Get("ready")` — and the tests target the actual surface rather than imagined routes); (c) `vehicles.service.test.ts` — create-defaults (odometer back-fill), `P2002` → `ConflictException` (the api-error-mapping runbook's spec), diff-PATCH semantics (no-op update, partial update, retiredAt clear-via-null), retirement-transition rule (ACTIVE→RETIRED auto-stamps `retiredAt`; explicit `retiredAt` wins over auto-stamp; ACTIVE-on-RETIRED clears `retiredAt`; SOLD also auto-stamps; no-status-change leaves `retiredAt` alone), list() filter/sort/paginate across the four whitelist columns with stability-tiebreaker assertions; (d) `vehicles.controller.test.ts` — pipe-level tests of `ListVehiclesQuerySchema` (bogus key → 400 via `.strict()`; invalid enum → 400; off-whitelist `sortBy` → 400; `take > 200` → 400 with field-named message; valid query coerces strings to numbers and CSV to arrays; empty query produces all-undefined so the controller applies defaults) and controller-level tests with `overrideGuard(AuthGuard).useValue({ canActivate: () => true })` asserting the response shape `{ items, total, skip, take, sortBy, sortDir }`. Full suite runs in ~2.2s on the dev machine — well under the 30s budget the kickoff named for adding `pnpm --filter @fleetco/api test` to the pre-commit gate list. The five-command gate is now: `pnpm format && pnpm format:check && pnpm lint && pnpm typecheck && pnpm --filter @fleetco/api test`.

### Vehicles list-page polish (filtering, sorting, pagination) deferred from iter 3 to iter 4 — paid off 2026-05-26

- **Discharged in:** Vehicles slice iter 4 (`feat/vehicles-list-polish`). The API gained a `ListVehiclesQuerySchema` accepting comma-separated `status` and `kind`, `sortBy` from a four-column whitelist (`registrationNumber` / `odometerCurrentKm` / `acquiredAt` / `createdAt`), `sortDir` `asc|desc` (default `desc`), and the existing `skip`/`take` (capped at 200); the schema is `.strict()` so unknown query keys return 400 via `ZodValidationPipe`. `VehiclesService.list` builds the WHERE clause once for both `findMany` and `count`, and applies an `orderBy` array with a secondary `createdAt`/`id` tiebreaker for pagination stability under sort. The web `/vehicles` page is server-rendered against URL searchParams (filters, sort, skip), uses the freshly copied-in shadcn `Select` for status and kind filters, clickable column headers for sort with an inline-SVG indicator and `aria-sort` reflection, page-number pagination controls with windowed numbers + ellipsis at the bottom, and an explicit "Showing M–N of T" total-count line. Empty states are distinguished: "No vehicles match the current filters." when any filter is active, and "No vehicles registered." with a register-the-first CTA when the unfiltered list is empty. The page ships `loading.tsx` matching the new layout for transition stability. Two pieces of related debt were created in the discharge: `lucide-react` icon adoption (above) — because `apps/web` did not in fact have `lucide-react` as a dependency, the sort indicator and Select chevrons use inline SVG byte-equivalents until adoption lands — and the page-size selector (25/50/100) called out in the original entry was dropped from scope as YAGNI for the current fleet size; if the CEO ever needs it, the `take` querystring is already wired through the API.

## Notes for future contributors

If you find yourself accepting a deferred commitment in a session — "we'll come back and clean this up later," "this is a known limitation we're shipping for now," "we'll need to revisit when X happens" — write the entry here in the same PR that creates the debt. Do not let "I'll add it later" pass; later does not arrive, and the writer's context evaporates within hours. The whole point of this register is to be the durable home for deferred work, and that only functions if entries are written at the moment the deferral is decided, not at some imagined future moment when there will be time to record them.
